import{_ as t,o as a,c as o,ao as n}from"./chunks/framework.CNm0hWSs.js";const g=JSON.parse('{"title":"Methodology & provenance","description":"","frontmatter":{},"headers":[],"relativePath":"methodology.md","filePath":"methodology.md","lastUpdated":null}'),r={name:"methodology.md"};function s(d,e,i,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[n(`<h1 id="Methodology-and-provenance" tabindex="-1">Methodology &amp; provenance <a class="header-anchor" href="#Methodology-and-provenance" aria-label="Permalink to &quot;Methodology &amp; provenance {#Methodology-and-provenance}&quot;">​</a></h1><p>How every published number on <a href="./performance">Performance</a> and <a href="./coverage">Coverage</a> is produced, where it comes from, and the caveats that change how a cell should be read.</p><h2 id="The-gate" tabindex="-1">The gate <a class="header-anchor" href="#The-gate" aria-label="Permalink to &quot;The gate {#The-gate}&quot;">​</a></h2><p><strong>PB ≥ max(OpenBLAS, AOCL), per (op, size, machine), single-threaded.</strong> A ratio &gt; 1.0 means PureBLAS is faster. Both references are always measured, because a routine can beat one by a wide margin and lose to the other; the gate takes the faster of the two at each individual cell.</p><p>The comparison is made on the ratio <strong>rounded to two significant digits</strong>: <code>0.995</code> rounds to <code>1.0</code> and gates; <code>0.9949</code> rounds to <code>0.99</code> and does not. The threshold itself is unchanged at <code>1.00</code> — what is specified is the precision. Per-cell machine-state drift on this fleet runs ~1–6% (every cached arm records the anchor it was measured under), so the third digit of a ratio is not adjudicable, and the rounded value is the one these tables print. A verdict that disagreed with the number on the page would be reporting a miss at a precision the measurement cannot support. The criterion lives in exactly one place, <code>bench/gatecrit.jl</code>, and every tool that prints a verdict, colours a cell or counts a miss calls into it.</p><p>Verdicts are <strong>per microarchitecture</strong> — a single pooled verdict cannot express &quot;gates on Zen4, misses on Zen3&quot;, and pooling once misreported banded Cholesky as 0.891 failing when Zen4 alone read 1.03. Every table therefore carries one column per box, each from that box&#39;s own <code>bench/plots.jl</code> sweep.</p><p>The dev fleet, gated independently (tuning for one µarch does not transfer):</p><table tabindex="0"><thead><tr><th style="text-align:right;">µarch</th><th style="text-align:right;">host</th><th style="text-align:right;">ISA</th><th style="text-align:right;">role</th></tr></thead><tbody><tr><td style="text-align:right;">Zen4</td><td style="text-align:right;"><code>wintermute</code></td><td style="text-align:right;">double-pumped AVX-512</td><td style="text-align:right;">primary tuning target</td></tr><tr><td style="text-align:right;">Zen3</td><td style="text-align:right;"><code>galen</code></td><td style="text-align:right;">AVX2, 16 ymm registers</td><td style="text-align:right;">hardest target</td></tr><tr><td style="text-align:right;">Zen5</td><td style="text-align:right;"><code>neuromancer</code></td><td style="text-align:right;">native 512-bit AVX-512</td><td style="text-align:right;">disjoint residual profile</td></tr></tbody></table><h2 id="Measurement" tabindex="-1">Measurement <a class="header-anchor" href="#Measurement" aria-label="Permalink to &quot;Measurement {#Measurement}&quot;">​</a></h2><p><code>bench/plots.jl</code>, single-thread (<code>BLAS.set_num_threads(1)</code>), native PureBLAS API vs <code>LinearAlgebra.BLAS</code>, Float64 plus the full ComplexF64 surface. Each (op, size) is measured over repeated rounds of ABBA-alternated windows with every arm (PureBLAS, OpenBLAS, AOCL) interleaved inside one run; per-round ratios are pooled and the <strong>median</strong> is the reported number (never a mean, never a minimum). Runs are only valid at locked frequency — a floating boost clock drifts between the two windows and fabricates ratios. Reproduce:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sudo bench/fleet_freqlock.sh lock       # passive governor, boost off, pin to base clock</span></span>
<span class="line"><span>taskset -c N julia --project=bench bench/plots.jl bench</span></span></code></pre></div><p>The published artifacts are a pure function of the caches on disk and are rebuilt in one command (<code>bench/publish.sh</code>); <code>bench/check_artifacts_current.sh</code> re-renders into a temp dir and byte-compares.</p><h2 id="Provenance" tabindex="-1">Provenance <a class="header-anchor" href="#Provenance" aria-label="Permalink to &quot;Provenance {#Provenance}&quot;">​</a></h2><p>Per-box CPU, host, code commit and measurement timestamp for the caches behind every published number: <a href="https://github.com/el-oso/PureBLAS.jl/blob/master/bench/provenance.md" target="_blank" rel="noreferrer"><code>bench/provenance.md</code></a>. Cells are stamped <strong>individually</strong> — a routine untouched since an earlier run keeps that run&#39;s stamp, which the cache records per arm — so the file&#39;s headline commit is the run, not a guarantee about each cell. Nothing in the tables is hand-transcribed: <code>bench/coverage_ops.jl</code> and <code>bench/coverage_routing.jl</code> compute each verdict from the stored quantile vectors.</p><p>Both reference views (OpenBLAS and AOCL) are rendered from that one cache set in a single invocation, so their provenance is identical by construction and they cannot disagree about the same fleet. The µarch labels are stamped authoritatively into each cache header (<code>uarch=</code>).</p><h2 id="How-to-read-a-cell" tabindex="-1">How to read a cell <a class="header-anchor" href="#How-to-read-a-cell" aria-label="Permalink to &quot;How to read a cell {#How-to-read-a-cell}&quot;">​</a></h2><ul><li><p><strong>Colour bands</strong> in the per-routine tables band the shortfall (<strong>gates, i.e. ≥ 1.0 to two significant digits</strong> · <strong>≥ 0.99</strong> · <strong>≥ 0.95</strong> · <strong>≥ 0.85</strong> · <strong>below 0.85</strong>). The number is the verdict; colour only bands it, so the table reads correctly in monochrome and for a colourblind reader. A failing gate is printed at three digits and <strong>floored</strong>, so a miss can never round up to a reassuring <code>1.0</code>.</p></li><li><p><strong>A full re-sweep moves every cell by ±1–2%, without any code change.</strong> A sweep re-measures the references as well as PB: two independent all-arms runs of <code>trmm</code> on Zen3 hours apart gave 0.948 and 0.929. Only differences larger than that, or ones corroborated by a controlled same-process A/B, should be read as real.</p></li><li><p><strong>Cells within ~1% of parity are not adjudicable</strong> when the run used <code>arms=pb</code> (fresh PB arm, cached reference arms). <code>bench/gate_verdict.jl</code> models this, flags them ⚠ and widens the interval by a drift term.</p></li><li><p><strong>A small ratio is not automatically a defect.</strong> <code>bench/gate_gaps.jl</code> prints each cell&#39;s round-to-round spread and marks any shortfall smaller than its own spread <code>within spread</code>. Those need more rounds, not engineering. The genuinely open cells are the ones outside their spread.</p></li><li><p><strong>Off-lock cells are excluded, not published.</strong> A cell measured while the frequency lock was floating is not adjudicable; <code>bench/coverage_ops.jl</code> drops it, reports it on stderr, and marks the routine&#39;s cell <code>off-lock (n)</code> so a partial verdict never reads as a complete one.</p></li><li><p><strong>A large win can mean the reference did not implement the routine.</strong> AOCL&#39;s packed Cholesky is ~6× slower than OpenBLAS&#39;s (near-reference code), so beating it there means little. This does not change the gate — <code>max(OpenBLAS, AOCL)</code> already handles it — but a cell far above one reference and below the other is the tell that the high ratio measures the reference&#39;s absence, not our speed.</p></li><li><p><strong>Small-n LAPACK vs AOCL flatters PureBLAS.</strong> AOCL-libFLAME has heavy per-call overhead at tiny n (e.g. <code>potrf</code>/<code>getrf</code> show 4–160× at n ≤ 32) — that is dispatch overhead, not an algorithmic gap. The mid/large-n figures are the honest signal.</p></li><li><p><strong>Zen5 is a laptop, not a dedicated bench box.</strong> Repeated sweeps there move worst cells by several points — <code>gemvT</code> n=2048 read 1.16 and 0.977 in two runs an hour apart at the same commit. Treat Zen5 cells within a few points of 1.0 as unresolved rather than as measurements; Zen3 and Zen4 are stable.</p></li></ul><h2 id="The-AOCL-baseline" tabindex="-1">The AOCL baseline <a class="header-anchor" href="#The-AOCL-baseline" aria-label="Permalink to &quot;The AOCL baseline {#The-AOCL-baseline}&quot;">​</a></h2><p>OpenBLAS is the default oracle; AOCL (AOCL-BLIS for BLAS, AOCL-libFLAME for LAPACK) is the second, AMD-tuned baseline, hand-tuned for these exact Zen chips. Everything is single-threaded (BLIS pinned to one thread — verified: <code>dgemm</code> runs at one-core throughput), boost-locked, same methodology; the AOCL artifacts carry an <code>_aocl</code> suffix and never mix with the OpenBLAS ones. The AOCL plots cover exactly the same ground as the OpenBLAS plots — same ops, panels, size sweep and µarchs — because both views come from the same cache.</p><p>The AOCL binary is a genuinely optimized build, not a reference fallback: measured directly against OpenBLAS, AOCL-BLIS <code>dgemm</code> matches it and AOCL-libFLAME <code>potrf</code>/<code>geqrf</code> meet or beat it (e.g. geqrf 40 vs 30 GFlops). It is a <em>mixed</em> competitor rather than uniformly tougher — its <code>geqrf</code> beats OpenBLAS while its <code>getrf</code> trails it — and it is tuned first for multi-threaded EPYC, so on these single-thread Zen parts it is a fair-but-not-dominant baseline.</p><h2 id="Contamination-class-fixed-on-2026-08-09" tabindex="-1">Contamination class fixed on 2026-08-09 <a class="header-anchor" href="#Contamination-class-fixed-on-2026-08-09" aria-label="Permalink to &quot;Contamination class fixed on 2026-08-09 {#Contamination-class-fixed-on-2026-08-09}&quot;">​</a></h2><p><code>bench/plots.jl</code> merges its cache per arm, so an A/B run with a <code>PUREBLAS_FORCE_&lt;knob&gt;</code> variable exported persisted its <strong>deliberately non-default</strong> PureBLAS arm next to reference arms measured in a different run, and every render afterwards republished it as the gate number. The Zen5 <code>gemvT</code> n=256 cell was published at 0.751 on that basis; measured with all three arms in one run it is 1.09, and <code>PUREBLAS_FORCE_gemvt_deep=0</code> reproduces the bad record exactly. <code>gemvT</code> n=128 and <code>trmv</code> n=256 came from the same contamination. <code>save_cache</code> now refuses to write while any <code>PUREBLAS_FORCE_*</code> is set, so the class cannot recur silently. The corrections did not all favour PureBLAS — <code>ztrsv</code> on Zen4 went from 0.991 to 0.865 and <code>ger</code> on Zen5 from 0.996 to 0.896.</p>`,22)])])}const u=t(r,[["render",s]]);export{g as __pageData,u as default};
