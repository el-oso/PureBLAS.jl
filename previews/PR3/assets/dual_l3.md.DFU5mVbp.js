import{_ as t,o as a,c as o,ao as s}from"./chunks/framework.Do46ZhkW.js";const p=JSON.parse('{"title":"Dual numbers: BLAS-3 design (Phase 1 — for sparring, nothing implemented)","description":"","frontmatter":{},"headers":[],"relativePath":"dual_l3.md","filePath":"dual_l3.md","lastUpdated":null}'),r={name:"dual_l3.md"};function d(i,e,n,l,c,h){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="Dual-numbers:-BLAS-3-design-Phase-1-—-for-sparring,-nothing-implemented" tabindex="-1">Dual numbers: BLAS-3 design (Phase 1 — for sparring, nothing implemented) <a class="header-anchor" href="#Dual-numbers:-BLAS-3-design-Phase-1-—-for-sparring,-nothing-implemented" aria-label="Permalink to &quot;Dual numbers: BLAS-3 design (Phase 1 — for sparring, nothing implemented) {#Dual-numbers:-BLAS-3-design-Phase-1-—-for-sparring,-nothing-implemented}&quot;">​</a></h1><p>Status: <strong>implemented on branch <code>dual-l2l3</code> (2026-09-13); the design as sparred, then &quot;Implementation notes&quot; at the end.</strong> Companion to <code>dual.md</code> and <code>dual_l2.md</code>. Citations are into <code>a0f0a703</code>.</p><h2 id="0.-Triage:-what-the-generic-scalar-path-achieves-today" tabindex="-1">0. Triage: what the generic scalar path achieves today <a class="header-anchor" href="#0.-Triage:-what-the-generic-scalar-path-achieves-today" aria-label="Permalink to &quot;0. Triage: what the generic scalar path achieves today {#0.-Triage:-what-the-generic-scalar-path-achieves-today}&quot;">​</a></h2><p>Zen3 (core 6, boost off), plots.jl&#39;s CL3 regime (one call per sample on fresh operands), Chairmarks median of 6 rounds. Cell = complex-twin time ÷ dual time on identical bytes; the complex twin&#39;s absolute time is given so the route it took is visible. Probe: <code>bench/probes/dual_l23_triage.jl</code> (gitignored).</p><table tabindex="0"><thead><tr><th style="text-align:right;">op (dual / complex twin)</th><th style="text-align:right;">n=32</th><th style="text-align:right;">n=128</th><th style="text-align:right;">n=512</th><th style="text-align:right;">n=1024</th></tr></thead><tbody><tr><td style="text-align:right;">gemm / zgemm (α=β default)</td><td style="text-align:right;">0.17 (0.01 ms)</td><td style="text-align:right;">0.13 (0.27 ms)</td><td style="text-align:right;">0.12 (15.9 ms)</td><td style="text-align:right;">0.12 (124 ms)</td></tr><tr><td style="text-align:right;">gemm / zgemm (dual α,β vs complex α,β)</td><td style="text-align:right;">0.20</td><td style="text-align:right;">0.14</td><td style="text-align:right;">0.12</td><td style="text-align:right;">0.12</td></tr><tr><td style="text-align:right;">syrk / zsyrk</td><td style="text-align:right;">0.12</td><td style="text-align:right;">0.11</td><td style="text-align:right;">0.10 (9.0 ms)</td><td style="text-align:right;">0.10 (69 ms)</td></tr><tr><td style="text-align:right;">trmm / ztrmm (side L)</td><td style="text-align:right;">0.28</td><td style="text-align:right;">0.24</td><td style="text-align:right;">0.12 (9.5 ms)</td><td style="text-align:right;">0.11 (69 ms)</td></tr><tr><td style="text-align:right;">trsm / ztrsm (side L)</td><td style="text-align:right;">0.19</td><td style="text-align:right;">0.19</td><td style="text-align:right;">0.12 (9.4 ms)</td><td style="text-align:right;">0.11 (70 ms)</td></tr></tbody></table><p>The generic loop (<code>_gemm_generic!</code>, <code>gemm.jl:1385</code>) is 8–10× slower than the complex twin at every size ≥ 128 and 4–6× at n=32. zgemm at n=1024 runs 124 ms = 69 GFlops on Zen3 (the 3M route, <code>gemm.jl:2947-2951</code>, window <code>48 ≤ max(m,n,k) ≤ 2048</code>, <code>min ≥ 16</code>). <strong>Every L3 op is worth a kernel</strong>; gemm first.</p><h2 id="1.-The-shape-of-the-level:-ride-the-REAL-kernels-through-planar-splits" tabindex="-1">1. The shape of the level: ride the REAL kernels through planar splits <a class="header-anchor" href="#1.-The-shape-of-the-level:-ride-the-REAL-kernels-through-planar-splits" aria-label="Permalink to &quot;1. The shape of the level: ride the REAL kernels through planar splits {#1.-The-shape-of-the-level:-ride-the-REAL-kernels-through-planar-splits}&quot;">​</a></h2><p>For a dual product <code>(A_v + A_p ε)(B_v + B_p ε)</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>C_v = A_v·B_v                 C_p = A_v·B_p + A_p·B_v          (A_p·B_p is ε² and is never formed)</span></span></code></pre></div><p>Three real products of the value/partial <strong>planes</strong>. This is exactly the structure PureBLAS already has for complex &quot;3M&quot; (<code>_gemm_3m!</code> <code>gemm.jl:2649</code>): split each operand into planes with one O(n²) pass (<code>_split3!</code> <code>:2549</code>), run three real gemms through <code>_gemm_real_dims!</code> (<code>:2629</code>, the real unpacked/blocked dispatch on <code>PtrMatrix</code> views of a persistent pool, <code>_gemm_3m_scratch</code> <code>workspace.jl:164</code>), and recombine (<code>_combine3!</code> <code>:2595</code>). The differences for dual are all simplifications:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">complex Karatsuba-3M</th><th style="text-align:right;">dual</th></tr></thead><tbody><tr><td style="text-align:right;">split outputs per operand</td><td style="text-align:right;">3 planes (re, im, re+im)</td><td style="text-align:right;"><strong>2</strong> (value, partial)</td></tr><tr><td style="text-align:right;">real products</td><td style="text-align:right;">3 (<code>Ar·Br</code>, <code>Ai·Bi</code>, <code>(Ar+Ai)(Br+Bi)</code>)</td><td style="text-align:right;"><strong>3</strong> (<code>Av·Bv</code>, <code>Av·Bp</code>, <code>Ap·Bv</code>)</td></tr><tr><td style="text-align:right;">combine</td><td style="text-align:right;"><code>Cr = P1−P2</code>, <code>Ci = P3−P1−P2</code> — <strong>cancellation</strong>, the reason 3M is windowed</td><td style="text-align:right;"><code>Cv = P1</code>, <code>Cp = P2 + P3</code> — a plain sum, no cancellation</td></tr><tr><td style="text-align:right;">scratch</td><td style="text-align:right;">9 buffers</td><td style="text-align:right;">6 (or 5, accumulating <code>P3</code> into <code>P2</code> with β=1)</td></tr></tbody></table><p>So dual gemm does the same three real products as the complex twin&#39;s 3M route, with less O(n²) traffic and no numerical reason for a window. Op count: complex gemm is 4 real products (non-3M) or 3 (3M); dual is 3. Where the twin runs 3M the honest bar is <strong>equal time</strong>; outside the 3M window (n &lt; 48, n &gt; 2048, thin k) the dual has fewer products than complex and should be <strong>faster</strong> — that is not a claim, it is what §4 measures.</p><p><strong>No dual microkernel, no pair pack buffers, no register-blocking question.</strong> The alternative — tagging the complex microkernel — was examined and rejected on the ladder: <code>_cmplx_kernel_body</code> (<code>gemm.jl:1640</code>) already packs <em>planar</em> (<code>ApR</code>/<code>ApI</code>, <code>_pack_A_cmplx!</code> <code>:1573</code>) and holds two accumulators per cell (<code>cr</code>, <code>ci</code>); a dual instantiation would keep the same tile and accumulator count and drop one of the four FMAs (<code>:1660</code>, the <code>−ai·bi</code> term). That is a real 25% flop cut on a kernel the real path already beats (the complex non-3M kernel exists because Karatsuba costs complex precision; dual has no such constraint). It would also put dual on the complex blocked driver and off Strassen. Rejected unless §5&#39;s measurement says the planar route loses somewhere that matters.</p><h2 id="2.-Per-op-decisions" tabindex="-1">2. Per-op decisions <a class="header-anchor" href="#2.-Per-op-decisions" aria-label="Permalink to &quot;2. Per-op decisions {#2.-Per-op-decisions}&quot;">​</a></h2><h3 id="2.1-gemm-—-rides-the-real-gemm;-three-products,-one-split,-one-combine" tabindex="-1">2.1 gemm — rides the real gemm; three products, one split, one combine <a class="header-anchor" href="#2.1-gemm-—-rides-the-real-gemm;-three-products,-one-split,-one-combine" aria-label="Permalink to &quot;2.1 gemm — rides the real gemm; three products, one split, one combine {#2.1-gemm-—-rides-the-real-gemm;-three-products,-one-split,-one-combine}&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>split:   A → (A_v, A_p)    B → (B_v, B_p)                 raw pair reads, planar writes; `_split2!`, mirror of `_split3!` minus the sum plane</span></span>\n<span class="line"><span>P1 = A_v·B_v                                               _gemm_real_dims!(tA, tB, …, 1, 0, A_v, B_v, P1)</span></span>\n<span class="line"><span>P2 = A_v·B_p ;  P2 += A_p·B_v                              second call with β = 1 (accumulate in gemm&#39;s own epilogue)</span></span>\n<span class="line"><span>combine: C_v = α_v P1 + β_v C_v</span></span>\n<span class="line"><span>         C_p = α_v P2 + α_p P1 + β_v C_p + β_p C_v(old)    elementwise pass over interleaved C, β=0 ⇒ overwrite; mirror of `_combine3!`</span></span></code></pre></div><p>Routing: a new branch in <code>_gemm_core!</code> (<code>gemm.jl:2841</code>) between the complex branch (<code>:2924</code>) and the generic fallback (<code>:2971</code>): <code>_pairT(T) &amp;&amp; _strided1(C) &amp;&amp; _strided1(A) &amp;&amp; _strided1(B)</code> → <code>_gemm_dual3!</code>. <code>tA/tB</code> pass through to the real sub-gemms exactly as 3M does; <code>cA/cB</code> are dropped (conjugation is the identity on <code>Dual &lt;: Real</code>, <code>dual_l2.md</code> §4.2). The existing α==0 quick-return is deliberately <strong>not</strong> widened to dual (<code>:2848-2852</code> explains why: <code>Dual(0, p)</code> has <code>iszero == true</code> and a live derivative) — the combine multiplies by both parts, so <code>α = Dual(0, p)</code> is correct by construction. Scratch: the same <code>_gemm_3m_scratch</code> pool, indices 1,2,4,5,7,8. Nothing complex is touched; <strong>complex byte-identity is trivially satisfied</strong>.</p><p>Window: none proposed for the upper edge (the complex <code>_CGEMM_3M_MAX = 2048</code>, <code>:1488</code>, exists because above it &quot;the extra passes cost more than the flop cut&quot; <em>relative to the 4-product kernel</em>; dual has no 4-product kernel to fall back to and the generic loop is 8× off). Lower edge: the split/combine is O(n²) against O(n³), 6% of the FMA count at n=32 and 1.5% at n=128; the crossover against the generic loop is measured (§4) and expected at or below the complex <code>_CGEMM_TINY = 6</code> (<code>:1443</code>). Thin-k: see §5.</p><h3 id="2.2-syrk-/-herk-—-real-syrk-real-syr2k" tabindex="-1">2.2 syrk / herk — real syrk + real syr2k <a class="header-anchor" href="#2.2-syrk-/-herk-—-real-syrk-real-syr2k" aria-label="Permalink to &quot;2.2 syrk / herk — real syrk + real syr2k {#2.2-syrk-/-herk-—-real-syrk-real-syr2k}&quot;">​</a></h3><p><code>C = α·A·Aᵀ + β·C</code> with <code>A = A_v + A_p ε</code>: value <code>A_v A_vᵀ</code>, partial <code>A_v A_pᵀ + A_p A_vᵀ</code> — the second is <strong>exactly <code>syr2k(A_v, A_p)</code></strong>. So: <code>P1 = syrk(A_v)</code>, <code>S2 = syr2k(A_v, A_p)</code> (both real, both triangular, <code>syrk!</code> <code>level3.jl:6283</code>, <code>syr2k!</code> <code>:7042</code>), then the triangular combine <code>C_v = α_v P1 + β_v C_v</code>, <code>C_p = α_v S2 + α_p P1 + β_v C_p + β_p C_v(old)</code> over the stored triangle (mirror of <code>_combine3_tri!</code> <code>:5512</code>). <code>trans=&#39;T&#39;</code> passes through. <code>herk</code> on dual collapses to syrk (α, β real by its contract; conjugation identity) — same reasoning as hemv → symv. One half-product more than a real syrk and the same three half-products as the complex 3M syrk (<code>_ctrk_3m_ok</code> <code>:5505</code>).</p><h3 id="2.3-trmm-—-three-real-trmms" tabindex="-1">2.3 trmm — three real trmms <a class="header-anchor" href="#2.3-trmm-—-three-real-trmms" aria-label="Permalink to &quot;2.3 trmm — three real trmms {#2.3-trmm-—-three-real-trmms}&quot;">​</a></h3><p><code>B := α·op(A)·B</code>, A triangular, <code>B = B_v + B_p ε</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>B_p ← trmm(A_v, B_p) + trmm(A_p°, B_v(old))      A_p° = A_p with its diagonal zeroed when diag=&#39;U&#39;; non-unit uses A_p&#39;s diagonal as is</span></span>\n<span class="line"><span>B_v ← trmm(A_v, B_v)                              computed AFTER B_p, which needs the old B_v</span></span></code></pre></div><p>Three real trmms on planar scratch (the second is <code>diag=&#39;N&#39;</code> on <code>A_p°</code>, since the partial of a unit diagonal is 0 — the only subtlety on this op, and it is handled by zeroing a diagonal in a private plane). <code>side</code>, <code>uplo</code>, <code>transA</code> pass through to the real <code>_trmm!</code> (<code>level3.jl:1020</code>). α applied in the combine, as gemm. The complex twin&#39;s own tiny-k arms (<code>_trmm_cmplx_small_L!</code> <code>:395</code> etc.) are irrelevant here.</p><h3 id="2.4-trsm-—-two-real-trsms-and-one-real-trmm;-no-dual-division" tabindex="-1">2.4 trsm — two real trsms and one real trmm; <strong>no dual division</strong> <a class="header-anchor" href="#2.4-trsm-—-two-real-trsms-and-one-real-trmm;-no-dual-division" aria-label="Permalink to &quot;2.4 trsm — two real trsms and one real trmm; **no dual division** {#2.4-trsm-—-two-real-trsms-and-one-real-trmm;-no-dual-division}&quot;">​</a></h3><p>Solve <code>op(A)·X = B</code> (side L; side R is the mirror). <code>(A_v + A_p ε)(X_v + X_p ε) = B_v + B_p ε</code> gives</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>A_v X_v = B_v                  →  X_v = trsm(A_v, B_v)</span></span>\n<span class="line"><span>A_v X_p = B_p − A_p X_v        →  R   = B_p − trmm(A_p°, X_v);   X_p = trsm(A_v, R)</span></span></code></pre></div><p>This is the &quot;own derivation&quot; the division rule demands, and it lands entirely on real kernels: the division by the dual diagonal is never performed — it is replaced by a second real solve with the same <code>A_v</code>. Cost: 2 real trsms + 1 real trmm (+ α combine), against the complex twin&#39;s 4 real-equivalent products in its recursive/gemm-blocked split (<code>level3.jl:4048</code>). <code>diag=&#39;U&#39;</code> is not a special case here either. Numerically the partial inherits the real trsm&#39;s conditioning twice (the second solve&#39;s right-hand side is a residual of the first) — the same as ForwardDiff&#39;s own <code>\\</code> on a dual matrix, which does exactly this.</p><h3 id="2.5-symm-/-hemm-/-syr2k-/-her2k-—-same-pattern,-listed-for-completeness,-after-the-four-above" tabindex="-1">2.5 symm / hemm / syr2k / her2k — same pattern, listed for completeness, after the four above <a class="header-anchor" href="#2.5-symm-/-hemm-/-syr2k-/-her2k-—-same-pattern,-listed-for-completeness,-after-the-four-above" aria-label="Permalink to &quot;2.5 symm / hemm / syr2k / her2k — same pattern, listed for completeness, after the four above {#2.5-symm-/-hemm-/-syr2k-/-her2k-—-same-pattern,-listed-for-completeness,-after-the-four-above}&quot;">​</a></h3><ul><li><p>symm: <code>_split3_sym!</code> (<code>gemm.jl:2569</code>, <code>herm=false</code>) already reflects a stored triangle into full planes; dual symm is three real gemms on (<code>A_v</code>, <code>A_p</code>) × (<code>B_v</code>, <code>B_p</code>), the mirror of <code>_hemm_3m_L!</code> (<code>:2681</code>). hemm collapses to symm.</p></li><li><p>syr2k: <code>C = α(ABᵀ + BAᵀ)</code>: value <code>syr2k(A_v, B_v)</code>, partial <code>syr2k(A_v, B_p) + syr2k(A_p, B_v)</code>. her2k collapses.</p></li></ul><h3 id="2.6-What-every-L3-op-shares" tabindex="-1">2.6 What every L3 op shares <a class="header-anchor" href="#2.6-What-every-L3-op-shares" aria-label="Permalink to &quot;2.6 What every L3 op shares {#2.6-What-every-L3-op-shares}&quot;">​</a></h3><p>One <code>_split2!</code> (raw pair → two planes, r×c, column stride honoured; <code>_split3!</code> minus the sum stream, ~10 lines), one interleaved <code>_combine_dual!</code> per shape class (full, triangular; dual α, β applied through <code>_parts</code>), scratch from the existing per-real-type pool. Zero new SIMD code. Zero complex code touched.</p><h2 id="3.-The-design-questions,-explicitly" tabindex="-1">3. The design questions, explicitly <a class="header-anchor" href="#3.-The-design-questions,-explicitly" aria-label="Permalink to &quot;3. The design questions, explicitly {#3.-The-design-questions,-explicitly}&quot;">​</a></h2><ul><li><p><strong>Do the pack buffers hold pairs? What happens to register blocking?</strong> They do not: the split happens <em>before</em> the real gemm, whose pack buffers, <code>_MR×_NR</code> tile and accumulators are untouched and real. There is no pair in any register on the L3 path. (For the record, the rejected tagged-complex alternative would also be planar — <code>_pack_A_cmplx!</code> splits re/im — and would keep complex&#39;s 2 accumulators per cell, so it would not spill <em>more</em> than complex; it would just be on the slower driver.)</p></li><li><p><strong>trsm division:</strong> §2.4 — eliminated, not vectorised.</p></li><li><p><strong>hemm / herk / her2k on dual:</strong> collapse to the symmetric op (identity conjugation; <code>dual_l2.md</code> §4.2).</p></li></ul><h2 id="4.-Measurement-plan-Zen3,-taskset-c-6,-Chairmarks-median" tabindex="-1">4. Measurement plan (Zen3, <code>taskset -c 6</code>, Chairmarks median) <a class="header-anchor" href="#4.-Measurement-plan-Zen3,-taskset-c-6,-Chairmarks-median" aria-label="Permalink to &quot;4. Measurement plan (Zen3, `taskset -c 6`, Chairmarks median) {#4.-Measurement-plan-Zen3,-taskset-c-6,-Chairmarks-median}&quot;">​</a></h2><ol><li><p>Correctness before speed: dual gemm/syrk/trmm/trsm vs the generic loop on awkward <code>(m, n, k)</code>, all <code>trans</code>/<code>side</code>/<code>uplo</code>/<code>diag</code>, dual α with zero value, β = 0/1/dual; <code>ForwardDiff.jacobian</code> of <code>A*B</code>, <code>A\\B</code> against the planar route; the ε²-leak test (<code>A_p</code>, <code>B_p ~ 1e155</code>: <code>A_p·B_p</code> never forms, so the value must be exactly the real product and the partial finite).</p></li><li><p>The triage table re-run: gemm at n ∈ {8, 16, 32, 48, 128, 512, 1024, 2048, 4096}; the bar is <strong>equal time to zgemm inside its 3M window (48–2048)</strong> and <em>less</em> outside it. Absolute: ≈ 3× dgemm&#39;s time</p><ul><li>O(n²).</li></ul></li><li><p>Thin-k sweep: <code>m = n = 1024</code>, <code>k ∈ {1, 2, 4, 8, 16, 32}</code> — where the O(n²) split stops being amortised (§5).</p></li><li><p>Scratch footprint at n = 4096 (§5), and whether the sub-gemms should go through <code>_gemm_core!</code> to pick up Strassen at large n (<code>_gemm_real_dims!</code> deliberately does not: <code>gemm.jl:2620-2628</code>).</p></li></ol><h2 id="5.-Adversarial:-the-case-against-this-design" tabindex="-1">5. Adversarial: the case against this design <a class="header-anchor" href="#5.-Adversarial:-the-case-against-this-design" aria-label="Permalink to &quot;5. Adversarial: the case against this design {#5.-Adversarial:-the-case-against-this-design}&quot;">​</a></h2><ul><li><p><strong>Thin k is where the planar route stops fitting.</strong> The split moves <code>2(mk + kn)</code> elements and the combine <code>2mn</code>; the products cost <code>3mnk</code> FMAs. At <code>k = 1</code> (a rank-1 update through gemm, common in AD code that builds Jacobians column by column) the O(n²) passes are of the same order as the work, and <code>P1</code>/<code>P2</code> are each a full <code>m×n</code> write + read. The complex 3M keeps <code>min(m,n,k) ≥ 16</code> for the same reason (<code>_CGEMM_3M_KMIN</code>, <code>:1491</code>). I propose <strong>no k floor for dual</strong> — below it the alternative is the generic loop at 0.12, and even at <code>k = 1</code> the planar route is 3 vectorised real gers plus two streaming passes — but the sweep in §4.3 decides, and if a floor is needed the honest fix for thin k is the tagged L2 ger applied k times, not a dual microkernel.</p></li><li><p><strong>Scratch at n = 4096 is 6 planes × 128 MB (Float64).</strong> The complex 3M stops at 2048 (9 × 32 MB); dual has no fallback, so an unpaneled implementation would hold ~800 MB for a 4096² dual gemm. Fix if the user cares: panel over <code>nc</code> columns of B/C (the three sub-gemms are column-separable), bounding scratch to A&#39;s two planes plus panels. It is a loop around the same three calls, not a new kernel — but it is also a knob (<code>nc</code>), and the PDM ladder says it must derive from <code>_L3_BYTES</code>. I would ship v1 unpaneled with the pool and add the panel loop only if the footprint is objected to.</p></li><li><p><strong>The sub-gemms do not ride Strassen.</strong> <code>_gemm_real_dims!</code> goes to unpacked/blocked only. At n ≥ the Strassen threshold real gemm beats OB by 1.2–1.3× through the 7-multiply recursion; the dual route would leave that on the table unless the sub-gemms go through <code>_gemm_core!</code> on the <code>PtrMatrix</code> planes. Cheap to try; measured in §4.4; it changes nothing in complex.</p></li><li><p><strong>trmm/trsm on the planar route are 3× a real trmm/trsm, which is the same ratio as gemm — but the triangular ops have tiny-k arms in complex</strong> (<code>_trmm_cmplx_small_L!</code> <code>:395</code>, the gemmtrsm leaf <code>:4048</code>) that the dual route does not reproduce at n ≤ 48. Expect the dual ratio to drop below 1 vs the complex twin at n = 32 on trmm/trsm; whether that matters is a question for the user (AD code calls <code>A\\B</code> at n = 32 far less than it calls gemm).</p></li><li><p><strong>The op forced into a shape that does not fit: none on L3</strong> — planar is the natural shape for ε² = 0. The forced <em>decision</em> is dropping <code>cA/cB</code> (conjugation) silently; a user who calls <code>gemm!(…, transA=&#39;C&#39;)</code> on duals gets the transpose, which is the only consistent answer, but nothing tells them.</p></li><li><p><strong>What measurement would detect a wrong design?</strong> Ratio vs zgemm inside the 3M window ≠ 1.0 ± noise means the O(n²) passes are not free (memory-bound combine on a cold C — the same <code>_combine3!</code> cost complex pays); the thin-k sweep; the ε² test (a <code>reinterpret</code> shortcut anywhere would fail it instantly).</p></li><li><p><strong>If only half ships: gemm.</strong> It is the op that matters, it is the one AD frameworks actually hit (<code>A*B</code> on <code>Matrix{Dual}</code> falls to a scalar loop today, everywhere in the ecosystem), and syrk/trmm/trsm are 30-line drivers over the same split/combine once it exists. If only a quarter ships: gemm with a <code>k ≥ 16</code> floor and the generic loop below, i.e. the complex 3M&#39;s own window without the upper edge.</p></li></ul><h2 id="Implementation-notes-2026-09-13,-after-the-sparring-round" tabindex="-1">Implementation notes (2026-09-13, after the sparring round) <a class="header-anchor" href="#Implementation-notes-2026-09-13,-after-the-sparring-round" aria-label="Permalink to &quot;Implementation notes (2026-09-13, after the sparring round) {#Implementation-notes-2026-09-13,-after-the-sparring-round}&quot;">​</a></h2><h3 id="Sparring-point-1-—-the-cost-model,-confirmed-by-measurement" tabindex="-1">Sparring point 1 — the cost model, confirmed by measurement <a class="header-anchor" href="#Sparring-point-1-—-the-cost-model,-confirmed-by-measurement" aria-label="Permalink to &quot;Sparring point 1 — the cost model, confirmed by measurement {#Sparring-point-1-—-the-cost-model,-confirmed-by-measurement}&quot;">​</a></h3><p>Dual gemm is three real gemms with no cancellation and no window; complex 3M is three real gemms plus a sum plane per operand and a cancelling combine. The sub-gemms go through <code>_gemm_core!</code> (not <code>_gemm_real_dims!</code>), so they take Strassen where real gemm does. Measured on Zen3 (median of 4 rounds, plots.jl&#39;s CL3 regime), zgemm ÷ dual on identical bytes, with dgemm for scale:</p><table tabindex="0"><thead><tr><th style="text-align:right;">n</th><th style="text-align:right;">dual ms</th><th style="text-align:right;">zgemm ms</th><th style="text-align:right;">dgemm ms</th><th style="text-align:right;">zgemm/dual</th><th style="text-align:right;">dual/(3·dgemm)</th></tr></thead><tbody><tr><td style="text-align:right;">8</td><td style="text-align:right;">0.0004</td><td style="text-align:right;">0.0001</td><td style="text-align:right;">0.0001</td><td style="text-align:right;">0.35</td><td style="text-align:right;">1.39</td></tr><tr><td style="text-align:right;">16</td><td style="text-align:right;">0.001</td><td style="text-align:right;">0.001</td><td style="text-align:right;">0.000</td><td style="text-align:right;">0.70</td><td style="text-align:right;">1.31</td></tr><tr><td style="text-align:right;">32</td><td style="text-align:right;">0.006</td><td style="text-align:right;">0.006</td><td style="text-align:right;">0.002</td><td style="text-align:right;">0.96</td><td style="text-align:right;">1.15</td></tr><tr><td style="text-align:right;">48</td><td style="text-align:right;">0.015</td><td style="text-align:right;">0.016</td><td style="text-align:right;">0.004</td><td style="text-align:right;">1.05</td><td style="text-align:right;">1.19</td></tr><tr><td style="text-align:right;">128</td><td style="text-align:right;">0.265</td><td style="text-align:right;">0.268</td><td style="text-align:right;">0.080</td><td style="text-align:right;">1.01</td><td style="text-align:right;">1.11</td></tr><tr><td style="text-align:right;">512</td><td style="text-align:right;">16.6</td><td style="text-align:right;">16.0</td><td style="text-align:right;">5.10</td><td style="text-align:right;">0.96</td><td style="text-align:right;">1.09</td></tr><tr><td style="text-align:right;">1024</td><td style="text-align:right;">117.9</td><td style="text-align:right;">124.3</td><td style="text-align:right;">37.3</td><td style="text-align:right;">1.05</td><td style="text-align:right;">1.06</td></tr><tr><td style="text-align:right;">2048</td><td style="text-align:right;">834</td><td style="text-align:right;">977</td><td style="text-align:right;">272</td><td style="text-align:right;"><strong>1.17</strong></td><td style="text-align:right;">1.02</td></tr></tbody></table><p>So the bar — dual ≥ the complex twin — holds from n = 48 and is within noise at 32 and 512 (0.96); at 2048, where the twin leaves its 3M window and the dual sub-gemms ride Strassen, dual is 17 % faster. The <code>dual/(3·dgemm)</code> column is the O(n²) overhead share: 2–15 %, shrinking with n. Below 32 the three entry overheads and the split dominate (0.35 at n = 8) — still 2× the generic loop&#39;s 0.17 — so the route is taken above <code>_fh_cgemm_tiny()</code> (= 6), the complex floor, reused as is. The triage&#39;s 0.12 became 1.0–1.17.</p><p>Thin k, m = n = 1024 (the adversarial §5 case): dual beats the generic loop at every k (1.5× at k = 1, 7.7× at k = 32) and beats zgemm from k = 16; below that the O(n²) planes are not amortised (0.44 at k = 1). No k floor was added: below 16 the alternative is the generic loop, which is slower still. The honest fix for thin k, if it ever matters, is k tagged L2 gers — not a floor.</p><h3 id="Sparring-point-2-—-scratch,-with-numbers" tabindex="-1">Sparring point 2 — scratch, with numbers <a class="header-anchor" href="#Sparring-point-2-—-scratch,-with-numbers" aria-label="Permalink to &quot;Sparring point 2 — scratch, with numbers {#Sparring-point-2-—-scratch,-with-numbers}&quot;">​</a></h3><p>Peak scratch is 6 planes: <code>A_v, A_p</code> (m×k), <code>B_v, B_p</code> (k×n), <code>P1, P2</code> (m×n) — for square n exactly <code>6n²</code> reals = <strong>the byte size of the three dual operands</strong> (each 2n² reals), i.e. a 2× footprint, from the same grow-only pool as complex 3M (slots 1,2 / 4,5 / 7,8 of <code>_gemm_3m_scratch</code>). At n = 4096 Float64 that is 805 MB, on a 30 GB box; the real Strassen path at the same n already holds ~440 MB of Winograd level scratch (10 buffers per level, depth 3), so the dual route is the same order as what the fastest real path does, and nothing pages. Correction to the sparring premise: <code>_split3!</code> does NOT panel — complex 3M splits the whole operand and bounds its scratch only by the <code>_CGEMM_3M_MAX = 2048</code> window (9 × 32 MB there). The two costs that remain real: the pool never shrinks (a process that once ran a 4096² dual gemm keeps 805 MB resident — complex 3M has the same property at 288 MB), and paneling would cap it at the price of losing Strassen on the panels. Unpaneled is what shipped; the panel loop is the upgrade path if the footprint is objected to.</p><h3 id="The-compositions-Zen3,-complex-twin-dual,-side-L,-median-of-6" tabindex="-1">The compositions (Zen3, complex twin ÷ dual, side L, median of 6) <a class="header-anchor" href="#The-compositions-Zen3,-complex-twin-dual,-side-L,-median-of-6" aria-label="Permalink to &quot;The compositions (Zen3, complex twin ÷ dual, side L, median of 6) {#The-compositions-Zen3,-complex-twin-dual,-side-L,-median-of-6}&quot;">​</a></h3><table tabindex="0"><thead><tr><th style="text-align:right;">op</th><th style="text-align:right;">n=32</th><th style="text-align:right;">n=128</th><th style="text-align:right;">n=512</th><th style="text-align:right;">n=1024</th></tr></thead><tbody><tr><td style="text-align:right;">syrk (real syrk + real syr2k)</td><td style="text-align:right;">0.88</td><td style="text-align:right;">1.17</td><td style="text-align:right;">1.14</td><td style="text-align:right;">1.17</td></tr><tr><td style="text-align:right;">trmm (three real trmms)</td><td style="text-align:right;">0.81</td><td style="text-align:right;">0.98</td><td style="text-align:right;">1.04</td><td style="text-align:right;">1.00</td></tr><tr><td style="text-align:right;">trsm (two real trsms + one trmm; no dual division)</td><td style="text-align:right;">0.82</td><td style="text-align:right;">1.07</td><td style="text-align:right;">1.06</td><td style="text-align:right;">1.04</td></tr></tbody></table><p>At n = 32 the complex twins&#39; tiny-k arms (<code>_trmm_cmplx_small_*</code>, the gemmtrsm leaf) have no dual counterpart and the three entries cost more than the work — as §5 predicted. From 128 the compositions sit at or above the twin. <code>herk</code> routes to <code>syrk</code> on a Dual (identity conjugation; <code>trans=&#39;C&#39;</code> → <code>&#39;T&#39;</code>), the same decision as <code>hemv → symv</code> and <code>dotc == dotu</code>. Correctness: <code>bench/probes/dual_l3_check.jl</code> — every uplo/trans/side/diag, n ∈ {1,3,8,17,64,129,257} × k ∈ {1,5,32,130}, both real types, dual α and a zero-value β, plus a <code>ForwardDiff.derivative</code> through <code>trsm!</code> against <code>−L⁻¹ dL L⁻¹ B</code> — 0 failures; the same in <code>test/dual_tests.jl</code> (&quot;Dual L3: syrk/herk/trmm/trsm compositions …&quot;).</p><h3 id="What-the-L3-route-never-touches" tabindex="-1">What the L3 route never touches <a class="header-anchor" href="#What-the-L3-route-never-touches" aria-label="Permalink to &quot;What the L3 route never touches {#What-the-L3-route-never-touches}&quot;">​</a></h3><p>No complex function changed on L3. The additions are <code>_split2!</code>, <code>_combine_dual!</code>, <code>_combine_dual_tri!</code>, <code>_zero_diag!</code>, <code>_copy_plane!</code>, the four drivers (<code>_gemm_dual3!</code>, <code>_syrk_dual!</code>, <code>_trmm_dual!</code>, <code>_trsm_dual!</code>) and one branch in each of <code>gemm!</code>, <code>syrk!</code>, <code>herk!</code>, <code>trmm!</code>, <code>trsm!</code> behind <code>_pairT</code>, which is <code>false</code> for every type in the main env — so the branches fold away without ForwardDiff and the <code>--trim</code> build is unaffected.</p>',51)])])}const g=t(r,[["render",d]]);export{p as __pageData,g as default};
