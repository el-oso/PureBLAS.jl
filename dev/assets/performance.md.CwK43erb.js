import{_ as t,o,c as s,ao as r}from"./chunks/framework.mOy25YJp.js";const n="/PureBLAS.jl/dev/assets/perf_l1_avx512.Dizagw5S.svg",a="/PureBLAS.jl/dev/assets/perf_l1_avx2.DMtzfO_b.svg",d="/PureBLAS.jl/dev/assets/perf_l1_zen5.BvbMkiVT.svg",l="/PureBLAS.jl/dev/assets/perf_l2_avx512.DgyCkhN2.svg",c="/PureBLAS.jl/dev/assets/perf_l2_avx2.CwZhS11p.svg",i="/PureBLAS.jl/dev/assets/perf_l2_zen5.D7pAfTHJ.svg",g="/PureBLAS.jl/dev/assets/perf_l3_avx512.B0w5Zefd.svg",m="/PureBLAS.jl/dev/assets/perf_l3_avx2.B8XZtL3N.svg",p="/PureBLAS.jl/dev/assets/perf_l3_zen5.BSfV4iMA.svg",h="/PureBLAS.jl/dev/assets/perf_lapack_avx512.RaWpDiM4.svg",u="/PureBLAS.jl/dev/assets/perf_lapack_avx2.LXW5Yf01.svg",f="/PureBLAS.jl/dev/assets/perf_lapack_zen5.B_-s-KLT.svg",y="/PureBLAS.jl/dev/assets/perf_cl1_avx512.DOVQWxvw.svg",v="/PureBLAS.jl/dev/assets/perf_cl2_avx512.C6sTa2dz.svg",b="/PureBLAS.jl/dev/assets/perf_cl1_avx2.BUrvh634.svg",x="/PureBLAS.jl/dev/assets/perf_cl2_avx2.JHaQ4cNI.svg",A="/PureBLAS.jl/dev/assets/perf_cl1_zen5.CAOYwn4F.svg",k="/PureBLAS.jl/dev/assets/perf_cl2_zen5.qXOPAnw9.svg",w="/PureBLAS.jl/dev/assets/perf_cl3_avx512.CTnS39vp.svg",L="/PureBLAS.jl/dev/assets/perf_cl3_avx2.xtUCFSnb.svg",_="/PureBLAS.jl/dev/assets/perf_cl3_zen5.C_vz183r.svg",Z=JSON.parse('{"title":"Performance","description":"","frontmatter":{},"headers":[],"relativePath":"performance.md","filePath":"performance.md","lastUpdated":null}'),S={name:"performance.md"};function B(z,e,P,V,X,M){return o(),s("div",null,[...e[0]||(e[0]=[r('<h1 id="Performance" tabindex="-1">Performance <a class="header-anchor" href="#Performance" aria-label="Permalink to &quot;Performance {#Performance}&quot;">​</a></h1><p>PureBLAS targets a hard, non-negotiable gate: <strong>≥ 0.96× OpenBLAS</strong>, measured per machine, single threaded. The plots below are the PureBLAS/OpenBLAS speed ratio (so <strong>higher is better; 1.0× is parity; the dashed line is the 0.96× gate</strong>). Each <strong>violin</strong> is the distribution of the ratio across the size sweep (and rounds); the horizontal line is the median and the dark dot is the <em>worst</em> size — a violin is green only when <strong>every</strong> size clears the gate.</p><p>Methodology (see <code>bench/</code>): single-thread (<code>BLAS.set_num_threads(1)</code>), Float64 (plus the full <strong>ComplexF64</strong> surface — see the Complex section), native PureBLAS API vs <code>LinearAlgebra.BLAS</code>, <strong>interleaved</strong> timing (each round times OpenBLAS then PureBLAS back-to-back so frequency drift cancels), <strong>median</strong> of many rounds, core pinned with <code>taskset</code>, and — importantly — <strong>CPU boost disabled</strong> (a floating boost clock silently biases small-n ratios; see below). Reproduce: <code>taskset -c 2 julia --project=bench bench/plots.jl</code>. Each section shows the dev fleet — double-pumped <strong>AVX-512</strong> (Zen4, the primary tuning target), native <strong>AVX2</strong> (Zen3), and native 512-bit <strong>AVX-512</strong> (Zen5, <code>neuromancer</code>) — so per-µarch differences are visible side by side.</p><h2 id="Per-ISA-gate-dev-fleet" tabindex="-1">Per-ISA gate (dev fleet) <a class="header-anchor" href="#Per-ISA-gate-dev-fleet" aria-label="Permalink to &quot;Per-ISA gate (dev fleet) {#Per-ISA-gate-dev-fleet}&quot;">​</a></h2><p>The 0.96× gate is <strong>per machine</strong>. The fleet spans double-pumped <strong>AVX-512</strong> (Zen4, <code>wintermute</code>, the tuning target), native 512-bit <strong>AVX-512</strong> (Zen5, <code>neuromancer</code>), and native <strong>AVX2</strong> (Zen3, <code>galen</code>). <strong>Haswell</strong>* is a deployment target that shares the <em>identical</em> AVX2 code path (W=4, 16 ymm) — its column mirrors the AVX2 (Zen3) proxy. Below is the full-stack <code>plots.jl bench</code> at pinned frequency (boost off), worst-size ratio (the gate metric) with ✓ ≥ 0.96 / ◐ borderline / ✗ &lt; 0.96; geomeans in text.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Op</th><th style="text-align:center;">AVX-512 dp (Zen4)</th><th style="text-align:center;">AVX-512 native (Zen5)</th><th style="text-align:center;">AVX2 (Zen3)</th><th style="text-align:center;">Haswell*</th></tr></thead><tbody><tr><td style="text-align:right;">L1 axpy · dot · asum · scal</td><td style="text-align:center;">✓ 0.99–1.06</td><td style="text-align:center;">✓ 0.98–1.07</td><td style="text-align:center;">✓ 0.99–1.33</td><td style="text-align:center;">✓ 0.99–1.33</td></tr><tr><td style="text-align:right;">L1 nrm2 (scaled-accum beats OpenBLAS)</td><td style="text-align:center;">✓ 3.6×</td><td style="text-align:center;">✓ 6.2×</td><td style="text-align:center;">✓ 5.5×</td><td style="text-align:center;">✓ 5.5×</td></tr><tr><td style="text-align:right;">L1 iamax</td><td style="text-align:center;">✓ 1.15</td><td style="text-align:center;">✓ 1.32</td><td style="text-align:center;">◐ 0.90 (noise)</td><td style="text-align:center;">◐ 0.90 (noise)</td></tr><tr><td style="text-align:right;">L2 gemvT · symv · trmv · trsv · spmv · gbmv · sbmv</td><td style="text-align:center;">✓ 1.0–1.6</td><td style="text-align:center;">✓ 0.99–1.23</td><td style="text-align:center;">✓ 0.97–1.64</td><td style="text-align:center;">✓ 0.97–1.64</td></tr><tr><td style="text-align:right;">L2 ger</td><td style="text-align:center;">✓ 1.01</td><td style="text-align:center;">✗ 0.69 (LPDDR5x DRAM-bandwidth ceiling)</td><td style="text-align:center;">✓ 0.99</td><td style="text-align:center;">✓ 0.99</td></tr><tr><td style="text-align:right;">L2 gemvN</td><td style="text-align:center;">◐ 0.96</td><td style="text-align:center;">✗ 0.85</td><td style="text-align:center;">✓ 0.97 (8-acc + panel route)</td><td style="text-align:center;">✓ 0.97</td></tr><tr><td style="text-align:right;">L3 gemm</td><td style="text-align:center;">✗ 0.83 (n=8 dispatch)</td><td style="text-align:center;">◐ 0.93 (n=8; geomean 1.14)</td><td style="text-align:center;">✓ 1.03 (clip)</td><td style="text-align:center;">✓ 1.03</td></tr><tr><td style="text-align:right;"><strong>L3 syrk · syr2k · symm</strong> (decomposed to the gate)</td><td style="text-align:center;">✓ 0.97–1.02</td><td style="text-align:center;">✓ 0.96–1.11</td><td style="text-align:center;">✓ 0.96–1.02</td><td style="text-align:center;">✓ 0.96–1.02</td></tr><tr><td style="text-align:right;"><strong>L2 complex</strong> zhemv · zgeru · zdotc · zscal · zaxpy · dzasum</td><td style="text-align:center;">✓ 0.99–2×</td><td style="text-align:center;">—</td><td style="text-align:center;">✓ 0.96–2× (n=1000 zdotc/zscal ◐)</td><td style="text-align:center;">✓</td></tr><tr><td style="text-align:right;"><strong>L2 complex zgemvC</strong> (column-blocked)</td><td style="text-align:center;">✓ 0.94–1.34</td><td style="text-align:center;">—</td><td style="text-align:center;">◐ 0.80–1.09 (n=64 + mid-n)</td><td style="text-align:center;">◐</td></tr><tr><td style="text-align:right;"><strong>L2 complex zgemvN</strong> (ILP-tuned; AVX2 kernel-formulation gap)</td><td style="text-align:center;">✓ 0.95–1.29</td><td style="text-align:center;">—</td><td style="text-align:center;">✗ 0.73–0.83 (n≤1024)</td><td style="text-align:center;">✗</td></tr><tr><td style="text-align:right;"><strong>L2 complex ztrmv · ztrsv</strong> (blocked)</td><td style="text-align:center;">✓ ≤2048 (ztrsv n≤128 ◐ 0.88)</td><td style="text-align:center;">—</td><td style="text-align:center;">◐ large-n gates, mid-n 0.85–0.96</td><td style="text-align:center;">◐</td></tr><tr><td style="text-align:right;"><strong>L3 zgemm (complex)</strong></td><td style="text-align:center;"><strong>✓ 1.12</strong></td><td style="text-align:center;"><strong>✓ 1.16</strong></td><td style="text-align:center;">◐ 0.94 (n=32 cold; ~1.02 warm)</td><td style="text-align:center;">◐ 0.94</td></tr><tr><td style="text-align:right;">L3 trsm (unpacked leaf + clip + blocked trtri)</td><td style="text-align:center;">✓ 1.02</td><td style="text-align:center;">✓ 1.15</td><td style="text-align:center;"><strong>✓ 0.98</strong> (geomean 1.03)</td><td style="text-align:center;">✓ 0.98</td></tr><tr><td style="text-align:right;">L3 trmm</td><td style="text-align:center;">✓ 0.95</td><td style="text-align:center;">✓ 1.08</td><td style="text-align:center;">✗ 0.81 (n=8 materialize-bound)</td><td style="text-align:center;">✗ 0.81</td></tr><tr><td style="text-align:right;">LAPACK geqrf · gesvd</td><td style="text-align:center;">✓ 1.03–1.21</td><td style="text-align:center;">✓ 0.97–1.00</td><td style="text-align:center;">✓ 1.06–1.08</td><td style="text-align:center;">✓ 1.06–1.08</td></tr><tr><td style="text-align:right;">LAPACK potrf · getrf</td><td style="text-align:center;">✓ 1.10 · ✓ 0.99</td><td style="text-align:center;"><strong>✓ 1.22 · ✓ 1.14</strong></td><td style="text-align:center;">✗ 0.83 (n=256) · <strong>✓ 1.03</strong> (geomean 1.34)</td><td style="text-align:center;">✗ 0.83 · ✓ 1.03</td></tr></tbody></table><blockquote><p>*** Haswell not tested on Haswell hardware.** Haswell runs the identical AVX2 code path (W=4, 16 ymm, two FMA units), so the AVX2 (Zen3) column is the best available proxy — but Zen3 ≠ Haswell (Haswell has a 256 KB L2 vs Zen3&#39;s 512 KB, FMA latency 5 vs 4, and lower memory bandwidth), and the gate is measured <strong>relative to OpenBLAS</strong>, whose per-µarch kernel dispatch differs between the two. Actual Haswell figures — and a <strong>vs-MKL</strong> comparison (MKL uses its native Haswell kernels on Intel) — are pending a run on the target machine: <code>taskset -c N julia --project=bench bench/plots.jl bench</code> (vs OpenBLAS) and <code>] add MKL; taskset -c N julia --project=bench bench/plots.jl bench mkl</code> (vs MKL). MKL throttles to a generic path on AMD, so the MKL baseline is only meaningful on the Intel Haswell target itself.</p></blockquote><p><strong>Zen5 (native AVX-512)</strong> shows a <em>disjoint</em> residual profile from Zen3/Zen4 — the key reason the gate is per-machine. Its native 512-bit ports clear every AVX2 pain point (<code>trmm</code> 0.81→<strong>1.08</strong>, <code>zgemm</code> 0.94→<strong>1.16</strong>, <code>potrf</code> 0.83→<strong>1.22</strong>, <code>trsm</code> <strong>1.15</strong>), but its LPDDR5x memory subsystem surfaces new ones: <strong><code>ger</code> 0.69</strong> (a rank-1 update is pure write-bandwidth at large n; OpenBLAS extracts ~1.3× more DRAM bandwidth there — non-temporal stores and multi-column blocking were both measured and don&#39;t recover it) and small-n <code>gemvN</code>/ <code>gemm</code>. Tuning for one µarch does not transfer, so each box is gated on its own hardware.</p><p>On <strong>AVX-512</strong> every op&#39;s <strong>geomean</strong> clears the gate (1.0–1.5×); the ✗ cells are small-n <strong>worst-size</strong> dips only (n=8 dispatch / cold-cache — <code>gemm</code> geomean is still 1.03). On <strong>AVX2</strong>, BLAS-1/2, real <code>gemm</code>, and now <code>syrk</code> + <code>syr2k</code> + <code>symm</code> gate — all closed by <strong>decomposition + OpenBLAS&#39;s source</strong> (the kernels already gated; the gap was overhead around them). <code>syrk</code> (0.79→<strong>1.02</strong>): <code>_syrk_scaleC!</code> was a full-n² β-prescale with a per-element branch (gemm folds β into its kernel) → branch-free + stored-triangle-only; plus route small n to the unified single-pack (½ the cold pack traffic, gates n=32). <code>symm</code> (0.90→<strong>0.99</strong>): OpenBLAS&#39;s symmetric copy switches the source stride <strong>once per column</strong> at the diagonal crossing instead of a per-element <code>i≤j</code> branch — the branchless pack made the fused path beat materialize+gemm. <code>syr2k</code> (0.91→<strong>0.96</strong>): OpenBLAS runs <strong>two full-kernel gemm passes</strong> (our fused 8-acc tile is ILP-starved on 16 regs), plus a <strong>β=0 overwrite</strong> mode (skip the scaleC zero-pass) — n=256 sits right at the gate. Complex <code>zgemm</code> is at the n=32 cold boundary (0.94 cold / <strong>~1.02 warm</strong>). <code>trsm</code> was first lifted (n≥512 gate) by <strong>disabling the po2 A-pad</strong> (the pad&#39;s O(k²) copy costs more than the aliasing it avoids — the old &quot;pad pays&quot; was a contended measurement), then the <strong>n=128/256 leaf</strong> was closed further: decomposing the invL/ invR base on an idle core showed the gap is the leaf <strong>GEMM</strong> (18µs/leaf, dominant), <em>not</em> the copyback (~1.5% — a prior mis-diagnosis). The leaf shape is skewed (nb ≤ 32 tiny, B wide), so the packed path pays a full B-pack + scaleC-zero pass for a k=32 contraction; routing the leaf multiply through the <strong>unpacked</strong> kernel (no B-pack, <code>Val{B0}</code> overwrite; 0.72× the packed time, beats OpenBLAS&#39;s own gemm there) lifts <strong>trsm n=256 0.85→~0.93</strong> and <strong>getrf n=256 0.91→~1.0 (now gates)</strong>. The residual then traced to the recursion&#39;s off-diagonal packed gemms at <strong>skewed small-square shapes</strong> (h=32/64, wide B — untested by the square-gemm gate): PB was 0.89–0.90× OB there purely from <strong>m-alignment</strong> (m a multiple of mr=12 ran 1.10–1.16, but m=32 with an 8-row=2·W remainder only 0.97; k barely mattered). The packed path masked such remainders (computing all mr rows to use mre); a <strong>clean clip kernel</strong> (<code>_microkernel_clip!</code> — reads the mr-strided panel but computes only the mre÷W live row-vectors, unmasked) removes the penalty: misaligned <strong>m=32 0.97→1.17</strong>, and <strong>trsm n=256 → ~0.97 (gates), getrf n=256 → ~1.0</strong>, with the square-gemm gate holding and several non-aligned sizes improving (AVX-512 too — pure gain, no regression). The clip is mirrored on the <strong>unpacked</strong> path (used at n ≤ <code>_GEMM_UNPACK_MAX</code>) — it closed the same penalty on trsm&#39;s n≤128 off-diagonals — and since the invL leaf&#39;s gemm is now cheap, the narrow-B dense cut <code>_TRSM_NCUT</code> dropped 96→64 (<strong>trsm n=96 0.90→1.09</strong>). Routing <em>unpacked</em> to the off-diagonal gemms of the wide path, by contrast, <em>regressed</em> (in-context cache thrash — the isolated micro-bench lied). Finally, a ceiling test (stubbing the leaf&#39;s <code>trtri</code>) showed the O(nb³) scalar triangular-inverse was ~20% of the invL leaf, so it was replaced with a <strong>blocked inverse</strong> (recurse the two diagonal half-blocks, combine the off-block with the now-cheap gemm): <strong>trsm n=128 0.92→0.98 and n=256 0.97→1.02, getrf n=128 0.92→0.98</strong> — with which <strong><code>trsm</code> now clears the full worst-size gate on AVX2</strong> (0.98, geomean 1.03), and <strong><code>getrf</code> too</strong> after a blocked panel factorization: a phase decomposition of <code>getrf(256)</code> showed the unblocked panel <code>getf2</code> was 39.5% of runtime — a right-looking rank-1 sweep that rewrites the trailing panel once per pivot column (store-bound BLAS-2) — while its trailing <code>gemm</code> gates and its panel <code>trsm</code> <em>beats</em> OpenBLAS 1.37×. So the whole residual was <code>getf2</code>. Splitting the panel once and doing the cross-half update via BLAS-3 (<code>trsm</code> U12=L11⁻¹A12, <code>gemm</code> A22−=L21·U12) instead of pb1 rank-1 passes halved the panel store traffic (<code>getf2</code> 213→150µs) with bit-identical pivoting (ipiv matches LAPACK, factor relerr 7e-15): <strong>getrf n=256 0.93→1.03, now gates every size (worst 1.03, geomean 1.34)</strong>. (Two earlier <code>getf2</code> tweaks — register- blocking the rank-1 update and reusing the SIMD <code>iamax</code> for the pivot search — were measured and <em>regressed</em>; the panel is store-bound, not compute- or reload-bound, so only the BLAS-3 restructure moved it.)</p><p>Remaining AVX2 ✗: <code>potrf</code> (worst n=256 <strong>0.79</strong>, geomean 0.98) and <code>trmm</code> n=8. The earlier fix — ISA- conditional faer base (1024→128 on AVX2, routing the O(n³) trailing update through the gating cache-blocked <code>syrk!</code>) — killed the old n=1024 0.70 cliff (→0.89). But a phase decomposition of <code>potrf(256)</code> shows the remaining small-n gap is <strong>50% in the two faer Cholesky base blocks</strong> (the left-looking SIMD kernels, sqrt-dependency-chain latency-bound and register-tuned for AVX-512&#39;s 32 regs), 28% <code>trsm</code> side-R (~0.90), 22% <code>syrk</code> (gates). Unlike <code>getf2</code>, there&#39;s no config lever: lowering the hybrid base <em>hurts</em> (the faer coupling beats the gating <code>syrk!</code>/<code>trsm!</code> at 64-block sizes), and even a perfect <code>trsm</code>-R lifts <code>potrf</code> only ~3%. Closing it needs an AVX2-specific rewrite of the faer base kernels (the 50% wall). <code>trmm</code> n=8 is the 8×8 materialize-bound floor (every non-materializing kernel measured slower). <code>zgemm</code> is a SIMD split-pack kernel that <strong>beats OpenBLAS on AVX-512</strong>.</p><p>All Level-3 scratch is owned by a single per-type <code>L3Workspace</code> (<code>src/workspace.jl</code>) — the seven former global buffer caches bundled into one concrete-field struct (const-dispatched for Float64/Float32, so no lookup on the hot path), mirroring PureFFT&#39;s plan-owned scratch. This removed the abstract-<code>Matrix</code> boxing that recurred on the old IdDict returns and makes the whole L3 thread-ready in one place; it is performance-neutral single-thread (measured, tiny-n within noise).</p><blockquote><p><strong>Measurement note (learned the hard way):</strong> with CPU boost enabled, allocating between timed regions drops the core off boost mid-measurement, biasing whichever side is timed first — this once fabricated a fake <code>zgemm</code> &quot;n=32 hardware floor&quot; that clean pinned-frequency measurement puts at 1.0–1.03. Always disable boost (<code>echo 0 | sudo tee /sys/devices/system/cpu/cpufreq/boost</code>, <code>performance</code> governor) before trusting a gate number.</p></blockquote><h2 id="BLAS-1" tabindex="-1">BLAS-1 <a class="header-anchor" href="#BLAS-1" aria-label="Permalink to &quot;BLAS-1 {#BLAS-1}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+n+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+a+'" alt=""></p><p><strong>Zen5 (native AVX-512):</strong></p><p><img src="'+d+'" alt=""></p><p>Bandwidth-bound; PureBLAS matches or beats OpenBLAS across the board. <code>nrm2</code> is markedly faster because OpenBLAS&#39;s <code>dnrm2</code> uses the slow always-scaled LAPACK algorithm, while PureBLAS uses a SIMD sum-of-squares with a scaled fallback only on overflow/underflow.</p><h2 id="BLAS-2" tabindex="-1">BLAS-2 <a class="header-anchor" href="#BLAS-2" aria-label="Permalink to &quot;BLAS-2 {#BLAS-2}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+l+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+c+'" alt=""></p><p><strong>Zen5 (native AVX-512):</strong></p><p><img src="'+i+'" alt=""></p><p>Matrix-vector and the packed/banded variants. The headline lessons (full detail in the project&#39;s <code>kb/</code> findings):</p><ul><li><p><strong>gemv</strong> is column-major, so <code>gemv-N</code> is transpose-like — a size-dispatched <strong>column-panel</strong> kernel cuts the y-restream; <code>gemv-T</code> is a column-block sharing x-chunks.</p></li><li><p><strong>symv</strong> reads only half of A, so the vector re-stream costs more — a <strong>fused panel</strong> does the symmetric <code>gemv-N + gemv-T</code> in one pass with the triangular diagonal folded into the same accumulators.</p></li><li><p><strong>trmv/trsv</strong> are blocked at large n (diagonal block + off-diagonal <code>gemv</code>, reading A once).</p></li><li><p><strong>packed/banded</strong> reuse the same per-column kernels (packed and band columns are contiguous); <code>gbmv</code> uses convolution-style kernels with BLASFEO-style register reuse for wide bands.</p></li></ul><h2 id="BLAS-3" tabindex="-1">BLAS-3 <a class="header-anchor" href="#BLAS-3" aria-label="Permalink to &quot;BLAS-3 {#BLAS-3}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+g+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+m+'" alt=""></p><p><strong>Zen5 (native AVX-512):</strong></p><p><img src="'+p+'" alt=""></p><p>The compute-bound level — <strong>median ratio vs size</strong> (log-log). On <strong>AVX-512</strong> every op&#39;s geomean clears the 0.96× gate and small sizes run 1–3× OpenBLAS (the former small-n dips were hidden overheads — scratch-lookup costs, per-call workspaces, kwarg dispatch — all catalogued in the project kb). A few small-n <strong>worst-size</strong> cells still dip below the strict gate (<code>gemm</code> n=8 ≈ 0.87 from dispatch/cold-cache; <code>symm</code>, <code>gemvN</code> ≈ 0.96) — geomeans stay ≥1.0. On <strong>AVX2</strong> (see the fleet table above) <code>gemm</code> and complex <code>zgemm</code> gate, but the triangular/symmetric ops still carry a Zen3 small-n gap. <code>gemm</code> is the BLIS 5-loop with a SIMD micro-kernel (unpacked small-matrix path); <code>zgemm</code> is a complex split-pack kernel (real+imag panels, 4-real-FMA MAC); the rest are built on <code>gemm</code>:</p><ul><li><p><strong>syrk/syr2k/symm</strong> pack the stored/symmetric triangle into <code>gemm</code>&#39;s format in a single pass and use a triangular-store micro-kernel at the diagonal (no materialize, no wasted flops).</p></li><li><p><strong>trmm/trsm</strong> trim the contracted K-range over the zero band per tile; <code>trsm</code> inverts small diagonal blocks and applies them as <code>gemm</code>s. Both factor/pack into a padded leading dim to dodge power-of-2 cache aliasing.</p></li></ul><h2 id="LAPACK" tabindex="-1">LAPACK <a class="header-anchor" href="#LAPACK" aria-label="Permalink to &quot;LAPACK {#LAPACK}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+h+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+u+'" alt=""></p><p><strong>Zen5 (native AVX-512):</strong></p><p><img src="'+f+'" alt=""></p><p>Factorizations driven by the gated BLAS, again <strong>median ratio vs size</strong> for <strong>Zen4/AVX-512</strong> — gating at every size, with tiny-n factors 1.5–4× OpenBLAS after the workspace-caching fixes. (On AVX2, <code>geqrf</code>/ <code>gesvd</code>/<code>getrf</code> gate every size and tiny-n factors run 1.3–2.5×; the only sub-gate cells are <code>potrf</code> n=128–256 — the latency-bound faer Cholesky base kernels, see the fleet table.) <code>potrf</code>/<code>geqrf</code> port the irreducible faer SIMD kernels and drive the blocked level with PureBLAS <code>gemm!</code>/<code>trsm!</code>; <code>getrf</code> is blocked right-looking with a BLAS-3 blocked panel and deferred pivoting; <code>gesvd</code> is gebrd → divide-and-conquer bidiagonal SVD → blocked compact-WY back-transform.</p><h2 id="Complex-ComplexF64" tabindex="-1">Complex (ComplexF64) <a class="header-anchor" href="#Complex-ComplexF64" aria-label="Permalink to &quot;Complex (ComplexF64) {#Complex-ComplexF64}&quot;">​</a></h2><p>The complex surface is now <strong>SIMD across L1/L2/L3</strong> — it used to be scalar except <code>zgemm</code>. The kernels are portable (SIMD.jl <code>Vec</code>/<code>fma</code>, no x86 intrinsics): a <strong>swap-pairs</strong> complex multiply for scalar×vector (scal/axpy/ger), an <strong>interleaved-product</strong> reduction for the complex dot (deinterleave only the accumulators — cheap on AVX2&#39;s shuffle ports), a <strong>real-reinterpret</strong> trick for <code>nrm2</code>/<code>asum</code> (which reduce exactly to the real kernel over the 2n-real buffer), a <strong>fused axpy+conj-dot</strong> column kernel for <code>hemv</code>, and — mirroring the real kernels so no half is wasted — a <strong>K-trimmed</strong> <code>trmm</code>/<code>trsm</code> (each tile contracts only the nonzero band; <code>trsm</code> inverts small diagonal blocks and re-uses the trim) plus a <strong>triangular-output</strong> <code>herk</code>/ <code>syrk</code> (a single-pass packed kernel that computes only the stored triangle, no full <code>A·Aᴴ</code>). The generic scalar path is kept as the AD (ForwardDiff/Enzyme) path.</p><p>The complex L2 gate campaign added the blocking the real kernels already had: <strong><code>zgemvC</code>/<code>zgemvT</code></strong> now <strong>column-block</strong> (NC columns per pass share each x-chunk <em>and</em> its swap — one shuffle feeds NC columns, x streamed once instead of per column); <strong><code>ztrmv</code>/<code>ztrsv</code></strong> are <strong>blocked</strong> (NB×NB diagonal via the per-column kernel, off-diagonal via the gating complex <code>gemv</code>, breaking the per-column x-restream + serialization at large n); and the <code>gemv</code> cache thresholds are <strong>keyed to detected L2</strong> (<code>_CGEMV_RB = L2÷16</code>, so Zen3&#39;s 512 KB doesn&#39;t inherit Zen4&#39;s 1 MB and thrash mid-n). Complex <code>zgemvN</code> is <strong>ILP-tuned</strong> (MR=4 square / MR=3 tall scatter).</p><p><strong>Complex BLAS-1 / BLAS-2 (violins) — AVX-512 (Zen4):</strong></p><p><img src="'+y+'" alt=""></p><p><img src="'+v+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+b+'" alt=""></p><p><img src="'+x+'" alt=""></p><p><strong>Zen5 (native AVX-512):</strong></p><p><img src="'+A+'" alt=""></p><p><img src="'+k+'" alt=""></p><p><strong>Complex BLAS-3 (trend) — AVX-512 (Zen4), AVX2 (Zen3), Zen5:</strong></p><p><img src="'+w+'" alt=""></p><p><img src="'+L+'" alt=""></p><p><img src="'+_+'" alt=""></p><p>On <strong>AVX-512</strong> the whole complex L1/L2 surface gates or beats OpenBLAS — <code>zhemv</code> ≈ <strong>2×</strong>, <code>dznrm2</code> <strong>4–6×</strong> (OpenBLAS&#39;s is the slow always-scaled <code>dznrm2</code>), <code>zgeru</code>/<code>zdotc</code>/<code>zscal</code>/<code>zaxpy</code> clear it, <code>zgemvC</code>/<code>zgemvN</code> gate (a couple of ~0.95 worst-size dips), and blocked <code>ztrmv</code>/<code>ztrsv</code> gate through n=2048; <code>zgemm</code>/<code>zhemm</code>/ <code>zherk</code> 1.05–1.4×, complex <code>trsm</code> gates at large n. Residuals there are small-n only (<code>ztrsv</code> n=64/128 ≈ 0.88, the sequential complex-divide substitution).</p><p>On <strong>AVX2 (Zen3)</strong> the campaign closed most of the surface: <code>zhemv</code>/<code>zgeru</code>/<code>dzasum</code> beat OpenBLAS, <code>zgemvC</code> gates almost everywhere (column-blocking; small residuals at n=64 and the mid-n cache transition), and blocked <code>ztrmv</code>/<code>ztrsv</code> gate at large n. Two AVX2 residuals remain: <strong><code>zgemvN</code></strong> (best ~0.73–0.83 at n≤1024, after MR=4 ILP tuning) and the axpy-based <strong><code>ztrmv</code>/<code>ztrsv</code> mid-n</strong> (0.85–0.96). This is <strong>not</strong> a hardware limit — OpenBLAS is faster on the same Zen3 silicon, so the chip can clearly do it — it&#39;s a <strong>kernel-formulation</strong> gap. The portable complex multiply-accumulate is <code>swap + 2 FMA</code> into a <code>Vec{2W}</code> accumulator (2 ymm on AVX2); to hide Zen3&#39;s FMA latency it wants ~8 independent chains but AVX2&#39;s 16 ymm fit only ~4, so it caps below OpenBLAS. <code>vfmaddsub</code>/<code>vaddsub</code> <strong>are</strong> reachable portably (the pattern <code>shufflevector(muladd(a,b,-c), muladd(a,b,c))</code> auto-fuses to <code>vfmaddsub213pd</code> on x86, plain shuffle+add/sub on ARM — no intrinsic needed), but they don&#39;t help the <em>accumulating</em> gemv: <code>vfmaddsub</code> subtracts the destination on even lanes, so it can&#39;t fold a running sum, and a single-accumulator variant measured <em>worse</em> (extra mul+add per element). Matching OpenBLAS here needs its specific register-blocking/scheduling, not a missing instruction — an open item, not a ceiling. On AVX-512 (32 zmm) the same portable kernel has room for the chains and gates. Small-n complex-triangular <code>ztrmm</code>/<code>ztrsm</code> carry the same materialize/copyback + complex-<code>gemm</code> overhead that bounds their real counterparts.</p><h2 id="Milestones" tabindex="-1">Milestones <a class="header-anchor" href="#Milestones" aria-label="Permalink to &quot;Milestones {#Milestones}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Milestone</th><th style="text-align:right;">Scope</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>M1</strong></td><td style="text-align:right;">BLAS-1 (axpy, dot, nrm2, asum, scal, copy, swap, iamax; s/d/c/z)</td><td style="text-align:right;">✅ gate met; LBT <code>.so</code> + native API</td></tr><tr><td style="text-align:right;"><strong>M2</strong></td><td style="text-align:right;"><code>dgemm</code> (BLIS 5-loop + SIMD microkernel; unpacked small-matrix path)</td><td style="text-align:right;">✅ single-thread parity (geomean ≈ 1.0×)</td></tr><tr><td style="text-align:right;"><strong>M3 (core L2)</strong></td><td style="text-align:right;">gemv, ger, symv, hemv, trmv, trsv + packed (spmv/hpmv/tpmv/tpsv) and banded (gbmv/sbmv/hbmv/tbmv/tbsv)</td><td style="text-align:right;">✅ gate met across the surface</td></tr><tr><td style="text-align:right;"><strong>L3</strong></td><td style="text-align:right;">gemm, symm, syrk, syr2k, trmm, trsm</td><td style="text-align:right;">✅ AVX-512 gates all n; AVX2 gates gemm + syrk + syr2k + symm + <strong>trsm</strong>; trmm n=8 WIP</td></tr><tr><td style="text-align:right;"><strong>M5 complex SIMD</strong></td><td style="text-align:right;">whole ComplexF64 surface — L1 (nrm2/asum/scal/axpy/dotu/dotc), L2 (gemv/ger/hemv/trmv/trsv), L3 (gemm/hemm/herk/symm/syrk + trmm/trsm both sides)</td><td style="text-align:right;">✅ AVX-512 gates/beats OpenBLAS (hemv 2×, nrm2 4–6×); AVX2 L1/L2 gate, complex-triangular + small-n residuals</td></tr><tr><td style="text-align:right;"><strong>LAPACK</strong></td><td style="text-align:right;">potrf (Cholesky), geqrf (QR), getrf (LU), gesvd (SVD)</td><td style="text-align:right;">✅ AVX-512 gates all n; AVX2 geqrf/gesvd/getrf gate (potrf n≤256 WIP)</td></tr><tr><td style="text-align:right;"><strong>M4</strong></td><td style="text-align:right;">multithreading</td><td style="text-align:right;">deferred</td></tr></tbody></table><p>Both consumption modes share one kernel set: the <strong>native API</strong> (<code>PureBLAS.gemv!(…)</code>, AD-traceable) and the <strong>LBT drop-in</strong> <code>.so</code> (<code>@ccallable</code> ILP64 symbols via <code>juliac --trim</code>).</p>',67)])])}const O=t(S,[["render",B]]);export{Z as __pageData,O as default};
