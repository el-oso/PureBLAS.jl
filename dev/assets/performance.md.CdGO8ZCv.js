import{_ as t,o as r,c as s,ao as o}from"./chunks/framework.mOy25YJp.js";const a="/PureBLAS.jl/dev/assets/perf_l1_avx512.CQjJp3Uh.svg",n="/PureBLAS.jl/dev/assets/perf_l1_avx2.DbBVJWGL.svg",d="/PureBLAS.jl/dev/assets/perf_l2_avx512.B9aubFS2.svg",l="/PureBLAS.jl/dev/assets/perf_l2_avx2.3WbMNP8r.svg",i="/PureBLAS.jl/dev/assets/perf_l3_avx512.CjG7BY2d.svg",g="/PureBLAS.jl/dev/assets/perf_l3_avx2.BXHmTfzF.svg",c="/PureBLAS.jl/dev/assets/perf_lapack_avx512.8pU_OZ2u.svg",m="/PureBLAS.jl/dev/assets/perf_lapack_avx2.DSsF92xK.svg",x=JSON.parse('{"title":"Performance","description":"","frontmatter":{},"headers":[],"relativePath":"performance.md","filePath":"performance.md","lastUpdated":null}'),h={name:"performance.md"};function p(y,e,u,v,f,b){return r(),s("div",null,[...e[0]||(e[0]=[o('<h1 id="Performance" tabindex="-1">Performance <a class="header-anchor" href="#Performance" aria-label="Permalink to &quot;Performance {#Performance}&quot;">​</a></h1><p>PureBLAS targets a hard, non-negotiable gate: <strong>≥ 0.96× OpenBLAS</strong>, measured per machine, single threaded. The plots below are the PureBLAS/OpenBLAS speed ratio (so <strong>higher is better; 1.0× is parity; the dashed line is the 0.96× gate</strong>). Each <strong>violin</strong> is the distribution of the ratio across the size sweep (and rounds); the horizontal line is the median and the dark dot is the <em>worst</em> size — a violin is green only when <strong>every</strong> size clears the gate.</p><p>Methodology (see <code>bench/</code>): single-thread (<code>BLAS.set_num_threads(1)</code>), Float64 (plus ComplexF64 for <code>zgemm</code>), native PureBLAS API vs <code>LinearAlgebra.BLAS</code>, <strong>interleaved</strong> timing (each round times OpenBLAS then PureBLAS back-to-back so frequency drift cancels), <strong>median</strong> of many rounds, core pinned with <code>taskset</code>, and — importantly — <strong>CPU boost disabled</strong> (a floating boost clock silently biases small-n ratios; see below). Reproduce: <code>taskset -c 2 julia --project=bench bench/plots.jl</code>. Each section shows <strong>both</strong> ISAs of the dev fleet — <strong>AVX-512</strong> (Zen4, the primary tuning target) and native <strong>AVX2</strong> (Zen3).</p><h2 id="Per-ISA-gate-dev-fleet" tabindex="-1">Per-ISA gate (dev fleet) <a class="header-anchor" href="#Per-ISA-gate-dev-fleet" aria-label="Permalink to &quot;Per-ISA gate (dev fleet) {#Per-ISA-gate-dev-fleet}&quot;">​</a></h2><p>The 0.96× gate is <strong>per machine</strong>. The fleet spans double-pumped <strong>AVX-512</strong> (Zen4, the tuning target) and native <strong>AVX2</strong> (Zen3). Below is the full-stack <code>plots.jl bench</code> at pinned frequency, worst-size ratio (the gate metric) with ✓ ≥ 0.96 / ✗ &lt; 0.96; geomeans are given in text.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Op</th><th style="text-align:center;">AVX-512 (Zen4)</th><th style="text-align:center;">AVX2 (Zen3)</th></tr></thead><tbody><tr><td style="text-align:right;">L1 axpy · dot · asum · scal</td><td style="text-align:center;">✓ 0.99–1.06</td><td style="text-align:center;">✓ 0.99–1.33</td></tr><tr><td style="text-align:right;">L1 nrm2 (scaled-accum beats OpenBLAS)</td><td style="text-align:center;">✓ 3.6×</td><td style="text-align:center;">✓ 5.5×</td></tr><tr><td style="text-align:right;">L1 iamax</td><td style="text-align:center;">✓ 1.15</td><td style="text-align:center;">✗ 0.90</td></tr><tr><td style="text-align:right;">L2 gemvT · ger · symv · trmv · trsv · spmv · gbmv · sbmv</td><td style="text-align:center;">✓ 1.0–1.6</td><td style="text-align:center;">✓ 0.97–1.64</td></tr><tr><td style="text-align:right;">L2 gemvN</td><td style="text-align:center;">✗ 0.96</td><td style="text-align:center;">✗ 0.87</td></tr><tr><td style="text-align:right;">L3 gemm</td><td style="text-align:center;">✗ 0.89 (n=8)</td><td style="text-align:center;">✓ 1.01</td></tr><tr><td style="text-align:right;"><strong>L3 syrk · syr2k</strong> (overhead-decomposed to the gate)</td><td style="text-align:center;">✓ 0.97–0.98</td><td style="text-align:center;">✓ 0.97–1.00</td></tr><tr><td style="text-align:right;"><strong>L3 zgemm (complex)</strong></td><td style="text-align:center;"><strong>✓ 1.12</strong></td><td style="text-align:center;">◐ 0.93 (n=32 cold; ~1.03 hot)</td></tr><tr><td style="text-align:right;"><strong>L3 trsm</strong> (vectorized trtri + 0-alloc)</td><td style="text-align:center;">✓ 1.00</td><td style="text-align:center;">◐ 0.86 (geomean 0.95; was 0.65)</td></tr><tr><td style="text-align:right;">L3 trmm · symm</td><td style="text-align:center;">✓ 0.97</td><td style="text-align:center;">✗ 0.94 · 0.89</td></tr><tr><td style="text-align:right;">LAPACK geqrf · gesvd</td><td style="text-align:center;">✓ 1.03–1.21</td><td style="text-align:center;">✓ 1.07–1.08</td></tr><tr><td style="text-align:right;">LAPACK potrf · getrf</td><td style="text-align:center;">✓ 1.10 · ✓ 0.99</td><td style="text-align:center;">✗ 0.67 · 0.90</td></tr></tbody></table><p>On <strong>AVX-512</strong> every op&#39;s <strong>geomean</strong> clears the gate (1.0–1.5×); the ✗ cells are small-n <strong>worst-size</strong> dips only (n=8 dispatch / cold-cache — <code>gemm</code> geomean is still 1.03). On <strong>AVX2</strong>, BLAS-1/2, real <code>gemm</code>, and now <code>syrk</code> + <code>syr2k</code> gate — both closed by <strong>decomposition</strong> (the kernels already gated at every size; the gap was overhead around them). <code>syrk</code> (worst 0.79→<strong>1.00</strong>): a full-n² <code>_syrk_scaleC!</code> pre-scale pass with a per-element branch (gemm folds β into its kernel), made branch-free and stored-triangle-only (gates n≥128), plus routing small n through the unified single-pack whose halved cold pack-traffic gates n=32 (0.90→1.19). <code>syr2k</code> (worst 0.95→<strong>0.97</strong>): n=128 had fallen to the gemm-temp recursion while the fused packed kernel gated — lowered its routing cut so n≥128 uses the kernel. <code>trsm</code> reached geomean <strong>0.95</strong> (worst 0.86) via the vectorized <code>_trtri!</code> (solve <code>A·V=I</code> on the SIMD dense-L base) and removing a per-recursion-leaf boxing (a non-concrete scratch return made the wide-B invL/invR <code>view</code> type-unstable). Complex <code>zgemm</code> sits at the n=32 cold boundary (0.93 cold / <strong>~1.03 hot</strong> — cold small-complex is 2× the bytes and unrepresentative). The remaining ✗ are <code>trmm</code> 0.94 · <code>symm</code> 0.89 and <code>potrf</code>/<code>getrf</code> built on them — the in-progress Zen3 small-n campaign (the same overhead-decomposition discipline that just gated <code>syrk</code>/<code>syr2k</code> isn&#39;t ported to <code>trmm</code>/<code>symm</code> yet). <code>zgemm</code> is a SIMD split-pack kernel that <strong>beats OpenBLAS on AVX-512</strong>.</p><blockquote><p><strong>Measurement note (learned the hard way):</strong> with CPU boost enabled, allocating between timed regions drops the core off boost mid-measurement, biasing whichever side is timed first — this once fabricated a fake <code>zgemm</code> &quot;n=32 hardware floor&quot; that clean pinned-frequency measurement puts at 1.0–1.03. Always disable boost (<code>echo 0 | sudo tee /sys/devices/system/cpu/cpufreq/boost</code>, <code>performance</code> governor) before trusting a gate number.</p></blockquote><h2 id="BLAS-1" tabindex="-1">BLAS-1 <a class="header-anchor" href="#BLAS-1" aria-label="Permalink to &quot;BLAS-1 {#BLAS-1}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+a+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+n+'" alt=""></p><p>Bandwidth-bound; PureBLAS matches or beats OpenBLAS across the board. <code>nrm2</code> is markedly faster because OpenBLAS&#39;s <code>dnrm2</code> uses the slow always-scaled LAPACK algorithm, while PureBLAS uses a SIMD sum-of-squares with a scaled fallback only on overflow/underflow.</p><h2 id="BLAS-2" tabindex="-1">BLAS-2 <a class="header-anchor" href="#BLAS-2" aria-label="Permalink to &quot;BLAS-2 {#BLAS-2}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+d+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+l+'" alt=""></p><p>Matrix-vector and the packed/banded variants. The headline lessons (full detail in the project&#39;s <code>kb/</code> findings):</p><ul><li><p><strong>gemv</strong> is column-major, so <code>gemv-N</code> is transpose-like — a size-dispatched <strong>column-panel</strong> kernel cuts the y-restream; <code>gemv-T</code> is a column-block sharing x-chunks.</p></li><li><p><strong>symv</strong> reads only half of A, so the vector re-stream costs more — a <strong>fused panel</strong> does the symmetric <code>gemv-N + gemv-T</code> in one pass with the triangular diagonal folded into the same accumulators.</p></li><li><p><strong>trmv/trsv</strong> are blocked at large n (diagonal block + off-diagonal <code>gemv</code>, reading A once).</p></li><li><p><strong>packed/banded</strong> reuse the same per-column kernels (packed and band columns are contiguous); <code>gbmv</code> uses convolution-style kernels with BLASFEO-style register reuse for wide bands.</p></li></ul><h2 id="BLAS-3" tabindex="-1">BLAS-3 <a class="header-anchor" href="#BLAS-3" aria-label="Permalink to &quot;BLAS-3 {#BLAS-3}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+i+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+g+'" alt=""></p><p>The compute-bound level — <strong>median ratio vs size</strong> (log-log). On <strong>AVX-512</strong> every op&#39;s geomean clears the 0.96× gate and small sizes run 1–3× OpenBLAS (the former small-n dips were hidden overheads — scratch-lookup costs, per-call workspaces, kwarg dispatch — all catalogued in the project kb). A few small-n <strong>worst-size</strong> cells still dip below the strict gate (<code>gemm</code> n=8 ≈ 0.87 from dispatch/cold-cache; <code>symm</code>, <code>gemvN</code> ≈ 0.96) — geomeans stay ≥1.0. On <strong>AVX2</strong> (see the fleet table above) <code>gemm</code> and complex <code>zgemm</code> gate, but the triangular/symmetric ops still carry a Zen3 small-n gap. <code>gemm</code> is the BLIS 5-loop with a SIMD micro-kernel (unpacked small-matrix path); <code>zgemm</code> is a complex split-pack kernel (real+imag panels, 4-real-FMA MAC); the rest are built on <code>gemm</code>:</p><ul><li><p><strong>syrk/syr2k/symm</strong> pack the stored/symmetric triangle into <code>gemm</code>&#39;s format in a single pass and use a triangular-store micro-kernel at the diagonal (no materialize, no wasted flops).</p></li><li><p><strong>trmm/trsm</strong> trim the contracted K-range over the zero band per tile; <code>trsm</code> inverts small diagonal blocks and applies them as <code>gemm</code>s. Both factor/pack into a padded leading dim to dodge power-of-2 cache aliasing.</p></li></ul><h2 id="LAPACK" tabindex="-1">LAPACK <a class="header-anchor" href="#LAPACK" aria-label="Permalink to &quot;LAPACK {#LAPACK}&quot;">​</a></h2><p><strong>AVX-512 (Zen4):</strong></p><p><img src="'+c+'" alt=""></p><p><strong>AVX2 (Zen3):</strong></p><p><img src="'+m+'" alt=""></p><p>Factorizations driven by the gated BLAS, again <strong>median ratio vs size</strong> for <strong>Zen4/AVX-512</strong> — gating at every size, with tiny-n factors 1.5–4× OpenBLAS after the workspace-caching fixes. (On AVX2, <code>geqrf</code>/ <code>gesvd</code> gate; <code>potrf</code>/<code>getrf</code> inherit the below-gate triangular L3 kernels — see the fleet table.) <code>potrf</code>/<code>geqrf</code> port the irreducible faer SIMD kernels and drive the blocked level with PureBLAS <code>gemm!</code>/<code>trsm!</code>; <code>getrf</code> is blocked right-looking with deferred pivoting; <code>gesvd</code> is gebrd → divide-and-conquer bidiagonal SVD → blocked compact-WY back-transform.</p><h2 id="Milestones" tabindex="-1">Milestones <a class="header-anchor" href="#Milestones" aria-label="Permalink to &quot;Milestones {#Milestones}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">Milestone</th><th style="text-align:right;">Scope</th><th style="text-align:right;">Status</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>M1</strong></td><td style="text-align:right;">BLAS-1 (axpy, dot, nrm2, asum, scal, copy, swap, iamax; s/d/c/z)</td><td style="text-align:right;">✅ gate met; LBT <code>.so</code> + native API</td></tr><tr><td style="text-align:right;"><strong>M2</strong></td><td style="text-align:right;"><code>dgemm</code> (BLIS 5-loop + SIMD microkernel; unpacked small-matrix path)</td><td style="text-align:right;">✅ single-thread parity (geomean ≈ 1.0×)</td></tr><tr><td style="text-align:right;"><strong>M3 (core L2)</strong></td><td style="text-align:right;">gemv, ger, symv, hemv, trmv, trsv + packed (spmv/hpmv/tpmv/tpsv) and banded (gbmv/sbmv/hbmv/tbmv/tbsv)</td><td style="text-align:right;">✅ gate met across the surface</td></tr><tr><td style="text-align:right;"><strong>L3</strong></td><td style="text-align:right;">gemm, symm, syrk, syr2k, trmm, trsm</td><td style="text-align:right;">✅ AVX-512 gates all n; AVX2 gates gemm + syrk + syr2k, trmm/symm WIP</td></tr><tr><td style="text-align:right;"><strong>L3 complex</strong></td><td style="text-align:right;">zgemm (ComplexF64 split-pack SIMD)</td><td style="text-align:right;">✅ beats OpenBLAS on AVX-512; gates on AVX2</td></tr><tr><td style="text-align:right;"><strong>LAPACK</strong></td><td style="text-align:right;">potrf (Cholesky), geqrf (QR), getrf (LU), gesvd (SVD)</td><td style="text-align:right;">✅ AVX-512 gates all n; AVX2 geqrf/gesvd gate</td></tr><tr><td style="text-align:right;"><strong>M4</strong></td><td style="text-align:right;">multithreading</td><td style="text-align:right;">deferred</td></tr></tbody></table><p>Both consumption modes share one kernel set: the <strong>native API</strong> (<code>PureBLAS.gemv!(…)</code>, AD-traceable) and the <strong>LBT drop-in</strong> <code>.so</code> (<code>@ccallable</code> ILP64 symbols via <code>juliac --trim</code>).</p>',37)])])}const k=t(h,[["render",p]]);export{x as __pageData,k as default};
