import{_ as t,o as a,c as i,ao as s}from"./chunks/framework.CJX_rJCp.js";const u=JSON.parse('{"title":"SIMD & Hardware Adaptation","description":"","frontmatter":{},"headers":[],"relativePath":"simd.md","filePath":"simd.md","lastUpdated":null}'),o={name:"simd.md"};function n(r,e,d,l,h,c){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="SIMD-and-Hardware-Adaptation" tabindex="-1">SIMD &amp; Hardware Adaptation <a class="header-anchor" href="#SIMD-and-Hardware-Adaptation" aria-label="Permalink to &quot;SIMD &amp; Hardware Adaptation {#SIMD-and-Hardware-Adaptation}&quot;">​</a></h1><p>PureBLAS is written in Julia, but its kernels are not written against any one CPU. This page covers how a single generic kernel set adapts to the machine it runs on: the vector abstraction, the hardware detection that happens at compile time, and the rule that every tuning parameter is a formula over detected hardware rather than a hardcoded per-microarchitecture literal. It also sets that against the ahead-of-time model used by C and Rust libraries like OpenBLAS and faer.</p><h2 id="Why-not-just-let-the-compiler-vectorise-it?" tabindex="-1">Why not just let the compiler vectorise it? <a class="header-anchor" href="#Why-not-just-let-the-compiler-vectorise-it?" aria-label="Permalink to &quot;Why not just let the compiler vectorise it? {#Why-not-just-let-the-compiler-vectorise-it?}&quot;">​</a></h2><p>This is the first question most people ask, and it deserves a straight answer, because the premise is correct: Julia&#39;s compiler <em>is</em> good at vectorising loops. Write the textbook three-line matrix multiply, put <code>@inbounds</code> and <code>@simd</code> on it, and LLVM really does turn the inner loop into AVX-512 code. You can check — the generated IR for that loop contains <code>fmul &lt;8 x double&gt;</code>, <code>fadd &lt;8 x double&gt;</code> and <code>load &lt;8 x double&gt;</code>, which is eight lanes at a time, exactly what you would hope for.</p><p>Then you measure it against PureBLAS on the same machine, one thread, Float64:</p><table tabindex="0"><thead><tr><th style="text-align:right;">n</th><th style="text-align:right;">naive loop</th><th style="text-align:right;">PureBLAS</th><th style="text-align:right;"></th></tr></thead><tbody><tr><td style="text-align:right;">256</td><td style="text-align:right;">0.70 GFlop/s</td><td style="text-align:right;">43.6 GFlop/s</td><td style="text-align:right;">62×</td></tr><tr><td style="text-align:right;">512</td><td style="text-align:right;">0.21 GFlop/s</td><td style="text-align:right;">42.5 GFlop/s</td><td style="text-align:right;">199×</td></tr><tr><td style="text-align:right;">1000</td><td style="text-align:right;">2.41 GFlop/s</td><td style="text-align:right;">42.2 GFlop/s</td><td style="text-align:right;">17×</td></tr></tbody></table><p>Both are running vectorised code. The difference is not the instructions — it is where the numbers are coming from.</p><p>Think about what the naive loop does to memory. To compute one entry of the result it walks a whole row of <code>A</code> and a whole column of <code>B</code>. Walking down a column means each step jumps a full row-length ahead in memory, so every value it wants sits in a different cache line. The CPU fetches 64 bytes and uses 8 of them. Do that a few billion times and the arithmetic units spend nearly all their time idle, waiting. Vectorising that loop makes the CPU wait eight lanes at a time. It does not make it wait less.</p><p>What a real BLAS does is rearrange the <em>work</em> so that data, once fetched, gets used many times before it is evicted. It chops the matrices into blocks sized to fit in L1 and L2, copies each block into a small contiguous scratch buffer so the inner loop reads straight through memory, and holds a tile of the result in registers across the whole inner loop. None of that changes the arithmetic — the same multiplications happen in a different order — but it changes how often each number has to be fetched, and that is what the 17–200× is.</p><p>A compiler will not do this for you, and not because it is not clever enough. Choosing block sizes needs the cache sizes, which are a property of the machine, not of the code. Packing operands into scratch buffers means allocating memory and copying data that the program you wrote never asked for. Reordering the loop nest that far changes the order of floating-point additions, which a compiler is not allowed to do on its own. These are algorithm-level decisions, and they belong to the library.</p><p>That is also why the naive numbers above bounce around — 0.70, then 0.21, then 2.41 — while PureBLAS sits flat near 42 across all three sizes. The naive version is at the mercy of how each size happens to land in the cache; <code>n=512</code> is the worst because a power-of-two row length makes columns collide in the same cache sets. A blocked kernel is not at the mercy of anything: it decides the access pattern itself.</p><p>The rest of this page is about how those decisions get made without hardcoding them for one CPU.</p><h2 id="The-vector-abstraction:-one-kernel,-every-width" tabindex="-1">The vector abstraction: one kernel, every width <a class="header-anchor" href="#The-vector-abstraction:-one-kernel,-every-width" aria-label="Permalink to &quot;The vector abstraction: one kernel, every width {#The-vector-abstraction:-one-kernel,-every-width}&quot;">​</a></h2><p>Hot kernels are written with <a href="https://github.com/eschnett/SIMD.jl" target="_blank" rel="noreferrer">SIMD.jl</a> — <code>Vec{N,T}</code> plus <code>vload</code> / <code>vstore</code> / <code>muladd</code> / <code>vifelse</code> / <code>shufflevector</code> — not <code>_mm256_*</code>-style intrinsics. A kernel is written once, parameterized on the vector width <code>N = _vwidth(T)</code>, and the same source compiles to AVX-512 or AVX2 code depending on the host. Width detection also has a 16-byte fallback that suits SSE2 and NEON, but the benchmark fleet is x86-64 and ARM is untested. For example a Level-1 axpy inner step is just:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">V </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Vec{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_vwidth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(T), T}</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vstore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">muladd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(va, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(V, px </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(V, py </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o)), py </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o)</span></span></code></pre></div><p>There are no per-ISA kernel copies to maintain. Unit-stride dense inputs take this SIMD path, real and complex alike — the complex kernels have their own vectorized form, described below. Strided data, <code>ForwardDiff.Dual</code> and other element types fall to the generic scalar loop, which is slower but is what keeps the native API differentiable (see <a href="./design">Design</a>).</p><h2 id="Compile-time-hardware-detection" tabindex="-1">Compile-time hardware detection <a class="header-anchor" href="#Compile-time-hardware-detection" aria-label="Permalink to &quot;Compile-time hardware detection {#Compile-time-hardware-detection}&quot;">​</a></h2><p>The register width and cache geometry are detected once, at build or load time, and baked into const-folded constants in <code>cpuinfo.jl</code>. Detection uses <code>CpuId</code> (<code>simdbytes</code>, <code>cpuvendor</code>, <code>cpufeature</code>, <code>cachesize</code>, <code>cachelinesize</code>), <code>CPUSummary</code> (<code>cache_size</code>), and <code>HostCPUFeatures</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Constant</th><th style="text-align:right;">Meaning</th></tr></thead><tbody><tr><td style="text-align:right;"><code>_SIMD_BYTES</code>, <code>_vwidth(T)</code></td><td style="text-align:right;">vector width in bytes / lanes per <code>T</code></td></tr><tr><td style="text-align:right;"><code>_L1_BYTES</code>, <code>_L2_BYTES</code>, <code>_L3_BYTES</code></td><td style="text-align:right;">cache sizes (L3 is the total, not per-core share)</td></tr><tr><td style="text-align:right;"><code>_CACHELINE</code>, <code>_L1D_ASSOC</code>, <code>_L1_WAY_BYTES</code></td><td style="text-align:right;">line size, L1 associativity, way-stride</td></tr><tr><td style="text-align:right;"><code>_NVREG</code></td><td style="text-align:right;">vector register count (32 on AVX-512, 16 on AVX2)</td></tr><tr><td style="text-align:right;"><code>_CPU_VENDOR</code>, <code>_CPU_FAMILY</code>, <code>_INTEL_AVX2</code></td><td style="text-align:right;">vendor / family / feature bits</td></tr><tr><td style="text-align:right;"><code>_HW</code></td><td style="text-align:right;">a named tuple bundling the above for the <code>_at_*</code> derive helpers</td></tr></tbody></table><p>Because Julia compiles to the host, these are ordinary <code>const</code>s that fold away entirely: the generated kernel contains no runtime <code>CpuId</code> or <code>cpuid</code> call, which is what lets it survive <code>juliac --trim</code>. Each constant also accepts a <code>Preferences</code> override (<code>@load_preference</code>) for cross-compilation, pinning, or correcting a bad heuristic — <code>simd_bytes</code>, <code>l3_bytes</code>, <code>l1d_assoc</code> and so on.</p><h2 id="Deriving-tuning-from-hardware,-not-tabulating-it" tabindex="-1">Deriving tuning from hardware, not tabulating it <a class="header-anchor" href="#Deriving-tuning-from-hardware,-not-tabulating-it" aria-label="Permalink to &quot;Deriving tuning from hardware, not tabulating it {#Deriving-tuning-from-hardware,-not-tabulating-it}&quot;">​</a></h2><p>This is the core rule, and the main structural advantage over an ahead-of-time BLAS. Block sizes, base-case cutoffs, panel widths and unroll factors are all formulas over the detected constants, each keyed on a physical criterion — cache <em>residency</em> for block sizes, datapath <em>latency</em> for unroll, ISA for width granularity. The <code>_at_*</code> helpers in <code>cpuinfo.jl</code> are those formulas:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_at_gemm_nr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_lanes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T), _ILP_TARGET </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">÷</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _lanes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T))     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NR from the ILP target</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_at_gemm_kc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _l1_block</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_at_gemm_nr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T))                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># B micropanel ≤ ½·L1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_acc_cap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (hw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nvreg </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _lanes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># accumulators the reg file holds</span></span></code></pre></div><p>A bare literal like <code>_vwidth == 4 ? 48 : 64</code>, or <code>const _KC = 256</code>, is a bug: the tell is a number you cannot trace to a detected constant through a residency/latency formula. Why this is mandatory rather than stylistic: Julia compiles to the host at load time, so PureBLAS can <em>compute</em> the right sizes for the actual machine — including CPUs never benchmarked (a new laptop, a cloud box). A static C/Rust BLAS cannot; it ships hand-tuned per-µarch tables baked at <em>its</em> compile time. Hardcoding literals here throws away Julia&#39;s one real structural advantage.</p><p>A derived formula has to reproduce the measured-optimal values on the known fleet (Zen3, Zen4, Zen5) before it is trusted to extrapolate: derive, validate on the fleet, then ship. The triangular-solve fused-leaf cutoff is a good example — the size at which the hot <code>KC × NR</code> panel fills L1:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_GT_TRANSPOSE </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(_GT_MR, _L1_BYTES </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">÷</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (_GT_NR </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Float64))) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 128</span></span></code></pre></div><p>On AVX-512 that computes a different number from each machine&#39;s own L1 rather than reading a table. Off AVX-512 it is a literal 128, because a larger base was measured to regress <code>n=256</code> — the rule allows a literal when a derivation has been tried and falsified, provided the measurement is recorded next to it. Most exceptions in the codebase look like this one: not an untested guess, but a formula that lost to the numbers.</p><h2 id="Microarchitecture,-not-just-width" tabindex="-1">Microarchitecture, not just width <a class="header-anchor" href="#Microarchitecture,-not-just-width" aria-label="Permalink to &quot;Microarchitecture, not just width {#Microarchitecture,-not-just-width}&quot;">​</a></h2><p>Width and cache size do not distinguish microarchitectures with the same ISA (Haswell and Zen3 are both AVX2 / <code>_vwidth == 4</code>). For a µarch-dependent choice, key on the vendor and feature bits, not the width:</p><ul><li><p><code>_INTEL_AVX2</code> (<code>cpuvendor() === :Intel &amp;&amp; cpufeature(:AVX2) &amp;&amp; !cpufeature(:AVX512F)</code>) selects a latency split in the Cholesky base.</p></li><li><p><code>_double_pumped(hw)</code> (<code>hw.simd == 64 &amp;&amp; hw.vendor === :AMD &amp;&amp; hw.family == 0x19</code>) recognizes that early Zen4 executes 512-bit ops as two 256-bit halves, so <code>_datapath_bytes</code> — not <code>_SIMD_BYTES</code> — drives latency-bound unroll counts.</p></li></ul><p>These are still build-time detections (<code>req#7</code>): they const-fold, so no runtime branch survives.</p><h2 id="Tail-handling:-pack-over-mask" tabindex="-1">Tail handling: pack over mask <a class="header-anchor" href="#Tail-handling:-pack-over-mask" aria-label="Permalink to &quot;Tail handling: pack over mask {#Tail-handling:-pack-over-mask}&quot;">​</a></h2><p>Partial vectors at the edge of a tile can be handled by masked load/store (<code>vifelse</code>, masked stores) <em>or</em> by packing the operand into a padded buffer so the dense kernel runs unmasked. PureBLAS uses both but leans toward packing and padding where masking would cost throughput — for instance packing a triangular diagonal block dense-with-zeros so the ordinary microkernel multiplies it (the OpenBLAS scheme), or padding a ragged column stripe to the panel width. Masked <em>vector stores</em> on diagonal-straddling tiles were measured slower than dense-compute-to-scratch plus a scalar triangular copy-back, and direct (unpacked) reads must clamp to valid bounds to stay inside the allocation.</p><h2 id="Complex-numbers" tabindex="-1">Complex numbers <a class="header-anchor" href="#Complex-numbers" aria-label="Permalink to &quot;Complex numbers {#Complex-numbers}&quot;">​</a></h2><p>Complex kernels stay in the <strong>interleaved</strong> <code>[re, im, re, im, …]</code> domain and use <strong>swap-adjacent</strong> shuffles (<code>shufflevector</code>) for the <code>i·i = -1</code> cross terms, rather than deinterleaving real and imaginary parts across lanes. This keeps the data layout contiguous and avoids cross-lane permutations, and it is expressed entirely in portable SIMD.jl — no x86 shuffle intrinsics. The same <code>Vec{N,Complex{T}}</code>-free formulation covers <code>c</code> and <code>z</code>.</p><h2 id="Contrast-with-ahead-of-time-BLAS-OpenBLAS,-faer" tabindex="-1">Contrast with ahead-of-time BLAS (OpenBLAS, faer) <a class="header-anchor" href="#Contrast-with-ahead-of-time-BLAS-OpenBLAS,-faer" aria-label="Permalink to &quot;Contrast with ahead-of-time BLAS (OpenBLAS, faer) {#Contrast-with-ahead-of-time-BLAS-OpenBLAS,-faer}&quot;">​</a></h2><p>The <em>kernel</em> philosophy is shared with modern portable-SIMD libraries — faer, via <a href="https://github.com/sarah-quinones/pulp" target="_blank" rel="noreferrer">pulp</a>, writes one generic kernel over a <code>Simd</code> trait with register-blocked multiple accumulators, much as PureBLAS writes one kernel over <code>Vec{N,T}</code>. The deep difference is when specialization happens:</p><ul><li><p><strong>AOT (Rust/C):</strong> the library is compiled before it knows the target. To cover a range of CPUs it must carry several ISA variants and select one at runtime, and its block-size tuning is a static table baked at the library&#39;s compile time. It cannot re-derive tuning for a CPU it was never built or benchmarked for.</p></li><li><p><strong>JIT-to-host (PureBLAS):</strong> Julia specializes the kernel to the <em>one</em> machine at load time. There is no runtime ISA dispatch and no multi-versioning — and, crucially, tuning is <em>computed</em> from the detected cache/ISA of the actual host, so an unbenchmarked CPU still gets sized blocks.</p></li></ul><p>The practical consequence is that any faer-versus-PureBLAS performance gap on a given machine is a matter of library maturity and tuning, not of language: Julia&#39;s LLVM backend generates numerical code competitive with Rust&#39;s, and JIT-to-host is the more flexible substrate for hardware adaptation, not a handicap.</p><h2 id="Reproducibility-and-BLAS-compatibility" tabindex="-1">Reproducibility and BLAS compatibility <a class="header-anchor" href="#Reproducibility-and-BLAS-compatibility" aria-label="Permalink to &quot;Reproducibility and BLAS compatibility {#Reproducibility-and-BLAS-compatibility}&quot;">​</a></h2><p>Two numerical properties are worth separating, because the reference BLAS spec treats them very differently:</p><ul><li><p><strong>Overflow/underflow safety is mandated.</strong> The spec <em>defines</em> <code>nrm2</code> to return <code>√(Σxᵢ²)</code> without spurious overflow or underflow, so a faithful drop-in must use scaled accumulation (LAPACK <code>lassq</code>, which carries a running rescale). It is not optional, and it does cost some speed in that one kernel versus a naive square-and-sum.</p></li><li><p><strong>Bit-reproducibility is not in the spec.</strong> faer <em>elects</em> to guarantee it — identical results across alignment offsets on a machine — by rotating reduction accumulators (needed because faer aligns its loads, which would otherwise make the summation order alignment-dependent).</p></li></ul><p>PureBLAS is not <em>required</em> to be reproducible, but it is — <strong>by construction, and now locked by regression tests</strong> (<code>test/reproducibility_tests.jl</code>). Its kernels load from the base pointer with a fixed lane grouping (no alignment peeling) and run single-threaded, so two properties hold and are asserted: every operation is bit-identical run to run (same input, repeated call), and a reduction is bit-identical across memory alignments within a code path.</p><p>What is <em>not</em> guaranteed — for PureBLAS, and typically not for any BLAS — is that the SIMD fast path and the generic scalar fallback agree to the last bit: they sum in a different order, so <code>nrm2</code> of a plain <code>Vector</code> (SIMD path) can differ by a ULP or two from <code>nrm2</code> of an offset view (scalar path). That is the normal fast-path/fallback split, not a reproducibility bug. Cross-<em>machine</em> reproducibility is likewise not guaranteed (a different vector width builds a different reduction tree), and adding multithreading would require a fixed reduction tree to preserve run-to-run identity.</p>`,44)])])}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
