import{_ as t,o,c as a,ao as d}from"./chunks/framework.DUdA5YZf.js";const p=JSON.parse(`{"title":"Dual numbers: LAPACK (Phase 1 — triage and design; §3/§4's geqrf fix landed, see §7)","description":"","frontmatter":{},"headers":[],"relativePath":"dual_lp.md","filePath":"dual_lp.md","lastUpdated":null}`),r={name:"dual_lp.md"};function s(i,e,n,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[d('<h1 id="Dual-numbers:-LAPACK-Phase-1-—-triage-and-design;-§3/§4&#39;s-geqrf-fix-landed,-see-§7" tabindex="-1">Dual numbers: LAPACK (Phase 1 — triage and design; §3/§4&#39;s <code>geqrf</code> fix landed, see §7) <a class="header-anchor" href="#Dual-numbers:-LAPACK-Phase-1-—-triage-and-design;-§3/§4&#39;s-geqrf-fix-landed,-see-§7" aria-label="Permalink to &quot;Dual numbers: LAPACK (Phase 1 — triage and design; §3/§4&#39;s `geqrf` fix landed, see §7) {#Dual-numbers:-LAPACK-Phase-1-—-triage-and-design;-§3/§4&#39;s-geqrf-fix-landed,-see-§7}&quot;">​</a></h1><p>Companion to <a href="./dual"><code>dual.md</code></a> (BLAS-1), <a href="./dual_l2"><code>dual_l2.md</code></a> and <a href="./dual_l3"><code>dual_l3.md</code></a>.</p><p><strong>The headline is that most of this is already done</strong>, and that is a consequence of CLAUDE.md requirement #3 (one kernel generic over <code>T&lt;:Number</code>) rather than luck. A LAPACK routine is mostly a driver over BLAS-2/BLAS-3 calls; dual L2/L3 landed in <code>6109aeec</code>; so the factorizations inherited the speedup without a line of new code. Triage exists to find the exceptions.</p><h2 id="1.-Triage:-correctness" tabindex="-1">1. Triage: correctness <a class="header-anchor" href="#1.-Triage:-correctness" aria-label="Permalink to &quot;1. Triage: correctness {#1.-Triage:-correctness}&quot;">​</a></h2><p><code>bench/probes/dual_lapack_triage.jl</code>, n=32, <code>Dual{Nothing,Float64,1}</code>, compared against LinearAlgebra&#39;s generic factorization on <strong>both value and partial</strong> — a routine that is right on the value alone has lost the derivative, which is the entire point.</p><p>| routine | status | max |Δvalue| | max |Δpartial| | |—|—|—|—| | <code>potrf</code> | OK | 0.0 | 0.0 | | <code>getrf</code> | OK | 0.0 | 2.2e-16 | | <code>geqrf</code> | OK | 3.6e-15 | 2.8e-14 | | <code>getrs</code> | OK | 6.9e-18 | 2.8e-17 | | <code>potrs</code> | OK | 1.7e-17 | 1.9e-17 | | <code>trtrs</code> | OK | 1.4e-17 | 5.6e-17 | | <code>trtri</code> | OK | 1.7e-18 | 1.1e-18 | | <code>potri</code> | OK | 1.4e-17 | 5.2e-18 | | <code>sytrf</code> | runs | no generic oracle compared | | <code>pstrf</code> | OK | — (see the correction below) | | <code>gesvd</code> (values) | OK | returns <code>Vector{Dual}</code> | | <code>_syev!</code> | OK <strong>since <code>18fc0e20</code>+</strong> | eigenvalues 1.7e-13, <strong>derivatives 1.7e-12</strong> vs <code>ForwardDiff.derivative</code> | | <code>gesvd</code> (vectors) | <strong>ArgumentError</strong> | gated to Float64/complex by design (<code>svd.jl:1656</code>) | | <code>geqp3</code> | <strong>MethodError</strong> | genuinely gated: both methods are <code>where {T &lt;: BlasFloat}</code> |</p><h3 id="Correction-2026-09-13-—-three-of-the-four-&quot;gaps&quot;-were-the-PROBE,-not-the-code" tabindex="-1">Correction (2026-09-13) — three of the four &quot;gaps&quot; were the PROBE, not the code <a class="header-anchor" href="#Correction-2026-09-13-—-three-of-the-four-&quot;gaps&quot;-were-the-PROBE,-not-the-code" aria-label="Permalink to &quot;Correction (2026-09-13) — three of the four &quot;gaps&quot; were the PROBE, not the code {#Correction-2026-09-13-—-three-of-the-four-&quot;gaps&quot;-were-the-PROBE,-not-the-code}&quot;">​</a></h3><p>An earlier revision of this page listed <code>_syev!</code>, <code>pstrf</code> and <code>geqp3</code> as <code>MethodError</code>s. Two of those were my triage probe calling the wrong signature, and I published them as missing capability:</p><ul><li><p><strong><code>pstrf!</code></strong> is <code>(A, piv, tol::Real; uplo)</code> and is declared <code>where {T}</code> — <strong>no type restriction at all</strong>. The probe omitted <code>tol</code>. It has worked on duals the whole time.</p></li><li><p><strong><code>_syev!</code></strong> is <code>(jobz::Char, uplo::Char, A)</code>, not <code>(A; uplo)</code>. The probe passed a keyword and one positional. A <code>MethodError</code> was the correct answer to a wrong call.</p></li><li><p><strong><code>geqp3!</code></strong> is the only real gate, and it is a type gate: <code>where {T &lt;: BlasFloat}</code> on both methods.</p></li></ul><p>The lesson is that a <code>MethodError</code> says &quot;no method for THESE arguments&quot;, not &quot;this routine cannot do this&quot; — and the difference is one <code>methods()</code> call away. Three probe bugs in one triage is a bad rate; read the signature before reporting a capability gap.</p><p><code>_syev!</code> did turn out to have a real, shallow defect underneath the bad call — see §8.</p><p><code>getrf</code>&#39;s partial at 2.2e-16 is the one to notice: <strong>pivoting already selects on |value|</strong>, because <code>_l1v</code> (<code>core.jl</code>) exists precisely so <code>iamax</code> does not break value ties on the derivative. Had it compared <code>_l1</code> on duals, ForwardDiff&#39;s lexicographic <code>isless</code> would have made the pivot sequence depend on the seed direction.</p><h2 id="2.-Triage:-performance" tabindex="-1">2. Triage: performance <a class="header-anchor" href="#2.-Triage:-performance" aria-label="Permalink to &quot;2. Triage: performance {#2.-Triage:-performance}&quot;">​</a></h2><p><code>bench/probes/dual_lapack_perf.jl</code>. Reference = LinearAlgebra&#39;s generic factorization (what a ForwardDiff user gets today without PureBLAS), Chairmarks median, fresh operand per sample.</p><table tabindex="0"><thead><tr><th style="text-align:right;">routine</th><th style="text-align:right;">n=64</th><th style="text-align:right;">n=256</th><th style="text-align:right;">reads as</th></tr></thead><tbody><tr><td style="text-align:right;"><code>potri</code></td><td style="text-align:right;">8.42</td><td style="text-align:right;"><strong>21.03</strong></td><td style="text-align:right;">reaches the blocked path</td></tr><tr><td style="text-align:right;"><code>trtri</code></td><td style="text-align:right;">9.79</td><td style="text-align:right;"><strong>20.84</strong></td><td style="text-align:right;">reaches the blocked path</td></tr><tr><td style="text-align:right;"><code>potrf</code></td><td style="text-align:right;">2.32</td><td style="text-align:right;">7.24</td><td style="text-align:right;">reaches the blocked path</td></tr><tr><td style="text-align:right;"><code>getrf</code></td><td style="text-align:right;">1.98</td><td style="text-align:right;">5.67</td><td style="text-align:right;">reaches the blocked path</td></tr><tr><td style="text-align:right;"><code>geqrf</code></td><td style="text-align:right;">0.57</td><td style="text-align:right;"><strong>0.45</strong></td><td style="text-align:right;">does NOT</td></tr></tbody></table><p>The direction of the trend is the diagnosis. Four routines get <em>better</em> with n — the signature of a blocked BLAS-3 trailing update amortising. <code>geqrf</code> gets <em>worse</em> (0.57 → 0.49 → 0.45 at 64/128/256).</p><p><strong>Control that isolates it:</strong> the same PB <code>geqrf!</code> on <code>Float64</code> at n=256 measures <strong>2.03×</strong> generic. So this is not a QR problem, it is a dispatch problem. (An earlier probe allocated <code>tau</code> inside the timed core; re-measured with <code>tau</code> in setup, the numbers are unchanged — that was not the cause.)</p><h2 id="3.-Root-cause-for-geqrf" tabindex="-1">3. Root cause for <code>geqrf</code> <a class="header-anchor" href="#3.-Root-cause-for-geqrf" aria-label="Permalink to &quot;3. Root cause for `geqrf` {#3.-Root-cause-for-geqrf}&quot;">​</a></h2><p><code>qr.jl:467</code>, the method duals dispatch to:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> geqrf!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AbstractMatrix{T}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, tau</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">AbstractVector{T}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; nb</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">where</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {T </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Real</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    qr_unblocked!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">view</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">m, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">view</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(tau, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">k))    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># nb accepted and DISCARDED</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>It runs a fully unblocked BLAS-2 reduction for every size. <code>Float64</code> (<code>:480</code>) and <code>BlasComplex</code> (<code>:340</code>) each get a blocked compact-WY driver whose trailing update is <code>gemm!</code>; <code>T&lt;:Real</code> gets nothing. The <code>nb</code> keyword is accepted and thrown away, which is how it reads as intentional and is not.</p><p>Not a bug, for the record: <code>_qr_nb</code>&#39;s <code>sizeof(Float64)</code> is correct — it is only called from the Float64 path, and complex has its own <code>_zqr_nb(T, m, n)</code> over <code>sizeof(T)</code> (<code>:325</code>).</p><h2 id="4.-Two-designs-for-dual-geqrf,-and-they-are-not-equivalent" tabindex="-1">4. Two designs for dual <code>geqrf</code>, and they are not equivalent <a class="header-anchor" href="#4.-Two-designs-for-dual-geqrf,-and-they-are-not-equivalent" aria-label="Permalink to &quot;4. Two designs for dual `geqrf`, and they are not equivalent {#4.-Two-designs-for-dual-geqrf,-and-they-are-not-equivalent}&quot;">​</a></h2><p><strong>(a) Generalise the blocked driver over <code>T</code>.</strong> Port <code>geqrf!(::AbstractMatrix{Float64}, …)</code> to <code>T&lt;:Real</code>: <code>1.0</code>/<code>0.0</code> → <code>one(T)</code>/<code>zero(T)</code>, a <code>T</code>-typed workspace pool beside <code>_qr_ws</code>, and a decision about the <code>useskinny</code> µarch gate (which assumes 8-byte elements: <code>16 * pb * mp &lt;= _L2_BYTES</code> is an L2-residency criterion in <em>bytes of Float64</em>, so it needs <code>sizeof(T)</code>, req#8). Mechanical, and it lifts every <code>T&lt;:Real</code> — not just duals.</p><p><strong>(b) Planar, mirroring <code>dual_l3.md</code>.</strong> For <code>A = A_v + ε A_p</code>, QR has a closed-form derivative: factor the value plane with the <strong>existing fast Float64 driver</strong>, then recover <code>R_p</code> and the <code>Q_p</code> action from real-only products. The differential relations are <code>Qᵀ A_p = Ṙ + S R</code> with <code>S = Qᵀ Q_p</code> skew-symmetric, so <code>S</code> is determined by the strictly-lower part of <code>Qᵀ A_p R⁻¹</code> and <code>Ṙ = Qᵀ A_p − S R</code> — i.e. one real <code>gemm</code>, one real <code>trsm</code>, and a skew completion. That rides the 2.03× Float64 path instead of re-deriving it, and mirrors the L3 result where planar dual <strong>beat</strong> the complex twin (<code>dsyrk1</code> 8.39 vs its complex sibling).</p><p>Recommendation: <strong>(b) for the dual entry, (a) anyway for <code>T&lt;:Real</code> generally</strong> — (a) is the honest fix for every other real element type (<code>Float32</code> already has its own path; <code>BigFloat</code>, <code>Measurement</code>, etc. do not), and (b) is where the performance is. They are complementary, not alternatives.</p><p>Open question for sparring: (b) needs <code>Q</code> applied, not formed. <code>ormqr</code>/<code>larfb</code> already exist on the Float64 path — can <code>S</code> be obtained without materialising <code>Q</code>, or does the skew completion force it?</p><h2 id="5.-The-remaining-gaps" tabindex="-1">5. The remaining gaps <a class="header-anchor" href="#5.-The-remaining-gaps" aria-label="Permalink to &quot;5. The remaining gaps {#5.-The-remaining-gaps}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">gap</th><th style="text-align:right;">kind</th><th style="text-align:right;">note</th></tr></thead><tbody><tr><td style="text-align:right;"><code>geqrf</code> blocked path for <code>T&lt;:Real</code></td><td style="text-align:right;">perf</td><td style="text-align:right;"><strong>DONE</strong> (§7): 0.57/0.49/0.45 → 1.67/2.56/2.75 at n=64/128/256</td></tr><tr><td style="text-align:right;"><code>_syev!</code> on <code>Dual</code></td><td style="text-align:right;">dispatch</td><td style="text-align:right;"><strong>DONE</strong> (§8): the gap was <code>_trdws</code>, not the driver</td></tr><tr><td style="text-align:right;"><code>pstrf</code> on <code>Dual</code></td><td style="text-align:right;">—</td><td style="text-align:right;"><strong>NOT A GAP</strong> — my probe omitted <code>tol</code>; it always worked (§1 correction)</td></tr><tr><td style="text-align:right;"><code>geqp3</code> on <code>Dual</code></td><td style="text-align:right;">dispatch</td><td style="text-align:right;"><strong>the one genuine gate</strong>: both methods are <code>where {T &lt;: BlasFloat}</code></td></tr><tr><td style="text-align:right;"><code>gesvd</code> singular <strong>vectors</strong></td><td style="text-align:right;">feature</td><td style="text-align:right;">needs generic <code>orgbr</code> + vector-carrying <code>bdsqr</code>; <strong>values already work</strong></td></tr><tr><td style="text-align:right;">stopping-rule audit</td><td style="text-align:right;"><strong>correctness</strong></td><td style="text-align:right;">clean for everything that dispatches, <code>pstrf</code> and <code>_syev!</code> included; see below</td></tr></tbody></table><h2 id="6.-The-audit-nothing-else-catches" tabindex="-1">6. The audit nothing else catches <a class="header-anchor" href="#6.-The-audit-nothing-else-catches" aria-label="Permalink to &quot;6. The audit nothing else catches {#6.-The-audit-nothing-else-catches}&quot;">​</a></h2><p><code>&lt;</code> on a <code>Dual</code> compares lexicographically on (value, partial). Every routine that <strong>branches on a comparison</strong> can therefore take a different branch depending on the seed direction, producing a factorization that is correct-looking and whose derivative is wrong — and no ordinary correctness test detects it, because any single seed is self-consistent.</p><p><strong>Built and run first, per that reasoning</strong>: <code>bench/probes/dual_seed_independence.jl</code> holds the value plane fixed, varies only the partial plane, and asserts the value part of the output is bit-identical.</p><p>Result — <strong>clean for everything that currently dispatches</strong>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">routine</th><th style="text-align:right;"></th></tr></thead><tbody><tr><td style="text-align:right;"><code>potrf</code> <code>getrf</code> <code>geqrf</code> <code>trtri</code> <code>potri</code> <code>sytrf</code></td><td style="text-align:right;">seed-independent</td></tr><tr><td style="text-align:right;"><code>getrf</code> <strong>pivot sequence</strong> (<code>ipiv</code>)</td><td style="text-align:right;">bit-identical across seeds</td></tr><tr><td style="text-align:right;"><strong><code>pstrf</code></strong> — rank, pivots AND values</td><td style="text-align:right;">bit-identical across seeds (<code>dual_seed_pstrf.jl</code>)</td></tr><tr><td style="text-align:right;"><strong><code>gesvd</code></strong> singular values</td><td style="text-align:right;">bit-identical across seeds</td></tr><tr><td style="text-align:right;"><strong><code>_syev!</code></strong> eigenvalues</td><td style="text-align:right;">bit-identical across seeds (<code>syev_dual_verify.jl</code>)</td></tr><tr><td style="text-align:right;"><code>geqp3</code></td><td style="text-align:right;">still untestable — the one genuine <code>MethodError</code></td></tr></tbody></table><p>An earlier revision said <code>pstrf</code> &quot;could not be tested&quot; and framed this audit as a gate on future work. That was wrong, and wrong in the risky direction: <code>pstrf</code> terminates on a <strong>rank criterion</strong> and had been dispatching on duals all along, so a seed-dependent rank would have been a LIVE correctness bug in shipped code, not a hypothetical one. Tested now — rank 48 vs 48, pivots and values identical under two unrelated seed directions.</p><p><code>_syev!</code> matters for a second reason: it is the first <strong>iterative</strong> routine to reach dual dispatch. Everything else audited here is a direct factorization with at most a pivot comparison, whereas an eigensolver runs to a convergence test — and <code>&lt;</code> on a <code>Dual</code> is lexicographic on (value, partial), so it could converge at a different iteration count per seed, giving eigenvalues correct to tolerance but <em>different</em> between seeds. It does not; the value plane is bit-identical.</p><p>Still owed: <code>geqp3</code> when it accepts duals (it pivots on column norms, so it can change the column ORDER), and an iteration-count witness rather than an output comparison — a convergence test that exits one step early for one seed is invisible in the output when both results are within tolerance.</p><h2 id="7.-Result:-a-shipped,-b-measured-and-declined-2026-09-13,-branch-geqrf-real-blocked" tabindex="-1">7. Result: (a) shipped, (b) measured and declined (2026-09-13, branch <code>geqrf-real-blocked</code>) <a class="header-anchor" href="#7.-Result:-a-shipped,-b-measured-and-declined-2026-09-13,-branch-geqrf-real-blocked" aria-label="Permalink to &quot;7. Result: (a) shipped, (b) measured and declined (2026-09-13, branch `geqrf-real-blocked`) {#7.-Result:-a-shipped,-b-measured-and-declined-2026-09-13,-branch-geqrf-real-blocked}&quot;">​</a></h2><p><strong>(a)</strong> The Float64 blocked driver is now the one <code>geqrf!</code> for every <code>T&lt;:Real</code> (<code>qr.jl</code>): <code>one(T)</code>/<code>zero(T)</code>, the panel width from <code>_qr_nb(T, m, n)</code> — the same L2-residency ramp with the byte term <code>sizeof(T)</code>, so a 16-byte <code>Dual</code> spills at half the m·n a Float64 does — the skinny unpacked <code>W = Vᵀ·C</code> gated on <code>T &lt;: BlasReal</code> with its criterion spelled in bytes (<code>2·pb·mp·sizeof(T) ≤ L2</code>, which is the old <code>16·pb·mp ≤ L2</code> for Float64), and a per-call workspace for the non-Float64 reals beside the owned Float64 pool. On a <code>Dual</code> the trailing <code>syrk!</code>/<code>trmm!</code>/<code>gemm!</code> are the planar compositions of <code>dual_l3.md</code>, so nothing new was written for the trailing update; the panel is the generic dgeqr2 (<code>_larfg!</code> + <code>_house_left!</code>, scalar on a Dual). Float32 and BigFloat were on the unblocked path too and are lifted by the same change.</p><p>galen (Zen3, core 6, freq-locked), <code>bench/probes/dual_geqrf_check.jl</code> — the probe §2&#39;s numbers came from, Chairmarks median of 24, fresh operand per sample, PB / LinearAlgebra generic:</p><table tabindex="0"><thead><tr><th style="text-align:right;">n</th><th style="text-align:right;">before</th><th style="text-align:right;">after</th></tr></thead><tbody><tr><td style="text-align:right;">64</td><td style="text-align:right;">0.57</td><td style="text-align:right;"><strong>1.67</strong></td></tr><tr><td style="text-align:right;">128</td><td style="text-align:right;">0.49</td><td style="text-align:right;"><strong>2.56</strong></td></tr><tr><td style="text-align:right;">256</td><td style="text-align:right;">0.45</td><td style="text-align:right;"><strong>2.75</strong></td></tr></tbody></table><p>The trend now has the sign of the other four factorizations (§2&#39;s table re-run: potrf 2.00/5.46, getrf 1.93/5.15, geqrf 1.65/2.73, trtri 9.40/17.9, potri 7.44/17.0 at n=64/256). The Float64 control is unchanged at 2.04×. Seed independence (<code>dual_seed_independence.jl</code>, N=48 — above <code>_QR_UNBLK_MAX</code>, so it runs the new blocked path): <code>geqrf</code> still seed-independent, as are the other five and getrf&#39;s pivot sequence. Float64 and complex byte-identity (<code>bench/probes/dual_qr_native.jl</code>, 32 instantiations: the Float64 driver body on <code>Matrix</code> and on views, the SIMD panel and its five helpers, <code>_qr_nb</code>, <code>_qr_ws</code>, <code>geqp3</code> as a <code>_qr_nb</code> consumer, the complex driver/panel/reflector/apply for ComplexF64 and ComplexF32, orgqr/ungqr/ormqr controls): <strong>19 identical, 12 same instruction count + opcode multiset, 0 different</strong> — exactly the classification the unedited tree gives against its own baseline in a second process (the 12 are the large drivers whose frame layout drifts per process), and the only DIFFERENT is Float32&#39;s <code>geqrf</code> body (123 → 1469 instructions), which is the change.</p><p><strong>(b), answered.</strong> Can <code>S = Qᵀ Q̇</code> be obtained without forming <code>Q</code>? Yes: <code>M = Qᵀ A_p</code> is one implicit application (<code>ormqr</code>, reflectors only), <code>S</code> is the strictly-lower part of <code>M R⁻¹</code> (one <code>trsm</code>, skew completion), <code>Ṙ = M − S R</code> (one <code>trmm</code>). No <code>Q</code> is ever materialised. <strong>But <code>S</code> is not the deliverable.</strong> <code>geqrf!</code>&#39;s output contract is the stored reflectors and <code>τ</code> — on a <code>Dual</code>, their partials <code>v̇_k</code>, <code>τ̇_k</code> — because that is what <code>ormqr</code>/<code>orgqr</code>/<code>\\</code>/LinearAlgebra&#39;s <code>QRCompactWY</code> read back. <code>v̇_k</code> depends on the derivative of column <code>k</code> of the <em>partially reduced</em> matrix <code>H_{k-1}⋯H_1 A</code>, i.e. on <code>Ḣ_1 … Ḣ_{k-1}</code>; recovering the <code>v̇_k, τ̇_k</code> from <code>S</code> (or from <code>Q̇ = QS</code>) means peeling the reflector sequence in order — which is the forward recursion (a) already performs. So (b) computes <code>Ṙ</code> and <code>Q̇</code> and then still has to run (a) for the reflector partials; it cannot replace it.</p><p>And it would not be faster even if that recovery were free. <code>bench/probes/dual_qr_b_accounting.jl</code> (galen, same regime, median of 24) measures (b)&#39;s optimistic bound — the value-plane Float64 QR plus one <code>ormqr</code> plus one <code>trsm</code> plus one <code>trmm</code>, the recovery <em>excluded</em> — against (a):</p><table tabindex="0"><thead><tr><th style="text-align:right;">n</th><th style="text-align:right;">(a) dual geqrf</th><th style="text-align:right;">= panel + trailing</th><th style="text-align:right;">(b) bound: F64 qr + ormqr + trsm + trmm</th><th style="text-align:right;">(a)/(b)</th></tr></thead><tbody><tr><td style="text-align:right;">128</td><td style="text-align:right;">539 µs</td><td style="text-align:right;">204 (38 %) + 335 (62 %)</td><td style="text-align:right;">113 + 324 + 54 + 55 = 546 µs</td><td style="text-align:right;">0.99</td></tr><tr><td style="text-align:right;">256</td><td style="text-align:right;">3626 µs</td><td style="text-align:right;">1615 (45 %) + 2011 (55 %)</td><td style="text-align:right;">689 + 2621 + 440 + 386 = 4136 µs</td><td style="text-align:right;"><strong>0.88</strong></td></tr></tbody></table><p>The <code>ormqr</code> term dominates: PB&#39;s <code>ormqr!</code> is the unblocked dorm2r, and applying <code>n</code> reflectors to an <code>n×n</code> <code>A_p</code> is ~2× the flops of the QR itself (4mn²−2n³ vs 2mn²−⅔n³ for square), so even a blocked larfb-style <code>ormqr</code> at the QR&#39;s own efficiency (~1.4 ms at 256) would put the bound at ~2.9 ms — 1.24× (a) at best, before paying the reflector-partial recovery, which is (a) again. Declined.</p><p><strong>What the accounting does say is where the next lever is:</strong> the panel — the scalar generic dgeqr2 on a Dual — is 45 % of (a) at n=256 and the trailing update is already planar. A pair-tagged panel (the dual BLAS-1 dot/axpy kernels of <code>dual.md</code> behind <code>_house_left!</code>, which today gates its SIMD arm on <code>T &lt;: BlasReal</code>) is the honest follow-on, worth up to ~1.8× on top of the 2.75 if the panel went to zero. Not done here.</p><h2 id="8.-_syev!:-the-gap-was-the-WORKSPACE,-not-the-driver-2026-09-13" tabindex="-1">8. <code>_syev!</code>: the gap was the WORKSPACE, not the driver (2026-09-13) <a class="header-anchor" href="#8.-_syev!:-the-gap-was-the-WORKSPACE,-not-the-driver-2026-09-13" aria-label="Permalink to &quot;8. `_syev!`: the gap was the WORKSPACE, not the driver (2026-09-13) {#8.-_syev!:-the-gap-was-the-WORKSPACE,-not-the-driver-2026-09-13}&quot;">​</a></h2><p>Once called correctly, <code>_syev!</code> still failed — but not on its own signature:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>MethodError: no method matching _trdws(::Type{ForwardDiff.Dual{Nothing, Float64, 1}})</span></span></code></pre></div><p><code>_sytrd_lower!</code> and <code>_syev!</code> are both <code>where {T &lt;: Real}</code> and generic throughout. What was not generic was the scratch: <code>_trdws</code> had exactly four methods, one per <code>BlasFloat</code>, each returning a const owned pool (<code>_TRDWS_F64</code> …) — the GKH ownership pattern. An open type set cannot have a const pool, so a <code>Dual</code> found no method at all.</p><p><strong>This is the same defect <code>_qr_ws</code> had</strong>, fixed the same way: a generic <code>_trdws(::Type{T}) = _TRDWork{T, real(T)}()</code> allocating per call. The four consts still win by specificity, so every <code>BlasFloat</code> path is untouched; only types that have no pool pay an allocation, against an O(n³) reduction.</p><p>That two independent routines had the identical hole suggests the class is worth sweeping rather than discovering one <code>MethodError</code> at a time: <strong>every const-per-type owned workspace is a candidate</strong>.</p><p>Verified after the fix (n=32, <code>Dual{Nothing,Float64,1}</code>), and note the second line is the one that matters — a factorization can be right on the value and have silently lost the derivative:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;"></th></tr></thead><tbody><tr><td style="text-align:right;">eigenvalues vs <code>eigvals</code></td><td style="text-align:right;">1.7e-13</td></tr><tr><td style="text-align:right;"><strong>derivatives vs <code>ForwardDiff.derivative</code></strong></td><td style="text-align:right;"><strong>1.7e-12</strong> (against a derivative magnitude of 2.14, so not vacuous)</td></tr><tr><td style="text-align:right;">seed-independent</td><td style="text-align:right;">yes — and <code>_syev!</code> is the first ITERATIVE routine to reach dual dispatch, so its convergence test was the real risk</td></tr></tbody></table><h3 id="_sytrd_nb-was-mis-sizing-the-panel,-silently" tabindex="-1"><code>_sytrd_nb</code> was mis-sizing the panel, silently <a class="header-anchor" href="#_sytrd_nb-was-mis-sizing-the-panel,-silently" aria-label="Permalink to &quot;`_sytrd_nb` was mis-sizing the panel, silently {#_sytrd_nb-was-mis-sizing-the-panel,-silently}&quot;">​</a></h3><p><code>_sytrd_nb(n) = _qr_nb(n, n)</code> used the Float64 two-arg form, so a 16-byte <code>Dual</code> was blocked as if it were 8 — a req#8 violation that produces no error, just a wrong panel width. Now <code>_sytrd_nb(::Type{T}, n) = _qr_nb(T, n, n)</code>, with the 2-arg form kept for existing callers.</p><p><strong>The complex <code>_hetrd!</code> call site is deliberately NOT typed — and the cost of typing it was measured, not guessed.</strong> The width is <code>clamp(8 · cld(m·n·sizeof(T), L2), 8, 32)</code>, a coarse step function taking only 8/16/24/32, so doubling <code>sizeof(T)</code> moves the bucket boundaries rather than every value. Swept n=32:16:1200 on Zen4: the two sizings <strong>agree at 51 of 74 sizes and differ at 23 (31%)</strong>, the split opening at <strong>n=272</strong>, where Float64-sized gives 8 and ComplexF64-sized gives <strong>16 — a doubled panel</strong>. So typing it changes shipped complex tridiagonalization (<code>hetrd</code> → <code>zheev</code>/<code>zheevN</code>/<code>hegvd</code>) across about a third of the size range, and <code>zheev</code>/<code>zheevN</code> publish at 1.176/1.072 against the current blocking.</p><p>The residency criterion arguably <em>does</em> want <code>sizeof(T)</code> there too — this is a real req#8 debt, not a false alarm. But it is a <strong>measured</strong> change needing a CLP sweep, not a free one taken as a side effect of a dual-number fix, and the sweep now has its range: the cells that can move are the 23 sizes from n=272 up where the buckets disagree. Deferred with its cost known rather than silently applied.</p><h2 id="9.-What-is-actually-left" tabindex="-1">9. What is actually left <a class="header-anchor" href="#9.-What-is-actually-left" aria-label="Permalink to &quot;9. What is actually left {#9.-What-is-actually-left}&quot;">​</a></h2><table tabindex="0"><thead><tr><th style="text-align:right;">gap</th><th style="text-align:right;">kind</th></tr></thead><tbody><tr><td style="text-align:right;"><code>geqp3</code> on <code>Dual</code></td><td style="text-align:right;">the one genuine type gate (<code>where {T &lt;: BlasFloat}</code>)</td></tr><tr><td style="text-align:right;"><code>gesvd</code> singular <strong>vectors</strong></td><td style="text-align:right;">deliberate; needs generic <code>orgbr</code> + vector-carrying <code>bdsqr</code>. Values work</td></tr><tr><td style="text-align:right;"><code>geqrf</code> scalar panel</td><td style="text-align:right;">perf, not correctness: the generic panel is 38%/45% of dual <code>geqrf</code> at n=128/256 while the trailing update is already planar. Route <code>_house_left!</code>&#39;s dot/axpy through the pair-tagged BLAS-1 kernels</td></tr></tbody></table>',61)])])}const u=t(r,[["render",s]]);export{p as __pageData,u as default};
