import{_ as t,o as s,c as a,ao as i}from"./chunks/framework.BSzmyLFO.js";const g=JSON.parse('{"title":"The Tuning-Constant System","description":"","frontmatter":{},"headers":[],"relativePath":"tuning.md","filePath":"tuning.md","lastUpdated":null}'),o={name:"tuning.md"};function n(r,e,d,l,c,h){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="The-Tuning-Constant-System" tabindex="-1">The Tuning-Constant System <a class="header-anchor" href="#The-Tuning-Constant-System" aria-label="Permalink to &quot;The Tuning-Constant System {#The-Tuning-Constant-System}&quot;">​</a></h1><p>Every fast BLAS is full of magic numbers: block sizes, panel widths, unroll factors, algorithm-switch thresholds. This page explains how PureBLAS handles those numbers — the <strong>PDM ladder</strong> — and gives you the vocabulary and the workflow to read, audit, and add one.</p><p>It is written for a developer new to the codebase. After reading it you should be able to look at a constant like <code>_CPOTRF_NBMAX = 64 + 16·W</code> and know <em>why</em> it has that shape, and know what to do when you are tempted to write <code>const _MY_BLOCK = 256</code>.</p><h2 id="1.-Why-tuning-constants-exist,-and-the-one-rule" tabindex="-1">1. Why tuning constants exist, and the one rule <a class="header-anchor" href="#1.-Why-tuning-constants-exist,-and-the-one-rule" aria-label="Permalink to &quot;1. Why tuning constants exist, and the one rule {#1.-Why-tuning-constants-exist,-and-the-one-rule}&quot;">​</a></h2><p>Dense linear algebra performance is mostly about fitting the right amount of data in the right level of the memory hierarchy and keeping enough independent FMA chains in flight. Both depend on the machine: cache sizes, SIMD width, register count, microarchitecture. So every real BLAS is parameterized by dozens of machine-dependent numbers.</p><p>Static C/Rust BLAS libraries (OpenBLAS, BLIS, AOCL) solve this with <strong>hand-tuned per-µarch tables</strong> baked in at <em>their</em> compile time. That works for CPUs their maintainers benchmarked and silently mis-sizes on everything else.</p><p>PureBLAS is Julia. Julia JITs to the host at load time, which means PureBLAS can <strong>compute</strong> the right sizes for the <em>actual</em> machine it is running on — including CPUs nobody ever benchmarked (a new laptop, a cloud box). This is the project&#39;s one structural advantage over static BLAS, and hardcoded literals throw it away. Hence the rule (<code>CLAUDE.md</code> requirements #7, #8, #8b):</p><blockquote><p><strong>The PDM ladder (Pin → Derive → Measure).</strong> Every machine-dependent constant is a <em>self-tuning constant</em>. It resolves in exactly this order:</p><ul><li><p><strong>P — Pin:</strong> <code>@load_preference(&quot;name&quot;, &lt;default&gt;)</code>. A set Preference always wins (calibration, cross-compilation, and the trim/<code>.so</code> build — which <em>must</em> pin any Measure-tier knob, because a runtime benchmark is not trim-safe).</p></li><li><p><strong>D — Derive:</strong> if the optimum is <strong>physically predictable from a detected const</strong> (cache residency, SIMD width, register count), the default is a <strong>formula</strong> over <code>_L1_BYTES</code>/<code>_L2_BYTES</code>/<code>_L3_BYTES</code>, <code>_vwidth</code>, <code>_NVREG</code>, … Zero runtime cost, const-folds (trim-safe), adapts to unseen machines.</p></li><li><p><strong>M — Measure:</strong> if the optimum is <strong>not</strong> predictable from detected consts — it depends on port balance, the prefetcher, or write-stream behavior, and can <em>invert sign</em> across microarchitectures — the default is an on-host auto-tune: a <code>Base.OncePerProcess</code> that benchmarks a <strong>formula-bounded candidate set</strong> on first use. See <code>_ger_np</code> below.</p></li></ul></blockquote><p>The decision rule between D and M <strong>is</strong> the rule:</p><blockquote><p><em>&quot;Is the optimum physically predictable from a detected const?&quot;</em> <strong>Yes ⇒ Derive. No ⇒ Measure. There is no third option.</strong></p></blockquote><p>A bare literal (<code>const _KC = 256</code>) or a datapath-gated literal (<code>_double_pumped(_HW) ? 8 : 4</code>) is not a valid answer — it is a Measure-tier knob that has not been converted yet: correct only on the machines someone benchmarked, wrong on the next one. A literal is permitted only as a documented <strong>exception</strong> — either a proven machine-<em>independent</em> invariant, or a validated literal whose derivation was tried and falsified — and it must say so in its comment (Section 4 catalogs these). The test suite enforces this: <code>test/req8_lint_tests.jl</code> fails on any <em>new</em> un-derived tuning literal that lacks a <code># req8-ok:</code> justification.</p><h3 id="Where-the-detected-consts-come-from" tabindex="-1">Where the detected consts come from <a class="header-anchor" href="#Where-the-detected-consts-come-from" aria-label="Permalink to &quot;Where the detected consts come from {#Where-the-detected-consts-come-from}&quot;">​</a></h3><p>The raw material for every Derive formula lives in <code>src/cpuinfo.jl</code>. The build machine is probed <strong>once, at package load/precompile time</strong>, via <code>CpuId.jl</code> / <code>HostCPUFeatures</code> / <code>CPUSummary</code>, and the results are baked into plain <code>const</code>s. This satisfies the trim-safety requirement (no <code>cpuid</code> ccall at runtime — the consts fold into the generated code) and every one is overridable through <code>Preferences</code> (for cross-compiling, pinning a <code>.so</code> build, or correcting a detection heuristic).</p><table tabindex="0"><thead><tr><th style="text-align:right;">Const</th><th style="text-align:right;">What it is</th><th style="text-align:right;">Source / fallback</th></tr></thead><tbody><tr><td style="text-align:right;"><code>_SIMD_BYTES</code></td><td style="text-align:right;">Widest SIMD register, in bytes (64 = AVX-512, 32 = AVX2, 16 = NEON/SSE2)</td><td style="text-align:right;"><code>CpuId.simdbytes()</code>; pref <code>&quot;simd_bytes&quot;</code>; fallback 16</td></tr><tr><td style="text-align:right;"><code>_vwidth(T)</code></td><td style="text-align:right;">SIMD lane count for element type <code>T</code>: <code>_SIMD_BYTES ÷ sizeof(T)</code> (the ubiquitous <strong>W</strong>)</td><td style="text-align:right;">derived from <code>_SIMD_BYTES</code></td></tr><tr><td style="text-align:right;"><code>_L1_BYTES</code></td><td style="text-align:right;">L1 data cache, bytes</td><td style="text-align:right;"><code>CPUSummary.cache_size(Val(1))</code>; fallback 32 KiB</td></tr><tr><td style="text-align:right;"><code>_L2_BYTES</code></td><td style="text-align:right;">L2 cache, bytes</td><td style="text-align:right;"><code>CPUSummary.cache_size(Val(2))</code>; fallback 512 KiB</td></tr><tr><td style="text-align:right;"><code>_L3_BYTES</code></td><td style="text-align:right;">L3 <strong>total</strong> size, bytes (<code>CPUSummary</code> reports a per-core <em>share</em>; L3 is shared, and nc-blocking wants the total, so this uses <code>CpuId.cachesize()[3]</code>)</td><td style="text-align:right;">pref <code>&quot;l3_bytes&quot;</code>; fallback 8 MiB</td></tr><tr><td style="text-align:right;"><code>_CACHELINE</code></td><td style="text-align:right;">Cache-line size (prefetch density unit)</td><td style="text-align:right;"><code>CpuId.cachelinesize()</code>; fallback 64</td></tr><tr><td style="text-align:right;"><code>_L1D_ASSOC</code>, <code>_L1_WAY_BYTES</code></td><td style="text-align:right;">L1d associativity (raw <code>cpuid</code> leaf) and the derived way stride — the address stride at which parallel streams collide in one L1 set (caps gemv-N stream counts on power-of-2 <code>lda</code>)</td><td style="text-align:right;">pref <code>&quot;l1d_assoc&quot;</code>; fallback 8-way</td></tr><tr><td style="text-align:right;"><code>_NVREG</code></td><td style="text-align:right;">Architectural vector registers: 32 (AVX-512, AArch64) vs 16 (AVX2/SSE) — the register-budget cap</td><td style="text-align:right;">derived from <code>_SIMD_BYTES</code> + arch</td></tr><tr><td style="text-align:right;"><code>_CPU_VENDOR</code>, <code>_CPU_FAMILY</code></td><td style="text-align:right;">Vendor symbol and display family (e.g. AMD <code>0x19</code> = Zen3/Zen4, <code>0x1A</code> = Zen5)</td><td style="text-align:right;"><code>CpuId.cpuvendor()</code> / <code>cpumodel()</code>; pref <code>&quot;cpu_family&quot;</code></td></tr><tr><td style="text-align:right;"><code>_INTEL_AVX2</code></td><td style="text-align:right;">Intel-AVX2-without-AVX-512 (Haswell/Broadwell class): narrow OOO window makes serial FMA reductions latency-bound; kernels opt into extra-accumulator splits on this</td><td style="text-align:right;">vendor + feature bits (width and cache size <em>cannot</em> distinguish µarchs with the same ISA)</td></tr><tr><td style="text-align:right;"><code>_HW</code></td><td style="text-align:right;">The whole machine as a <code>NamedTuple</code> const (<code>simd</code>, <code>l1</code>, <code>l2</code>, <code>l3</code>, <code>vendor</code>, <code>family</code>, <code>nvreg</code>). Derivation functions take <code>hw</code> as an argument, so they are <strong>pure</strong> — <code>test/autotune_tests.jl</code> feeds descriptors of every fleet box offline and asserts each formula reproduces the measured optima</td><td style="text-align:right;">assembled from the above</td></tr><tr><td style="text-align:right;"><code>_double_pumped(hw)</code></td><td style="text-align:right;">Full-width registers but <strong>half-width FP datapath</strong> (a 512-bit op occupies the 256-bit pipes twice). Not CPUID-discoverable — the one legitimate family lookup, a silicon <em>fact</em>: AMD family <code>0x19</code> + AVX-512 = Zen4</td><td style="text-align:right;"><code>hw.vendor</code>/<code>hw.family</code>/<code>hw.simd</code></td></tr><tr><td style="text-align:right;"><code>_datapath_bytes(hw)</code></td><td style="text-align:right;">Effective FP datapath width: <code>simd ÷ 2</code> if double-pumped, else <code>simd</code></td><td style="text-align:right;">derived</td></tr></tbody></table><h2 id="2.-Glossary" tabindex="-1">2. Glossary <a class="header-anchor" href="#2.-Glossary" aria-label="Permalink to &quot;2. Glossary {#2.-Glossary}&quot;">​</a></h2><p>The vocabulary you will hit in every kernel file. Skim it now, come back when a comment confuses you.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Term</th><th style="text-align:right;">What it physically is</th><th style="text-align:right;">Example</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>W</strong></td><td style="text-align:right;">SIMD lanes per register for the element type (<code>_vwidth(T)</code>): 8 for <code>Float64</code> on AVX-512, 4 on AVX2</td><td style="text-align:right;">everywhere</td></tr><tr><td style="text-align:right;"><strong>NB</strong></td><td style="text-align:right;">Block / panel width of a blocked LAPACK factorization: columns factored by the slow BLAS-2 panel per iteration before handing the trailing update to BLAS-3</td><td style="text-align:right;"><code>_qr_nb</code>, <code>_lu_nb</code>, <code>_chol_nb</code></td></tr><tr><td style="text-align:right;"><strong>MR / NR</strong></td><td style="text-align:right;">Register microkernel tile: MR row-blocks × NR columns of C held <em>in registers</em> through the k-loop. Bounded by <code>_NVREG</code></td><td style="text-align:right;"><code>_at_gemm_mr</code>, <code>_at_gemm_nr</code>, <code>_CPOTF2_MR</code></td></tr><tr><td style="text-align:right;"><strong>KC / MC / NC</strong> (cache blocks)</td><td style="text-align:right;">The BLIS 5-loop cache blocking: KC = k-depth so a B micropanel stays L1-resident; MC = A-block rows so <code>mc·kc</code> stays in L2; NC = B-block columns so <code>kc·nc</code> stays in L3</td><td style="text-align:right;"><code>_at_gemm_kc/_mc/_nc</code></td></tr><tr><td style="text-align:right;"><strong>NC / NP</strong> (streams)</td><td style="text-align:right;">In Level-2 kernels: columns processed per pass / concurrent memory streams (each stream = one column being read or RMW-updated)</td><td style="text-align:right;"><code>_ger_np</code>, <code>_CGEMVT_NC</code>, <code>_gemvn_minner_np</code></td></tr><tr><td style="text-align:right;"><strong>base / base-case cutoff</strong></td><td style="text-align:right;">The size at/below which a recursion or blocked driver stops and runs a simple unblocked kernel</td><td style="text-align:right;"><code>_POTRF_BASE</code>, <code>_GETF2_BASE</code>, <code>_CPOTRF_BASE</code></td></tr><tr><td style="text-align:right;"><strong>crossover / algorithm-switch threshold</strong></td><td style="text-align:right;">The size where one <em>algorithm</em> stops winning and another takes over (not a block size — a routing decision)</td><td style="text-align:right;"><code>_QR_UNBLK_MAX</code>, <code>_zqr_unblk_max</code>, <code>_SVD_DC_CROSS</code></td></tr><tr><td style="text-align:right;"><strong>unblocked vs blocked</strong></td><td style="text-align:right;">Unblocked = straight BLAS-2 loop over the whole problem (low overhead, poor cache reuse). Blocked = factor an NB-wide panel, then update the trailing matrix with BLAS-3</td><td style="text-align:right;"><code>geqrf!</code> in <code>src/lapack/qr.jl</code></td></tr><tr><td style="text-align:right;"><strong>panel</strong></td><td style="text-align:right;">The tall-skinny <code>m×NB</code> submatrix a blocked factorization processes per step; also a group of columns a Level-2 kernel sweeps together</td><td style="text-align:right;">QR/LU panel, ger NP-column panel</td></tr><tr><td style="text-align:right;"><strong>unroll / fuse factor (U)</strong></td><td style="text-align:right;">How many W-wide steps one loop iteration performs — independent accumulators to cover FMA latency (ILP)</td><td style="text-align:right;"><code>_GER_PANEL_U = 4</code>, <code>_GEMVN_MINNER_U = 4</code></td></tr><tr><td style="text-align:right;"><strong>compact-WY</strong></td><td style="text-align:right;">The LAPACK blocked-Householder representation <code>Q = I − V·T·Vᵀ</code>: lets a panel&#39;s NB reflectors be applied to the trailing matrix as two gemms instead of NB rank-1 updates</td><td style="text-align:right;"><code>src/lapack/wy.jl</code></td></tr><tr><td style="text-align:right;"><strong>residency</strong></td><td style="text-align:right;">&quot;Working set X stays in cache level Y across the sweep&quot; — the criterion behind almost every block size</td><td style="text-align:right;"><code>kc·nr·sizeof(T) ≤ ½·L1</code></td></tr><tr><td style="text-align:right;"><strong>double-pumped</strong></td><td style="text-align:right;">See <code>_double_pumped</code> above: 512-bit registers fed through 256-bit pipes (Zen4)</td><td style="text-align:right;"><code>_datapath_bytes</code></td></tr></tbody></table><h2 id="3.-The-three-physical-criteria" tabindex="-1">3. The three physical criteria <a class="header-anchor" href="#3.-The-three-physical-criteria" aria-label="Permalink to &quot;3. The three physical criteria {#3.-The-three-physical-criteria}&quot;">​</a></h2><p>Every Derive formula keys on one of three physical mechanisms. When you read a derived const, your job is to run this decoding: <em>which detected consts appear, what fraction of which cache (or which budget) do they express, and what do the clamp bounds protect?</em></p><h3 id="3.1-Cache-residency-block-sizes" tabindex="-1">3.1 Cache residency (block sizes) <a class="header-anchor" href="#3.1-Cache-residency-block-sizes" aria-label="Permalink to &quot;3.1 Cache residency (block sizes) {#3.1-Cache-residency-block-sizes}&quot;">​</a></h3><p>The block size is chosen so a working set stays resident in a specific cache level while the kernel sweeps over it. The canonical shape is <code>size = capacity_share ÷ footprint_per_unit</code>, clamped by overhead floors and caps.</p><p><strong>Worked example — <code>_qr_nb</code> (<code>src/lapack/qr.jl</code>), the real blocked-QR panel width:</strong></p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@inline</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _qr_nb</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(m</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, n</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    fl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 256</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ÷</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _NVREG;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    clamp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fl </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cld</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(m </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Float64), _L2_BYTES), fl, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>Decode it term by term:</p><ul><li><p><code>cld(m·n·8, _L2_BYTES)</code> — <em>how many times does the matrix overflow L2?</em> While the matrix is L2-resident the trailing update re-streams from cache and a narrow panel is fine; once it spills, each panel re-streams the trailing matrix from DRAM (total sweep traffic ∝ k/nb), so nb must <strong>grow</strong> with the overflow factor to bound DRAM bytes.</p></li><li><p><code>fl = 256 ÷ _NVREG</code> — the floor keys on <strong>register count</strong>, not vector width, because the µarch relationship <em>inverts</em>: fewer vector registers (AVX2&#39;s 16 vs AVX-512&#39;s 32) ⇒ a smaller gemm microkernel tile ⇒ the trailing gemm needs a <em>deeper</em> panel to amortize. So <code>nb_floor ∝ 1/_NVREG</code>: Zen4/Zen5 (32 regs) → 8, Zen3 (16 regs) → 16. The 256 scale is fleet-calibrated (measured Zen4 floor 8 × <code>_NVREG</code> 32).</p></li><li><p><code>cap 32</code> — where the BLAS-2 panel share (≈ 0.75·nb/n) starts to bite; also equals the old flat value, so large sizes are regression-free by construction.</p></li></ul><p>Measured-validated on Zen4 (8→16→32) and Zen3 (16→24→32); the formula reproduces both within ~5% — that fleet validation is what lets it be <em>trusted to extrapolate</em>.</p><p>More residency formulas to pattern-match against:</p><ul><li><p><code>_at_gemm_kc(hw, T)</code> (<code>cpuinfo.jl</code>): the B micropanel per k-step, <code>kc·nr·sizeof(T) ≤ ½·L1</code>.</p></li><li><p><code>_at_gemm_mc</code> / <code>_at_gemm_nc</code>: A-block ≤ ~30% of L2 (the rest streams B/C/prefetch); B-block ≤ ¼ of shared L3, with <code>_avoid_po2</code> dodging power-of-2 strides (set aliasing).</p></li><li><p><code>_zqr_nb(T, m, n)</code> (<code>qr.jl</code>): complex panel width, grows one base-width per ¼-L3 overflow, <code>clamp(_QR_NB_C · cld(m·n·sizeof(T), _L3_BYTES ÷ 4), _QR_NB_C, 4·_QR_NB_C)</code>.</p></li><li><p><code>_zqr_unblk_max(T)</code> (<code>qr.jl</code>): the <em>crossover</em> below which unblocked complex QR wins — <code>_CGEMM_3M ? 2·_L2_BYTES : _L3_BYTES ÷ 2</code> (matrix bytes, not a dimension: the BLAS-2 panel wins exactly while its O(n³) re-streams hit cache rather than DRAM, and the relevant cache level differs between the AVX2/3M and wide-SIMD complex-gemm regimes).</p></li><li><p><code>_GEMVN_MINNER_MAXA</code> (<code>src/blas2/level2.jl</code>): <code>4·_L3_BYTES</code> — the m-inner gemv-N path helps the mid-n/L3 regime but its y-restream loses in deep DRAM, so it is capped at &quot;A no bigger than a few × L3&quot;.</p></li><li><p><code>_CGETF2_BASE</code> (<code>src/lapack/lu.jl</code>): <code>_GETF2_BASE · (sizeof(ComplexF64) ÷ sizeof(Float64)) = 32</code> — a <em>sizeof-derived</em> sibling: the complex panel base is wider than the real one by exactly the element-size ratio, so <code>ComplexF32</code> tracks the same criterion automatically.</p></li><li><p><code>_lu_nb(n)</code> (<code>lu.jl</code>): <code>clamp((n ÷ 8) &amp; ~15, _LU_NB, 128)</code> — shape-adaptive slope (grow nb so the rank-nb trailing gemm isn&#39;t skinny at large n), but note its floor and cap are <em>validated literals</em>, not residency formulas — see <code>_LU_NB</code> in Section 4.</p></li></ul><h3 id="3.2-Datapath-and-latency-unrolls,-fuse-factors,-stream-counts" tabindex="-1">3.2 Datapath and latency (unrolls, fuse factors, stream counts) <a class="header-anchor" href="#3.2-Datapath-and-latency-unrolls,-fuse-factors,-stream-counts" aria-label="Permalink to &quot;3.2 Datapath and latency (unrolls, fuse factors, stream counts) {#3.2-Datapath-and-latency-unrolls,-fuse-factors,-stream-counts}&quot;">​</a></h3><p>These knobs exist to keep the execution units fed: enough independent FMA chains to cover latency, enough load streams to keep the datapath at line rate.</p><ul><li><p><strong>Derivable case:</strong> <code>_ILP_TARGET = 16</code> in <code>cpuinfo.jl</code> is 2 × FMA_latency(4) × FMA_ports(2) — the number of independent accumulator chains needed to saturate the FMA pipes; <code>_at_gemm_mr/_at_gemm_nr</code> split that budget into a tile. <code>_CPOTF2_MR</code> (<code>lapack.jl</code>) is a line-rate match: unroll rows until one step consumes a 64-byte cache line at datapath width — <code>max(1, 64 ÷ _datapath_bytes(_HW))</code>, i.e. MR=2 on double-pumped Zen4 and AVX2, MR=1 on native-512 Zen5/Intel. Both are Derive: the optimum follows from published, detected silicon facts.</p></li><li><p><strong>Non-derivable case:</strong> the DRAM-bound <code>ger!</code> stream count <code>_ger_np</code> (Section 3.4 shows the mechanism). The measured optima are Zen5 → 1, Zen3 → 4, Zen4 → 8 — with every external cause eliminated (memory subsystem scales fine on all three, DIMMs rank-matched, OS/clock cancel in the ratio, 4K aliasing padded out, identical LLVM codegen on Zen4/Zen5). The optimum is an intrinsic per-core property of the prefetcher/write-stream machinery with <strong>no detected const that predicts it</strong>, and it <em>inverts sign</em> across µarchs — that inversion is the tell that a knob is Measure-tier, not Derive-tier. Any formula we wrote would just be a disguised per-µarch table.</p></li></ul><h3 id="3.3-ISA-/-register-count-width-granularity,-budgets,-candidate-bounds" tabindex="-1">3.3 ISA / register count (width granularity, budgets, candidate bounds) <a class="header-anchor" href="#3.3-ISA-/-register-count-width-granularity,-budgets,-candidate-bounds" aria-label="Permalink to &quot;3.3 ISA / register count (width granularity, budgets, candidate bounds) {#3.3-ISA-/-register-count-width-granularity,-budgets,-candidate-bounds}&quot;">​</a></h3><p>The third family of constraints comes from the ISA itself:</p><ul><li><p><strong>Width granularity:</strong> blocks and unrolls are rounded to multiples of W (<code>_round_dn(x, W)</code>), because a partial vector means masked tails.</p></li><li><p><strong>Register budget:</strong> microkernel tiles must fit <code>_NVREG</code> — e.g. <code>_at_gemm_mr</code> caps MR at <code>(nvreg − 1) ÷ (nr + 1)</code> (accumulators + A-loads + one B-broadcast), and <code>_acc_cap(hw, T) = (nvreg − 4)·W</code> — the register file&#39;s accumulator capacity in elements — is the base quantity behind the gemm/rank-k pack crossovers.</p></li><li><p><strong>Candidate-set bounds:</strong> in Measure-tier knobs, even the <em>search space</em> should be Derived (a stream count bounded by <code>_NVREG</code>, an unroll bounded by the register budget) so that both the bounds and the selection adapt to unseen hardware.</p></li></ul><h2 id="4.-Constants-that-are-NOT-pure-formulas-—-and-why" tabindex="-1">4. Constants that are NOT pure formulas — and why <a class="header-anchor" href="#4.-Constants-that-are-NOT-pure-formulas-—-and-why" aria-label="Permalink to &quot;4. Constants that are NOT pure formulas — and why {#4.-Constants-that-are-NOT-pure-formulas-—-and-why}&quot;">​</a></h2><p>The PDM ladder allows literals only as documented exceptions. This table is the current catalog. Two honest categories show up: <strong>proven invariants</strong> (the number is genuinely machine-independent — an algorithm property or a bound that holds on any real machine) and <strong>validated literals</strong> (a derivation was attempted and <em>falsified</em>, or the fleet data to derive from doesn&#39;t exist yet — the literal preserves measured-good behavior until it does). The second category is acknowledged debt, not a loophole.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Const</th><th style="text-align:right;">Value</th><th style="text-align:right;">What it controls</th><th style="text-align:right;">Why it&#39;s not a formula</th><th style="text-align:right;">PDM classification</th></tr></thead><tbody><tr><td style="text-align:right;"><code>_STEDC_NB</code> (<code>eigen_dc.jl</code>)</td><td style="text-align:right;">25</td><td style="text-align:right;">Symmetric-tridiagonal eigensolver: <code>steqr</code> ↔ divide-and-conquer base-case switch</td><td style="text-align:right;">LAPACK <code>dstedc</code>&#39;s SMLSIZ — an <strong>algorithm-intrinsic</strong> crossover between two algorithms&#39; O() constants, machine-independent. Not a hardware knob at all.</td><td style="text-align:right;">Proven-invariant — <strong>Exempt</strong></td></tr><tr><td style="text-align:right;"><code>_SVD_DC_CROSS</code> (<code>svd.jl</code>)</td><td style="text-align:right;">1</td><td style="text-align:right;">SVD with vectors: <code>bdsqr</code> (QR sweeps) ↔ divide-and-conquer</td><td style="text-align:right;">Currently a <strong>correctness override</strong>, not a perf choice: the <code>bdsqr!</code> port fails on near-degenerate σ clusters (non-convergence, or silently dropped largest σ on graded bidiagonals), so <em>all</em> with-vectors SVD routes through D&amp;C. If <code>bdsqr</code> convergence is fixed, the perf crossover (bdsqr won n≤96 pre-fix) returns — and it is algorithm-intrinsic like SMLSIZ, still not cache-derived.</td><td style="text-align:right;">Proven-invariant / correctness-pinned — <strong>Exempt</strong> (revisit only if <code>bdsqr!</code> is fixed)</td></tr><tr><td style="text-align:right;"><code>_QR_UNBLK_MAX</code> (<code>qr.jl</code>)</td><td style="text-align:right;">32</td><td style="text-align:right;">Real QR: unblocked ↔ blocked crossover (dimension-wise, so tall-skinny fronts stay unblocked)</td><td style="text-align:right;">The per-µarch unblocked crossover is <strong>unmeasured off Zen4</strong>. A vwidth-scaled formula would shrink Zen3&#39;s proven-good unblocked range (32 → 16) with no data behind it. Pinned at the old flat value so every µarch keeps master&#39;s validated n≤32 behavior.</td><td style="text-align:right;">Validated-literal, <strong>pending fleet data</strong> (derive when fleet unblocked-crossover measurements exist)</td></tr><tr><td style="text-align:right;"><code>_LU_NB</code> (<code>lu.jl</code>)</td><td style="text-align:right;">48 (floor of <code>_lu_nb</code>; cap 128)</td><td style="text-align:right;">Real LU blocked panel width base</td><td style="text-align:right;">The residency derivation was <strong>fleet-falsified</strong>: a full nb×n sweep on Zen4+Zen3 shows a flat, roughly µarch-invariant optimum whose curve is <em>parity-bumpy</em> (multiples of 64 win — 128, 192 beat 168), not residency-shaped; the derived <code>_l1_block</code> value (168 on Zen3) lands in a measured <em>trough</em>, worse than the literal. The true large-n optimum is per-µarch by only ~0.5–1.5% — a formula would add spurious variation for no gain.</td><td style="text-align:right;">Validated-literal (<strong>derivation falsified</strong>; Measure-candidate only if that last ~1% ever matters)</td></tr><tr><td style="text-align:right;"><code>_GETF2_BASE</code> (<code>lu.jl</code>)</td><td style="text-align:right;">16</td><td style="text-align:right;">LU panel: flat rank-1 sweep ↔ recursive BLAS-3 split</td><td style="text-align:right;">A <strong>store-traffic algorithm-switch crossover</strong>, not a cache-residency block — the split halves O(pb²·mp) store traffic regardless of cache size. Validated by the gate (getrf gates vs OB+AOCL fleet-wide); a residency formula would add spurious variation (cf. the falsified <code>_LU_NB</code> derivation). Its complex sibling <code>_CGETF2_BASE</code> <em>is</em> sizeof-derived from this anchor.</td><td style="text-align:right;">Proven-invariant — <strong>Exempt</strong> (invariant crossover, validated-by-gate)</td></tr><tr><td style="text-align:right;"><code>_TR_TB</code> (<code>lapack.jl</code>)</td><td style="text-align:right;">32</td><td style="text-align:right;">Cache-blocked triangular-transpose tile (the upper→lower reuse levers)</td><td style="text-align:right;">Residency bound that holds <strong>universally</strong>: two 32² Float64 tiles = 16 KB ≤ any real L1 (complex: 32 KB, still fits typical L1). Deriving it from <code>_L1_BYTES</code> would change nothing on any machine that exists.</td><td style="text-align:right;">Proven-invariant — <strong>Exempt</strong> (residency-invariant)</td></tr><tr><td style="text-align:right;"><code>_POTRF_BASE</code> (<code>lapack.jl</code>)</td><td style="text-align:right;">32</td><td style="text-align:right;">Generic Cholesky recursion: unblocked-base cutoff (F64/Dual; F32 uses <code>_POTRF_BASE &gt;&gt; 1</code> — a sizeof-style derived sibling)</td><td style="text-align:right;">Measured to be a <strong>recursion-overhead floor, not L1 residency</strong>: the residency guess √(L1/8) = 64 is <em>worse</em> than 32; the optimum is small and µarch-flat (16–32 on both fleet AVX2/AVX-512 boxes). Fleet A/B validated (base=512 was up to 43.9× slower than OB).</td><td style="text-align:right;">Validated-literal (invariant crossover; knob <code>&quot;potrf_base&quot;</code>)</td></tr><tr><td style="text-align:right;"><code>_chol_sth</code> / <code>_chol_sb</code> (<code>lapack.jl</code>)</td><td style="text-align:right;">16 / 32</td><td style="text-align:right;">F64 fast-path Cholesky small-n blocking: diagonal base-case cap and block size (<code>sb = 2·sth</code>)</td><td style="text-align:right;">The width-scaled hypothesis was <strong>disproven by controlled A/B</strong> on Zen4: th=16 beats th=32(=4·W) at n=32–128, ties above — the slow left-looking base wants a <em>small fixed</em> diagonal block regardless of SIMD width, so 16 is a measured µarch-invariant crossover, correctly flat. Source comment still says &quot;fleet-tuned later&quot;.</td><td style="text-align:right;">Validated-literal, <strong>pending fleet confirmation</strong> (measured invariant on 2 boxes; knob-able)</td></tr></tbody></table><p>Note what the table does <em>not</em> contain: <code>_CPOTRF_BASE</code>, <code>_CPOTF2_MR</code>, <code>_CPOTRF_NBMAX</code>, and <code>_CGETF2_BASE</code> were all literals once and are now derived (<code>_at_cpotrf_base(_HW) = 32 + 4·W</code>, <code>_at_cpotf2_mr = 64 ÷ datapath</code>, <code>_at_cpotrf_nbmax = 64 + 16·W</code>, <code>sizeof</code>-ratio). The affine-in-W shape of the cpotrf pair is worth internalizing: a width-independent overhead floor plus a per-lane slope is the common form for implementation crossovers that are cache-independent but width-dominant. The table above is the <em>residue</em> after that migration, and each row states which kind of residue it is.</p><h2 id="5.-The-Measure-tier:-_ger_np-walkthrough" tabindex="-1">5. The Measure tier: <code>_ger_np</code> walkthrough <a class="header-anchor" href="#5.-The-Measure-tier:-_ger_np-walkthrough" aria-label="Permalink to &quot;5. The Measure tier: \`_ger_np\` walkthrough {#5.-The-Measure-tier:-_ger_np-walkthrough}&quot;">​</a></h2><p>When the D-vs-M question answers &quot;No&quot;, the pattern to copy is <code>_ger_np</code> in <code>src/blas2/level2.jl</code> (<code>CLAUDE.md</code> 8b names <code>_ger_np</code>/<code>_gemvt_nc</code> as the pattern; as of this writing <code>_ger_np</code> is the implemented instance — the real gemv-T column-block still rides a fixed <code>Val(4)</code> and the complex one a pinned <code>_CGEMVT_NC</code>, i.e. Measure-tier candidates not yet converted). Four pieces, in order:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 1. Pin: a set Preference always wins.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _GER_NP_PREF </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @load_preference</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ger_panel_np&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nothing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 2. @static if: when pinned, the auto-tune branch is NEVER DEFINED.</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> if</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> isnothing</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(_GER_NP_PREF)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _measure_ger_np</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">generating_output</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">      # no benchmarking during precompilation</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        try</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # DRAM-bound problem (~2×L3 rows), pre-touched pages,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # candidates (1, 2, 4, 8), warmup absorbs each Val&#39;s JIT,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # min-of-4 timing, best wins.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            ...</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        catch</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                              # safe middle; see poisoning note below</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _GER_NP_ONCE </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">OncePerProcess{Int}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(_measure_ger_np)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @inline</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _ger_np</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _GER_NP_ONCE</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">else</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @inline</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _ger_np</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _GER_NP_PREF</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>Why each piece is the way it is:</p><ul><li><p><strong><code>@load_preference</code> pin first.</strong> <code>bench/calibrate.jl</code> writes the Preference after a proper locked-frequency calibration; a pinned box never benchmarks itself.</p></li><li><p><strong><code>@static if</code>, not dead-code-elimination-by-faith.</strong> When the Preference <em>is</em> set, the measuring function and the <code>OncePerProcess</code> const are never even defined — the pinned build is trivially trim-clean rather than hopefully trim-clean.</p></li><li><p><strong><code>Base.OncePerProcess</code>, not <code>__init__</code>.</strong> The benchmark runs at most once per process, lazily, on the first DRAM-bound <code>ger!</code> call — so a process that never hits that path never pays for it, and there is no load-time side effect. Two guards matter: the <code>Base.generating_output()</code> early-out (don&#39;t burn a benchmark during precompilation) and the total <code>try/catch</code> (an <code>OncePerProcess</code> whose initializer throws <strong>poisons every subsequent call</strong> in the process, so the initializer must be infallible, falling back to the safe middle value).</p></li><li><p><strong>Formula-bounded candidate set.</strong> The candidates are few and bounded by a Derived quantity (a stream count can&#39;t usefully exceed the register/associativity budget — cf. <code>_NVREG</code>, <code>_L1D_ASSOC</code>), so both the bounds <em>and</em> the selection adapt to unseen hardware instead of being a hidden per-µarch table.</p></li><li><p><strong>The <code>.so</code> build MUST pin.</strong> <code>juliac --trim</code> cannot contain a runtime benchmark. <code>juliac/build.jl</code> sets <code>ger_panel_np =&gt; 4</code> for the duration of the build (and restores the previous state after), which flips the <code>@static if</code> to the pinned branch. Any new Measure-tier knob needs the same treatment in <code>juliac/build.jl</code> — forgetting it breaks the trim build.</p></li></ul><p>One more trap, learned the hard way (<code>_POTRF_BASE_F32</code>): <code>@load_preference</code> <strong>must sit in a top-level <code>const</code></strong>, never inside a function body — in a function it becomes a per-call runtime Preferences lookup, which dominated microsecond-scale small-n factorizations.</p><h3 id="5.1-Measure-a-crossover,-not-a-flag:-_trsm_r_scratch_kmin" tabindex="-1">5.1 Measure a <em>crossover</em>, not a flag: <code>_trsm_r_scratch_kmin</code> <a class="header-anchor" href="#5.1-Measure-a-crossover,-not-a-flag:-_trsm_r_scratch_kmin" aria-label="Permalink to &quot;5.1 Measure a _crossover_, not a flag: \`_trsm_r_scratch_kmin\` {#5.1-Measure-a-crossover,-not-a-flag:-_trsm_r_scratch_kmin}&quot;">​</a></h3><p><code>_ger_np</code> picks a count. The cautionary example is <code>_trsm_r_scratch_kmin</code> (<code>src/blas3/level3.jl</code>, pref <code>&quot;trsm_r_alias_scratch_kmin&quot;</code>): when side-R <code>trsm</code>&#39;s <code>ldb</code> is an L1 way-stride multiple (<code>_alias_ld</code>), the solved-column re-reads all collide in one L1 set. Two cures — solve each row-chunk into the odd-<code>ld</code> <code>rpack</code> scratch and copy back, or solve in place and eat the conflicts.</p><p>It was first written as a <strong><code>Bool</code></strong>, probed at <code>k = _TRSM_DBASE</code>. That was wrong twice over, and only the fleet run caught it: 2. <strong>The winner depends on <code>k</code>, and <code>k</code> was the dimension nobody was looking at.</strong> Fleet A/B on galen/Zen3 at an aliasing <code>ldb</code>: scratch/in-place = 1.18 at k=16 (in-place wins), <strong>1.00–1.02 at k=32</strong>, 0.68–0.79 at k≥48 (scratch wins by 20–32%). That is physics, not noise — the copy-back costs O(m·k) against an O(m·k²) solve, so its relative price falls as 1/k and scratch can only pay above some k. A single Bool cannot represent a crossover. Worth recording <em>why</em> this was missed: the original code comment asserted a <strong>µarch</strong> inversion (&quot;Zen3 scratch +41–49%, Zen4 a net loss&quot;), so the knob was modelled as a per-box flag. Measured properly, both boxes cross over at the same k (64) — the µarch-inversion framing was an artifact, and it hid the real variable.</p><ol start="3"><li><strong>The probe sat exactly on the tie.</strong> <code>k = _TRSM_DBASE</code> = 32 is the crossover itself, so the measurement was a coin flip: two galen processes in one run returned different answers, costing <code>&#39;T&#39;</code> 1024×16 = 0.763 (chose scratch) and <code>&#39;N&#39;</code> 1024×48 = 0.718 (chose in-place) — wrong in <em>both</em> directions on the same box.</li></ol><p>The fix is to measure the <strong>crossover k</strong> itself: walk a Derived candidate set (powers of two from <code>_vwidth</code> up to <code>_TRSM_R_FUSE</code>) at an aliasing probe shape and return the first k where scratch wins, or <code>typemax(Int)</code> for &quot;never&quot;. Lessons that generalize:</p><ul><li><p><strong>Probe where the decision is decisive, never where it is marginal.</strong> If your probe shape happens to be the tie point, the knob returns noise and looks like a flaky benchmark.</p></li><li><p><strong>Before choosing Bool, ask what the answer depends on.</strong> If it varies along a dimension the caller already knows (here <code>k</code>), measure the threshold along that dimension.</p></li><li><p><strong>The <code>catch</code> returns the conservative strategy</strong>, not the fast one. An infallible initializer is mandatory (a throwing <code>OncePerProcess</code> poisons the process), and the safe fallback is the one with no extra buffer traffic.</p></li><li><p><strong>Don&#39;t reach for Measure when Derive fits.</strong> The sibling question in the same routine — should side-R <code>op=&#39;N&#39;</code> pay an O(k²) reflect copy to reach the fused leaf? — <em>is</em> predictable: the copy&#39;s per-element cost is governed by whether A&#39;s k×k footprint stays L1-resident, so the gate is the formula <code>k·k·sizeof(T) ≤ _L1_BYTES</code> (Derive), not a measured crossover. Measuring what a residency formula already predicts is its own kind of debt.</p></li></ul><h2 id="6.-How-to-add-or-change-a-tuning-constant-checklist" tabindex="-1">6. How to add or change a tuning constant (checklist) <a class="header-anchor" href="#6.-How-to-add-or-change-a-tuning-constant-checklist" aria-label="Permalink to &quot;6. How to add or change a tuning constant (checklist) {#6.-How-to-add-or-change-a-tuning-constant-checklist}&quot;">​</a></h2><ol start="2"><li><p><strong>Confirm it&#39;s actually a tuning constant.</strong> Machine-dependent block size, cutoff, panel width, unroll, fuse factor, stream count, or algorithm-switch threshold ⇒ yes, the PDM ladder applies. A correctness bound (e.g. <code>lassq</code> scaling) ⇒ no — different rules, don&#39;t &quot;tune&quot; it.</p></li><li><p><strong>Ask the D-vs-M question:</strong> <em>is the optimum physically predictable from a detected const?</em> Cache residency, SIMD width, register count, datapath width ⇒ <strong>Derive</strong>. Port balance / prefetcher / stream behavior, or your model mispredicts a box you <em>have</em> ⇒ <strong>Measure</strong>. There is no third option; if you can produce neither a formula nor a measure harness, stop and flag it.</p></li><li><p><strong>Derive tier:</strong> write the default as a pure formula over <code>_HW</code> fields (an <code>_at_*(hw, T)</code> function in <code>cpuinfo.jl</code> if it&#39;s shared, or inline over the detected consts). Take cache size <strong>and</strong> ISA into account together — a block size depends on what fits in cache <em>and</em> the vector width. Round to W, clamp with a stated floor/cap. Add the fleet descriptors&#39; expected values to <code>test/autotune_tests.jl</code>.</p></li><li><p><strong>Measure tier:</strong> mirror <code>_ger_np</code> exactly — top-level <code>@load_preference</code> const, <code>@static if isnothing(pref)</code>, <code>Base.OncePerProcess</code> with <code>generating_output()</code> guard and total <code>try/catch</code>, formula-bounded candidates. Then <strong>add the pin to <code>juliac/build.jl</code></strong> (the trim build must never contain the benchmark branch) and a calibration entry to <code>bench/calibrate.jl</code>.</p></li><li><p><strong>Keep the Preferences override</strong> in both tiers (<code>@load_preference(&quot;name&quot;, &lt;derived default&gt;)</code>): pinning, cross-compilation, and calibration all ride it.</p></li><li><p><strong>Write the comment</strong> — every tuning const cites (a) which detected consts it derives from, (b) the physical criterion (residency share / latency budget / register budget / store-traffic crossover), and (c) its PDM tier. If it&#39;s a deliberate literal, say <em>why</em> (invariant? falsified derivation? pending fleet data?) and add <code># req8-ok: &lt;reason&gt;</code> — otherwise <code>test/req8_lint_tests.jl</code> will fail on it. Watch for the &quot;derived selection, hardcoded materialization&quot; trap: <code>Val(8)</code> at a call site instead of <code>Val(_THE_CONST)</code> is a violation the lint catches.</p></li><li><p><strong>Validate on the fleet before trusting it.</strong> A derived formula must <em>reproduce the measured optima on the known fleet</em> before it&#39;s trusted to extrapolate to unseen machines. All gate measurements run under <code>sudo bench/fleet_freqlock.sh lock</code> (passive governor, boost off, cores pinned to base clock, frequency verified under load) — a run whose verify step fails is invalid, full stop. Use controlled same-machine A/B comparisons, never cross-run one-offs.</p></li><li><p><strong>Be ready for falsification.</strong> <code>_LU_NB</code> and <code>_chol_sth</code> both killed plausible formulas with measurements. A validated literal with an honest comment beats an elegant formula that adds spurious variation — but only <em>after</em> the derivation was tried and measured, never instead of trying.</p></li></ol>`,52)])])}const u=t(o,[["render",n]]);export{g as __pageData,u as default};
