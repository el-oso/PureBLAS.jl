import{_ as t,o as a,c as i,ao as s}from"./chunks/framework.BSzmyLFO.js";const k=JSON.parse('{"title":"SIMD & Hardware Adaptation","description":"","frontmatter":{},"headers":[],"relativePath":"simd.md","filePath":"simd.md","lastUpdated":null}'),n={name:"simd.md"};function o(r,e,d,l,h,c){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="SIMD-and-Hardware-Adaptation" tabindex="-1">SIMD &amp; Hardware Adaptation <a class="header-anchor" href="#SIMD-and-Hardware-Adaptation" aria-label="Permalink to &quot;SIMD &amp; Hardware Adaptation {#SIMD-and-Hardware-Adaptation}&quot;">​</a></h1><p>PureBLAS is a pure-Julia BLAS, but its kernels are not written against any one CPU. This page describes how one generic kernel set adapts to the machine it runs on — the vector abstraction, the compile-time hardware detection, and the rule that <strong>every tuning parameter is a formula over detected hardware, never a hardcoded per-microarchitecture literal</strong>. It also contrasts this with the ahead-of-time (AOT) model used by C/Rust BLAS such as OpenBLAS and faer.</p><h2 id="The-vector-abstraction:-one-kernel,-every-width" tabindex="-1">The vector abstraction: one kernel, every width <a class="header-anchor" href="#The-vector-abstraction:-one-kernel,-every-width" aria-label="Permalink to &quot;The vector abstraction: one kernel, every width {#The-vector-abstraction:-one-kernel,-every-width}&quot;">​</a></h2><p>Hot kernels are written with <strong><a href="https://github.com/eschnett/SIMD.jl" target="_blank" rel="noreferrer">SIMD.jl</a></strong> — <code>Vec{N,T}</code> plus <code>vload</code> / <code>vstore</code> / <code>muladd</code> / <code>vifelse</code> / <code>shufflevector</code> — not <code>_mm256_*</code>-style intrinsics. A kernel is written once, parameterized on the vector width <code>N = _vwidth(T)</code>, and the same source compiles to AVX-512, AVX2, or NEON code depending on the build host. For example a Level-1 axpy inner step is just:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">V </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Vec{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_vwidth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(T), T}</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vstore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">muladd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(va, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(V, px </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(V, py </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o)), py </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> o)</span></span></code></pre></div><p>There are no per-ISA kernel copies to maintain. Real, unit-stride, dense inputs take this SIMD path; everything else (complex, strided, <code>ForwardDiff.Dual</code>, any other <code>T&lt;:Number</code>) falls to the generic scalar loop — which is what keeps Mode 2 differentiable (see <a href="./design">Design</a>).</p><h2 id="Compile-time-hardware-detection" tabindex="-1">Compile-time hardware detection <a class="header-anchor" href="#Compile-time-hardware-detection" aria-label="Permalink to &quot;Compile-time hardware detection {#Compile-time-hardware-detection}&quot;">​</a></h2><p>The register width and cache geometry are detected <strong>once, at build/load time</strong>, and baked into const-folded constants in <code>cpuinfo.jl</code>. Detection uses <code>CpuId</code> (<code>simdbytes</code>, <code>cpuvendor</code>, <code>cpufeature</code>, <code>cachesize</code>, <code>cachelinesize</code>), <code>CPUSummary</code> (<code>cache_size</code>), and <code>HostCPUFeatures</code>:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Constant</th><th style="text-align:right;">Meaning</th></tr></thead><tbody><tr><td style="text-align:right;"><code>_SIMD_BYTES</code>, <code>_vwidth(T)</code></td><td style="text-align:right;">vector width in bytes / lanes per <code>T</code></td></tr><tr><td style="text-align:right;"><code>_L1_BYTES</code>, <code>_L2_BYTES</code>, <code>_L3_BYTES</code></td><td style="text-align:right;">cache sizes (L3 is the <strong>total</strong>, not per-core share)</td></tr><tr><td style="text-align:right;"><code>_CACHELINE</code>, <code>_L1D_ASSOC</code>, <code>_L1_WAY_BYTES</code></td><td style="text-align:right;">line size, L1 associativity, way-stride</td></tr><tr><td style="text-align:right;"><code>_NVREG</code></td><td style="text-align:right;">vector register count (32 on AVX-512, 16 on AVX2)</td></tr><tr><td style="text-align:right;"><code>_CPU_VENDOR</code>, <code>_CPU_FAMILY</code>, <code>_INTEL_AVX2</code></td><td style="text-align:right;">vendor / family / feature bits</td></tr><tr><td style="text-align:right;"><code>_HW</code></td><td style="text-align:right;">a named tuple bundling the above for the <code>_at_*</code> derive helpers</td></tr></tbody></table><p>Because Julia JIT-compiles to the host, these are ordinary <code>const</code>s that <strong>const-fold away</strong>: the generated kernel contains no runtime <code>CpuId</code>/<code>cpuid</code> <code>ccall</code>, which is what makes it <code>juliac --trim</code>-compatible (Mode 1). Every one of these constants also accepts a <strong><code>Preferences</code> override</strong> (<code>@load_preference</code>) for cross-compilation, pinning, or correcting a heuristic — e.g. <code>simd_bytes</code>, <code>l3_bytes</code>, <code>l1d_assoc</code>.</p><h2 id="Deriving-tuning-from-hardware,-not-tabulating-it" tabindex="-1">Deriving tuning from hardware, not tabulating it <a class="header-anchor" href="#Deriving-tuning-from-hardware,-not-tabulating-it" aria-label="Permalink to &quot;Deriving tuning from hardware, not tabulating it {#Deriving-tuning-from-hardware,-not-tabulating-it}&quot;">​</a></h2><p>This is the core rule (and PureBLAS&#39;s main structural advantage over AOT BLAS): <strong>block sizes, base-case cutoffs, panel widths, and unroll factors are formulas over the detected constants, keyed on a physical criterion</strong> — cache <em>residency</em> for block sizes, datapath <em>latency</em> for unroll, ISA for width granularity. The <code>_at_*</code> helpers in <code>cpuinfo.jl</code> are these formulas:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_at_gemm_nr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> max</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_lanes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T), _ILP_TARGET </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">÷</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _lanes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T))     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NR from the ILP target</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_at_gemm_kc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)  </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _l1_block</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">_at_gemm_nr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T))                </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># B micropanel ≤ ½·L1</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">_acc_cap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)     </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (hw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">nvreg </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _lanes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(hw, T)                      </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># accumulators the reg file holds</span></span></code></pre></div><p>A bare literal like <code>_vwidth == 4 ? 48 : 64</code>, or <code>const _KC = 256</code>, is a <strong>bug</strong>: the tell is a number you cannot trace to a detected constant through a residency/latency formula. Why this is mandatory rather than stylistic: Julia compiles to the host at load time, so PureBLAS can <em>compute</em> the right sizes for the <strong>actual</strong> machine — including CPUs never benchmarked (a new laptop, a cloud box). A static C/Rust BLAS cannot; it ships hand-tuned per-µarch tables baked at <em>its</em> compile time. Hardcoding literals here throws away Julia&#39;s one real structural advantage.</p><p>A derived formula must <strong>reproduce the measured-optimal values on the known fleet (Zen3 / Zen4 / Zen5) before it is trusted to extrapolate</strong> — derive → validate on the fleet → ship. A recent example: the triangular-solve fused-leaf cutoff is the size at which the hot <code>KC × NR</code> panel fills L1,</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">_TRSM_FUSED_BASE </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> _L1_BYTES </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">÷</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (_GT_NR </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sizeof</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Float64))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 170 on Zen4 (32 K L1), 256 on Zen5 (48 K)</span></span></code></pre></div><p>— two different values, both computed from each machine&#39;s own L1, not a lookup table.</p><h2 id="Microarchitecture,-not-just-width" tabindex="-1">Microarchitecture, not just width <a class="header-anchor" href="#Microarchitecture,-not-just-width" aria-label="Permalink to &quot;Microarchitecture, not just width {#Microarchitecture,-not-just-width}&quot;">​</a></h2><p>Width and cache size do <strong>not</strong> distinguish microarchitectures with the same ISA (Haswell and Zen3 are both AVX2 / <code>_vwidth == 4</code>). For a µarch-dependent choice, key on the <strong>vendor and feature bits</strong>, not the width:</p><ul><li><p><code>_INTEL_AVX2</code> (<code>cpuvendor() === :Intel &amp;&amp; cpufeature(:AVX2) &amp;&amp; !cpufeature(:AVX512F)</code>) selects a latency split in the Cholesky base.</p></li><li><p><code>_double_pumped(hw)</code> (<code>hw.simd == 64 &amp;&amp; hw.vendor === :AMD &amp;&amp; hw.family == 0x19</code>) recognizes that early Zen4 executes 512-bit ops as two 256-bit halves, so <code>_datapath_bytes</code> — not <code>_SIMD_BYTES</code> — drives latency-bound unroll counts.</p></li></ul><p>These are still build-time detections (<code>req#7</code>): they const-fold, so no runtime branch survives.</p><h2 id="Tail-handling:-pack-over-mask" tabindex="-1">Tail handling: pack over mask <a class="header-anchor" href="#Tail-handling:-pack-over-mask" aria-label="Permalink to &quot;Tail handling: pack over mask {#Tail-handling:-pack-over-mask}&quot;">​</a></h2><p>Partial vectors at the edge of a tile can be handled by masked load/store (<code>vifelse</code>, masked stores) <em>or</em> by packing the operand into a padded buffer so the dense kernel runs unmasked. PureBLAS uses both but leans toward <strong>packing/padding</strong> where masking would cost throughput — for instance packing a triangular diagonal block dense-with-zeros so the ordinary microkernel multiplies it (the OpenBLAS scheme), or padding a ragged column stripe to the panel width. Masked <em>vector stores</em> on diagonal-straddling tiles were measured slower than dense-compute-to-scratch plus a scalar triangular copy-back, and direct (unpacked) reads must clamp to valid bounds to stay inside the allocation.</p><h2 id="Complex-numbers" tabindex="-1">Complex numbers <a class="header-anchor" href="#Complex-numbers" aria-label="Permalink to &quot;Complex numbers {#Complex-numbers}&quot;">​</a></h2><p>Complex kernels stay in the <strong>interleaved</strong> <code>[re, im, re, im, …]</code> domain and use <strong>swap-adjacent</strong> shuffles (<code>shufflevector</code>) for the <code>i·i = -1</code> cross terms, rather than deinterleaving real and imaginary parts across lanes. This keeps the data layout contiguous and avoids cross-lane permutations, and it is expressed entirely in portable SIMD.jl — no x86 shuffle intrinsics. The same <code>Vec{N,Complex{T}}</code>-free formulation covers <code>c</code> and <code>z</code>.</p><h2 id="Contrast-with-ahead-of-time-BLAS-OpenBLAS,-faer" tabindex="-1">Contrast with ahead-of-time BLAS (OpenBLAS, faer) <a class="header-anchor" href="#Contrast-with-ahead-of-time-BLAS-OpenBLAS,-faer" aria-label="Permalink to &quot;Contrast with ahead-of-time BLAS (OpenBLAS, faer) {#Contrast-with-ahead-of-time-BLAS-OpenBLAS,-faer}&quot;">​</a></h2><p>The <em>kernel</em> philosophy is shared with modern portable-SIMD libraries — faer, via <a href="https://github.com/sarah-quinones/pulp" target="_blank" rel="noreferrer">pulp</a>, writes one generic kernel over a <code>Simd</code> trait with register-blocked multiple accumulators, much as PureBLAS writes one kernel over <code>Vec{N,T}</code>. The deep difference is <strong>when specialization happens</strong>:</p><ul><li><p><strong>AOT (Rust/C):</strong> the library is compiled before it knows the target. To cover a range of CPUs it must carry several ISA variants and select one at runtime, and its block-size tuning is a static table baked at the library&#39;s compile time. It cannot re-derive tuning for a CPU it was never built or benchmarked for.</p></li><li><p><strong>JIT-to-host (PureBLAS):</strong> Julia specializes the kernel to the <em>one</em> machine at load time. There is no runtime ISA dispatch and no multi-versioning — and, crucially, tuning is <em>computed</em> from the detected cache/ISA of the actual host, so an unbenchmarked CPU still gets sized blocks.</p></li></ul><p>The practical consequence is that any faer-versus-PureBLAS performance gap on a given machine is a matter of library maturity and tuning, not of language: Julia&#39;s LLVM backend generates numerical code competitive with Rust&#39;s, and JIT-to-host is the more flexible substrate for hardware adaptation, not a handicap.</p><p>A note on scope: faer additionally <em>guarantees</em> bit-reproducible results across alignment offsets on a machine (by rotating reduction accumulators). PureBLAS does not currently make that guarantee; its numerical-correctness budget goes to overflow-safe accumulation (<code>nrm2</code> via LAPACK <code>lassq</code>) instead.</p>`,30)])])}const u=t(n,[["render",o]]);export{k as __pageData,u as default};
