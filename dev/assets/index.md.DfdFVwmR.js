import{_ as a,o as s,c as i,ao as t}from"./chunks/framework.CJX_rJCp.js";const k=JSON.parse('{"title":"PureBLAS.jl","description":"","frontmatter":{},"headers":[],"relativePath":"index.md","filePath":"index.md","lastUpdated":null}'),n={name:"index.md"};function r(o,e,h,l,d,c){return s(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="PureBLAS.jl" tabindex="-1">PureBLAS.jl <a class="header-anchor" href="#PureBLAS.jl" aria-label="Permalink to &quot;PureBLAS.jl {#PureBLAS.jl}&quot;">​</a></h1><p>A BLAS and LAPACK written entirely in Julia, part of the Pure Julia Ecosystem — pure-Julia replacements for Julia&#39;s non-Julia default libraries (sibling: <a href="https://github.com/el-oso/PureFFT.jl" target="_blank" rel="noreferrer">PureFFT.jl</a>).</p><p>Everything is plain Julia source built on <a href="https://github.com/eschnett/SIMD.jl" target="_blank" rel="noreferrer">SIMD.jl</a>. There is no <code>libopenblas</code>, no Fortran and no vendor blob anywhere in the stack.</p><h2 id="The-performance-rule" tabindex="-1">The performance rule <a class="header-anchor" href="#The-performance-rule" aria-label="Permalink to &quot;The performance rule {#The-performance-rule}&quot;">​</a></h2><p>PureBLAS has to be at least as fast as <code>max(OpenBLAS, AOCL)</code> at every size, on every microarchitecture. Not a geometric mean, not &quot;competitive with&quot; — the gate looks at the <em>worst</em> cell of a routine against the <em>faster</em> of the two references, and the routine passes only if that worst cell reaches parity.</p><p>Measurements run single-threaded on frequency-locked machines (<code>amd_pstate=passive</code>, boost off, cores pinned to base clock) across Zen3/AVX2, Zen4/AVX-512 and Zen5/AVX-512. Both references are measured in the same round as PureBLAS, so the <code>max</code> compares numbers that saw one machine state rather than three.</p><p>The <a href="./coverage">Coverage</a> page has the per-routine results, generated from the benchmark caches rather than typed in by hand — including every routine that currently misses.</p><h2 id="Two-ways-to-use-it" tabindex="-1">Two ways to use it <a class="header-anchor" href="#Two-ways-to-use-it" aria-label="Permalink to &quot;Two ways to use it {#Two-ways-to-use-it}&quot;">​</a></h2><p>Call the native API directly, and you are calling ordinary Julia functions with no <code>ccall</code> boundary in the way:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PureBLAS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PureBLAS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">axpy!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(y, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x)        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># y .+= 2x</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PureBLAS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, y)               </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># conjugated inner product</span></span></code></pre></div><p>Or hand the whole ecosystem over. <code>PureBLAS.activate()</code> registers in-process <code>@cfunction</code> pointers to the native <code>@ccallable</code> kernels — 492 symbols — after which <code>A*B</code>, <code>mul!</code>, <code>cholesky</code>, <code>qr</code>, <code>svd</code>, <code>eigen</code> and <code>LinearAlgebra.BLAS.*</code> all land in PureBLAS. <code>deactivate()</code> puts OpenBLAS back.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> PureBLAS, LinearAlgebra</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PureBLAS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">activate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">F </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> cholesky</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A)                  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → PureBLAS potrf</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">U, S, V </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> svd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(B)                 </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → PureBLAS gesdd</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">PureBLAS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">deactivate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><p>Those same <code>@ccallable</code> symbols compile to <code>libpureblas.so</code> through <code>juliac --trim</code>, so a C, C++ or Rust program can link PureBLAS as its BLAS backend. Keeping that build working also keeps the kernels trim-compatible, which is a useful thing to have checked continuously.</p><h2 id="What-is-covered" tabindex="-1">What is covered <a class="header-anchor" href="#What-is-covered" aria-label="Permalink to &quot;What is covered {#What-is-covered}&quot;">​</a></h2><p>One generic kernel set handles all four element types — <code>Float32</code>, <code>Float64</code>, <code>ComplexF32</code> and <code>ComplexF64</code>. Every benchmark run measures the full BLAS levels 1 through 3 plus a large slice of LAPACK: Cholesky in its dense, banded, packed and pivoted forms, LU, QR with and without pivoting, Bunch–Kaufman, triangular and general solves, least squares, SVD, and the symmetric, Hermitian and general eigensolvers. <a href="./coverage">Coverage</a> lists them with current numbers.</p><p>Results are checked against OpenBLAS and LAPACK across types, sizes, strides and edge cases, and StrictMode contracts (<code>@assert_typestable</code>, <code>@assert_noalloc</code>, trim-safety) run against the hot paths as part of the suite.</p><h2 id="Differentiation" tabindex="-1">Differentiation <a class="header-anchor" href="#Differentiation" aria-label="Permalink to &quot;Differentiation {#Differentiation}&quot;">​</a></h2><p>Forward mode works today. The kernels are generic, so a <code>ForwardDiff.Dual</code> passes straight through them and the suite differentiates a dozen routines against known derivatives. It is not fast: the SIMD kernels only fire for <code>Float32</code> and <code>Float64</code>, so a <code>Dual</code> drops to plain scalar loops and you get the right answer slowly.</p><p>Reverse mode is not implemented. The plan is ChainRules and Enzyme rules that call the ordinary <code>Float64</code> kernel at full speed and express the adjoint as further BLAS calls — differentiation at BLAS speed rather than element by element. That work has not started.</p><h2 id="How-it-adapts-to-your-machine" tabindex="-1">How it adapts to your machine <a class="header-anchor" href="#How-it-adapts-to-your-machine" aria-label="Permalink to &quot;How it adapts to your machine {#How-it-adapts-to-your-machine}&quot;">​</a></h2><p>PureBLAS detects the host when it precompiles (<code>CpuId</code>/<code>HostCPUFeatures</code>) and bakes cache sizes, SIMD width and microarchitecture into const-folded, trim-safe constants. Every tuning parameter — block sizes, base-case cutoffs, unroll factors, algorithm-switch thresholds — then resolves through what the project calls the PDM ladder. A <code>Preferences</code> <strong>P</strong>in wins if one is set. Otherwise the default is either <strong>D</strong>erived, as a formula over those detected constants, or <strong>M</strong>easured on the host by a one-shot auto-tune, which is reserved for the cases where the optimum depends on a ratio of our own kernels&#39; rates that no formula predicts. A fixed per-microarchitecture literal counts as a defect rather than a tuning, and there is a lint that fails the build over it. <a href="./tuning">Tuning Constants</a> goes into detail.</p><p>This is the one structural advantage a JIT-compiled BLAS has over a statically shipped one, and much of the point of the project. OpenBLAS and BLIS have to choose their block sizes when <em>they</em> are compiled; PureBLAS chooses yours when it loads.</p><p>MIT licensed. See the <a href="./guide">Guide</a>, <a href="./design">Design</a> and <a href="./simd">SIMD &amp; Hardware Adaptation</a>.</p>`,23)])])}const u=a(n,[["render",r]]);export{k as __pageData,u as default};
