import{_ as o,o as t,c as a,ao as r}from"./chunks/framework.BSzmyLFO.js";const p=JSON.parse('{"title":"Design","description":"","frontmatter":{},"headers":[],"relativePath":"design.md","filePath":"design.md","lastUpdated":null}'),n={name:"design.md"};function i(d,e,c,s,l,u){return t(),a("div",null,[...e[0]||(e[0]=[r('<h1 id="Design" tabindex="-1">Design <a class="header-anchor" href="#Design" aria-label="Permalink to &quot;Design {#Design}&quot;">​</a></h1><h2 id="One-kernel-set,-two-consumption-modes" tabindex="-1">One kernel set, two consumption modes <a class="header-anchor" href="#One-kernel-set,-two-consumption-modes" aria-label="Permalink to &quot;One kernel set, two consumption modes {#One-kernel-set,-two-consumption-modes}&quot;">​</a></h2><p>PureBLAS has a single set of low-level Level-1 kernels in BLAS-native <code>(n, …, inc)</code> form, written over a tiny accessor interface (<code>_ld</code>/<code>_st!</code>) that works uniformly over <code>Ptr{T}</code> (C-ABI) and <code>AbstractVector{T}</code> (native). Two layers sit on top:</p><ul><li><p><strong>Mode 2 — native API</strong> (<code>backend.jl</code>, <code>native.jl</code>): ergonomic <code>AbstractVector</code> methods on a <code>SIMDBackend &lt;: AbstractBLAS1</code> (a TypeContracts interface). No <code>ccall</code> boundary, so the whole call tree is plain Julia and <strong>differentiable</strong>.</p></li><li><p><strong>Mode 1 — C ABI</strong> (<code>cabi.jl</code>): <code>@ccallable</code> ILP64 reference-BLAS symbols (<code>daxpy_64_</code>, by reference, column-major, <code>Int64</code>). BLAS-1 has no character arguments, so there are no hidden Fortran string-length args. Compiled to <code>libpureblas.so</code> by <code>juliac --trim</code>.</p></li></ul><h2 id="Generic-over-T:Number" tabindex="-1">Generic over <code>T&lt;:Number</code> <a class="header-anchor" href="#Generic-over-T:Number" aria-label="Permalink to &quot;Generic over `T&lt;:Number` {#Generic-over-T:Number}&quot;">​</a></h2><p>One kernel implementation covers <code>s/d/c/z</code> and any other <code>T&lt;:Number</code>. Real, unit-stride, dense inputs dispatch to a SIMD.jl path (<code>Vec{N,T}</code> at the detected register width — AVX-512 / AVX2 / NEON); everything else uses the generic scalar loop. The generic path is what lets <code>ForwardDiff.Dual</code> flow through.</p><h2 id="Why-LBT-forwarding-into-live-Julia-does-not-work-yet" tabindex="-1">Why LBT forwarding into live Julia does not work (yet) <a class="header-anchor" href="#Why-LBT-forwarding-into-live-Julia-does-not-work-yet" aria-label="Permalink to &quot;Why LBT forwarding into live Julia does not work (yet) {#Why-LBT-forwarding-into-live-Julia-does-not-work-yet}&quot;">​</a></h2><p>A <code>juliac --trim</code> library <strong>embeds the Julia runtime</strong>. Its <code>@ccallable</code> entry points are wrapped with <code>ijl_autoinit_and_adopt_thread</code>, which lazily initializes that runtime on first call — perfect for a non-Julia host. But <code>BLAS.lbt_forward</code> from inside a running Julia process makes LBT call a probe symbol (<code>isamax_64_</code>) during interface autodetection, which <strong>double-initializes the shared <code>libjulia</code></strong> and aborts (signal 6).</p><p>Therefore:</p><ul><li><p>Inside Julia, use <strong>Mode 2</strong> (native API / pkgimage) — also the AD-enabling path.</p></li><li><p>The <code>.so</code> is for <strong>non-Julia consumers</strong> and for proving <strong>trim-compatibility</strong> (the build succeeds and exports all 30 BLAS-1 symbols; a C host runs it correctly — <code>juliac/ctest.c</code>).</p></li><li><p>Re-enabling LBT forwarding needs upstream juliac support for initializing against the host runtime (or a runtime-free codegen path). Tracked in <code>ROADMAP.md</code>.</p></li></ul>',10)])])}const m=o(n,[["render",i]]);export{p as __pageData,m as default};
