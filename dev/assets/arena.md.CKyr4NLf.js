import{_ as t,o as a,c as s,ao as i}from"./chunks/framework.CJX_rJCp.js";const g=JSON.parse('{"title":"The scratch arena","description":"","frontmatter":{},"headers":[],"relativePath":"arena.md","filePath":"arena.md","lastUpdated":null}'),n={name:"arena.md"};function o(r,e,l,h,d,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="The-scratch-arena" tabindex="-1">The scratch arena <a class="header-anchor" href="#The-scratch-arena" aria-label="Permalink to &quot;The scratch arena {#The-scratch-arena}&quot;">​</a></h1><p>Every LAPACK routine needs scratch. PureBLAS used to give each one a named field on a struct: <code>L3Workspace</code> reached <strong>180 fields</strong>, most of them dedicated to a single routine — <code>trsen11</code>, <code>qp3wrow</code>, <code>ggs_rqtau</code>, <code>stnseed</code>. That shape has three costs beyond the ugliness. Every call site drags in scratch for routines it can never reach. The peak footprint is the sum of each role&#39;s maximum rather than the maximum of what is concurrently live. And it forces one sharing policy on every role, which is the wrong shape for the multithreading milestone.</p><p>The arena replaces it. A role does not own storage; it <strong>borrows</strong> a shape and stride for the duration of a lexical scope, and the bump pointer rewinds when the scope exits.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@scope</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> arn </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">begin</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    W </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> borrow!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(arn, T, n, nb)          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># n×nb, leading dimension n</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    v </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> borrow!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(arn, Int, n)            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># a vector</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    _panel!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(A, W, v)                    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># handles pass DOWN freely</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                     # rewound here; W and v are dead</span></span></code></pre></div><p><code>L3Workspace</code> is now <strong>7 fields</strong> — the GEMM and syr2k packing buffers, which are genuinely shared allocator state rather than per-routine scratch.</p><h2 id="How-it-works" tabindex="-1">How it works <a class="header-anchor" href="#How-it-works" aria-label="Permalink to &quot;How it works {#How-it-works}&quot;">​</a></h2><p>The arena is a byte-addressed bump allocator over one slab. Entering a scope records the current position; leaving restores it. Nothing is freed individually and nothing is reference-counted.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sequenceDiagram</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant C as caller</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant S as &quot;@scope&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant A as Arena</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C-&gt;&gt;S: enter</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    S-&gt;&gt;A: record (slab, offset, depth)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C-&gt;&gt;A: borrow!(T, m, n)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A--&gt;&gt;C: PtrMatrix at offset, offset += ld·n·sizeof(T)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C-&gt;&gt;A: borrow!(T, k)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A--&gt;&gt;C: PtrVector, offset advances again</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Note over C: kernels run; handles pass into callees</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C-&gt;&gt;S: exit</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    S-&gt;&gt;A: restore (slab, offset, depth) — both handles now dangle</span></span></code></pre></div><p>Because a scope restores an absolute position rather than undoing individual borrows, an <em>enclosing</em> scope&#39;s exit repairs anything an inner one failed to release. That property is what makes the handler-free variant below safe.</p><h3 id="Growth" tabindex="-1">Growth <a class="header-anchor" href="#Growth" aria-label="Permalink to &quot;Growth {#Growth}&quot;">​</a></h3><p>If a borrow does not fit, the arena takes a <strong>new slab and keeps the old one live</strong>, so growth never invalidates a handle already handed out. At depth zero the chain folds back into a single slab sized to the high-water demand.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart LR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A[&quot;borrow! wants N bytes&quot;] --&gt; B{&quot;fits in the current slab?&quot;}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B -- yes --&gt; C[&quot;bump the offset&lt;br/&gt;return a handle&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B -- no --&gt; D[&quot;allocate a new slab&lt;br/&gt;KEEP the old one live&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    D --&gt; E[&quot;record the high-water demand&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E --&gt; C</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C --&gt; F{&quot;scope exits at depth 0?&quot;}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    F -- no --&gt; G[&quot;restore the saved position&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    F -- yes --&gt; H[&quot;fold the chain into ONE slab&lt;br/&gt;sized to the high-water DEMAND&quot;]</span></span></code></pre></div><p>Folding to the <em>demand</em> rather than to the sum of slab capacities matters: summing capacities compounds the growth doubling into the permanent size, so a slab of <code>S</code> overflowed by one byte becomes <code>2S</code>, folds to <code>3S</code>, and the next one-byte overflow gives <code>9S</code>, without bound.</p><h3 id="Handles" tabindex="-1">Handles <a class="header-anchor" href="#Handles" aria-label="Permalink to &quot;Handles {#Handles}&quot;">​</a></h3><p><code>borrow!</code> returns <code>PtrMatrix{T}</code> or <code>PtrVector{T}</code> (<code>src/ptrmat.jl</code>) — isbits structs holding a pointer, a shape and a leading dimension. They index, <code>view</code>, and pass into kernels like an <code>Array</code>, and being isbits they cross a non-inlined call boundary without a heap box. A non-isbits element type such as <code>BigFloat</code> falls back to a heap <code>Matrix</code>, which is correct but not allocation-free; no gated path uses one.</p><p><strong>Slicing keeps the type.</strong> <code>view(A, rows, cols)</code> on a <code>PtrMatrix</code> is another <code>PtrMatrix</code>; a column view is a <code>PtrVector</code>. That is load-bearing — see <em>the closed-union trap</em> below.</p><h2 id="What-the-macro-enforces,-and-when" tabindex="-1">What the macro enforces, and when <a class="header-anchor" href="#What-the-macro-enforces,-and-when" aria-label="Permalink to &quot;What the macro enforces, and when {#What-the-macro-enforces,-and-when}&quot;">​</a></h2><p>Three rules are checked when <code>@scope</code> <strong>expands</strong>, so a violation is a compile error rather than a lint finding or a debugging session.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    M[&quot;@scope arn begin … end&quot;] --&gt; R1{&quot;does the TOKEN appear&lt;br/&gt;anywhere but as borrow!&#39;s&lt;br/&gt;first argument?&quot;}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    R1 -- yes --&gt; E1[&quot;error: the token escapes&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    R1 -- no --&gt; R2{&quot;is there a borrow! inside a&lt;br/&gt;for / while / comprehension /&lt;br/&gt;closure / do in this block?&quot;}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    R2 -- yes --&gt; E2[&quot;error: a borrow in a loop&lt;br/&gt;consumes Σ(iterations)&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    R2 -- no --&gt; R3{&quot;is a borrowed HANDLE returned&lt;br/&gt;from the block, or its tail value?&quot;}</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    R3 -- yes --&gt; E3[&quot;error: the handle outlives&lt;br/&gt;the bytes it points at&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    R3 -- no --&gt; OK[&quot;expand&quot;]</span></span></code></pre></div><p>The third rule is deliberately narrow: it rejects a handle in <em>value position</em> only. <code>return sum(A)</code> is normal and stays legal, and so does passing a handle down into a callee — handles are meant to travel, they are only forbidden from outliving.</p><h3 id="What-the-checks-do-not-catch" tabindex="-1">What the checks do not catch <a class="header-anchor" href="#What-the-checks-do-not-catch" aria-label="Permalink to &quot;What the checks do not catch {#What-the-checks-do-not-catch}&quot;">​</a></h3><p>They are lexical checks on the unexpanded body, not a proof. Three holes, all measured:</p><table tabindex="0"><thead><tr><th style="text-align:right;">hole</th><th style="text-align:right;">why it cannot be closed lexically</th></tr></thead><tbody><tr><td style="text-align:right;">a handle <strong>stored</strong> into a field or global, or captured by a closure that outlives the block</td><td style="text-align:right;">the handle is an ordinary local; only the token is tracked</td></tr><tr><td style="text-align:right;">a loop emitted by <strong>another macro</strong></td><td style="text-align:right;">the body is walked unexpanded, where <code>@turbo</code>/<code>@nloops</code> is a <code>macrocall</code> node, not a <code>for</code></td></tr><tr><td style="text-align:right;"><strong>recursion</strong></td><td style="text-align:right;">not lexical, so no macro can see it — a self-recursive routine holding a scope per level holds every level&#39;s borrows at once</td></tr></tbody></table><p>For the first, <code>@fenced_scope</code> is the runtime answer: every borrow becomes its own <code>mmap</code> behind a <code>PROT_NONE</code> guard page, so <em>using</em> a released handle faults at the offending line instead of silently reading whatever the next borrow wrote. It is roughly a thousand times the cost of a bump, so it is opt-in per scope — written into the source of the routine being debugged, never a global mode.</p><h2 id="@scope-or-@leafscope" tabindex="-1"><code>@scope</code> or <code>@leafscope</code> <a class="header-anchor" href="#@scope-or-@leafscope" aria-label="Permalink to &quot;\`@scope\` or \`@leafscope\` {#@scope-or-@leafscope}&quot;">​</a></h2><p><code>@scope</code> wraps its body in <code>try</code>/<code>finally</code> so the arena is released on all three exit paths: falling off the end, an early <code>return</code>, and a throw. That handler is free in a dispatch-level routine and <strong>ruinous in one that inlines a register-hungry kernel</strong>, because it lowers to <code>jl_enter_handler</code> plus a <code>returns_twice</code> setjmp, and LLVM must then be conservative about registers for the whole function.</p><p>Measured on Zen 3, where the SIMD leaf holds 18 live vectors against a 16-register file:</p><table tabindex="0"><thead><tr><th style="text-align:right;"></th><th style="text-align:right;">vector spills in <code>_trsm_rl_fused_drv!</code></th><th style="text-align:right;"><code>trsmR@100</code></th><th style="text-align:right;"><code>trsmR@128</code></th></tr></thead><tbody><tr><td style="text-align:right;">before the arena</td><td style="text-align:right;">113</td><td style="text-align:right;">1.037</td><td style="text-align:right;">0.978</td></tr><tr><td style="text-align:right;">with <code>@scope</code> (handler)</td><td style="text-align:right;"><strong>172</strong></td><td style="text-align:right;">0.746</td><td style="text-align:right;">0.708</td></tr><tr><td style="text-align:right;">with <code>@leafscope</code></td><td style="text-align:right;"><strong>113</strong></td><td style="text-align:right;">1.035</td><td style="text-align:right;">0.973</td></tr></tbody></table><p>The AVX-512 machines have fourteen spare vector registers and were unaffected — which is why the whole effect was invisible on the machine the conversion was written on.</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Q{&quot;does this function inline&lt;br/&gt;a SIMD kernel?&quot;} -- no --&gt; S[&quot;@scope&lt;br/&gt;keeps the throw guarantee,&lt;br/&gt;costs nothing here&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Q -- yes --&gt; L[&quot;@leafscope&lt;br/&gt;no exception handler&quot;]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    L --&gt; N[&quot;requires a @scope somewhere&lt;br/&gt;ABOVE it in the call chain&quot;]</span></span></code></pre></div><p><code>@leafscope</code> reproduces the first two exit paths itself — it rewrites every <code>return</code> in the block to release first, skipping any <code>return</code> belonging to a nested closure or <code>do</code> block. It gives up only the throw path, and that is bounded rather than ignored: releasing restores the arena&#39;s position <em>absolutely</em> from the record made on entry, so the first enclosing scope that does run its exit repairs everything at once. Hence the rule in the diagram.</p><h2 id="The-closed-union-trap" tabindex="-1">The closed-union trap <a class="header-anchor" href="#The-closed-union-trap" aria-label="Permalink to &quot;The closed-union trap {#The-closed-union-trap}&quot;">​</a></h2><p><code>StridedMatrix</code> and <code>StridedVector</code> are <strong>closed</strong> unions — <code>Array</code>, <code>SubArray</code>, <code>ReshapedArray</code>, <code>ReinterpretArray</code> and combinations. <code>PtrMatrix</code> and <code>PtrVector</code> are not in them and never can be. So a fast-path gate written inline as</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">isa</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> StridedVector </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&amp;&amp;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> stride</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # WRONG for a borrow</span></span></code></pre></div><p>sends every borrowed operand to the generic scalar path: right answer, no test failure, no gate cell moves. Use <strong><code>_strided1(A)</code></strong> and <strong><code>_dense1(x)</code></strong> (<code>src/ptrmat.jl</code>), which const-fold to the identical check for a Strided argument and carry explicit methods for the pointer types. <code>test/fastpath_lint.jl</code> fails the suite on a new bare <code>isa</code>.</p><p>This class had fired five times before the lint existed — twice through the C-ABI shims, twice through the conversion, and once in a way that sent <strong>every</strong> drop-in <code>getrf</code> down a scalar panel.</p><h2 id="Costs,-stated-plainly" tabindex="-1">Costs, stated plainly <a class="header-anchor" href="#Costs,-stated-plainly" aria-label="Permalink to &quot;Costs, stated plainly {#Costs,-stated-plainly}&quot;">​</a></h2><ul><li><p>A converted routine is <strong>not</strong> allocation-free on its very first call at a new arena high-water; it is allocation-free from the second. The struct paid that cost at module load instead.</p></li><li><p>Two borrows in one scope are two disjoint ranges, so the self-aliasing bug class the old fields kept producing — a role re-claimed from inside a live claim of itself, right on the first call and wrong on the second — cannot be written any more.</p></li><li><p>A borrow is <strong>exact</strong>. A grown field handed back whatever leading dimension an earlier call had asked for, which is a measured 1.16 → 0.57 hazard: an ascending benchmark sweep hides it, and a caller going large-then-small gets a different library.</p></li></ul><h2 id="Threading" tabindex="-1">Threading <a class="header-anchor" href="#Threading" aria-label="Permalink to &quot;Threading {#Threading}&quot;">​</a></h2><p>The arena is a process-global bump allocator, exactly as the struct it replaces was a process-global object. That is not a regression in kind, but it is one in degree: interleave two tasks and the failure is arbitrary cross-role aliasing, where the struct&#39;s was bounded to one role.</p><p>A per-task owner is therefore a <strong>precondition</strong> of enabling threads, not an optimisation to weigh against its cost. It is also why the conversion was worth doing before that milestone: going per-task costs the arena one line, where per-task ownership of a 180-field struct means duplicating 180 grown buffers per task.</p>`,41)])])}const k=t(n,[["render",o]]);export{g as __pageData,k as default};
