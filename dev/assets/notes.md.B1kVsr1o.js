import{_ as t,o as a,c as o,ao as r}from"./chunks/framework.CJX_rJCp.js";const m=JSON.parse('{"title":"Performance notes","description":"","frontmatter":{},"headers":[],"relativePath":"notes.md","filePath":"notes.md","lastUpdated":null}'),s={name:"notes.md"};function n(d,e,i,c,l,h){return a(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="Performance-notes" tabindex="-1">Performance notes <a class="header-anchor" href="#Performance-notes" aria-label="Permalink to &quot;Performance notes {#Performance-notes}&quot;">​</a></h1><p>Per-routine analysis behind the numbers on <a href="./performance">Performance</a> and <a href="./coverage">Coverage</a>. Those pages are the source of truth for what the numbers are; this page is about why they are what they are, and what is still open. How the measurements are produced and how to read a cell: <a href="./methodology">Methodology</a>.</p><p>The ratios quoted below come from the specific experiment being described, on the date stated, and they are hand-written rather than generated. Where one disagrees with the generated tables, the tables are right — they are rebuilt from the caches on every publish, and this page is not.</p><h2 id="Real-BLAS" tabindex="-1">Real BLAS <a class="header-anchor" href="#Real-BLAS" aria-label="Permalink to &quot;Real BLAS {#Real-BLAS}&quot;">​</a></h2><p><strong>BLAS-1</strong> is bandwidth-bound and at parity fleet-wide (worst sizes ≥ 0.98, except <code>iamax</code> on AVX2 at 0.95). <code>nrm2</code> runs 7–10× because OpenBLAS uses the always-scaled LAPACK algorithm, while PureBLAS scales only on overflow or underflow.</p><p><strong>BLAS-2</strong> gates fleet-wide with two exceptions, both on Zen5: <code>gemvN</code> (mid-n native-512 residual, worst 0.90) and <code>trmv</code>/<code>trsv</code> at large n (DRAM regime, worst 0.98–0.99). <code>spmv</code> is flat at 1.9–2.2 across the fleet from an AP-residency packed panel, and <code>ger</code> sits at gate on all three boxes (worst 0.97–1.06) with a per-µarch-calibrated write-stream count.</p><p><strong>BLAS-3.</strong> <code>gemm</code> gates at every size on all three boxes, with Strassen–Winograd running 1.2–1.4× at large n. The triangular and symmetric ops gate on AVX-512; on AVX2 the worst size of <code>trmm</code> (0.84) is still open, while <code>trsm</code> gates against OpenBLAS at every size on both available boxes (AVX2 worst 1.14).</p><h3 id="Complex-gemv-T/C-is-an-ISA-split" tabindex="-1">Complex <code>gemv</code>-T/C is an ISA split <a class="header-anchor" href="#Complex-gemv-T/C-is-an-ISA-split" aria-label="Permalink to &quot;Complex `gemv`-T/C is an ISA split {#Complex-gemv-T/C-is-an-ISA-split}&quot;">​</a></h3><p>Past L2 these route to a column-block whose width comes from a Measure-tier knob bounded by the register file: <code>regs = (2·NC + 2)·(HALF ? 1 : 2)</code>. On AVX-512 that admits <code>NC = 8</code> narrow accumulators — eight concurrent column streams, matching what AOCL&#39;s fused <code>zdotxf</code> runs — and <code>zgemvT</code> gates at every size on Zen4 (n=512 0.96→1.00, n=1024 0.91→1.02 vs AOCL). On AVX2 the same formula wants 18 vector registers against the 16 available, so Zen3 keeps <code>NC = 4</code> and its mid-band misses stand: <code>zgemvT</code> 0.95–0.98 and <code>zgemvC</code> 0.94–0.96 vs AOCL at n=512…2048. That is a register-file limit rather than an unexplored lever. Closing it needs a different accumulator shape, not a wider block.</p><p>The split below L2 is separate, and derived rather than tuned: while A is L2-resident the loop is FMA-latency-bound and prefers fewer, wider accumulators (measured at n=256, 1.15 wide against 1.04 narrow), so the wide arm stays there on both ISAs.</p><h3 id="The-BLAS-3-conversion-gap" tabindex="-1">The BLAS-3 conversion gap <a class="header-anchor" href="#The-BLAS-3-conversion-gap" aria-label="Permalink to &quot;The BLAS-3 conversion gap {#The-BLAS-3-conversion-gap}&quot;">​</a></h3><p><code>gemm</code> is the fastest thing on all three boxes and pulls further ahead as <code>n</code> grows, yet the routines built on that same engine fall behind AOCL at exactly the sizes where <code>gemm</code> wins most:</p><table tabindex="0"><thead><tr><th style="text-align:right;">vs max(OpenBLAS, AOCL)</th><th style="text-align:right;">n=2048</th><th style="text-align:right;">n=4096</th></tr></thead><tbody><tr><td style="text-align:right;"><code>gemm</code> — Zen4 / Zen3 / Zen5</td><td style="text-align:right;">1.150 / 1.150 / 1.168</td><td style="text-align:right;"><strong>1.252 / 1.280 / 1.308</strong></td></tr><tr><td style="text-align:right;"><code>syrk</code></td><td style="text-align:right;">0.955 / 0.963 / 0.978</td><td style="text-align:right;"><strong>0.937 / 0.968 / 0.953</strong></td></tr><tr><td style="text-align:right;"><code>syr2k</code></td><td style="text-align:right;">0.976 / 0.974 / 0.977</td><td style="text-align:right;">0.966 / 0.983 / 0.957</td></tr></tbody></table><p><code>syrk</code>, <code>syr2k</code>, <code>symm</code>, <code>trmm</code> and <code>trsm</code> all route their trailing updates through the same <code>_gemm_core!</code> / <code>_microkernel_db!</code> engine. That engine is 25–31% faster than AOCL at n=4096 on this silicon, so the roughly 30-point spread between it and the routines built on it is a conversion loss rather than a hardware limit. It reproduces on three microarchitectures, which makes it structural rather than a per-box tuning artifact, and it is the largest shared lever currently on the board.</p><p><code>trsm</code>&#39;s residual sits at mid-<code>n</code> (Zen4 0.941 at n=128), and it is decomposed rather than guessed. Fitting the leaf&#39;s rate as <code>1/GF = α + β/KC</code> puts the asymptotic microkernel rate at 44.4 GF, above PureBLAS&#39;s own dgemm at 43.75, so the inner loop is not the problem. What is left is an 18.6% per-slab fixed cost at KC=128, and an opcode histogram of the emitted slab prices it: scalar addressing about 22% and vector moves about 24%, against only ~3.2% of total leaf time for the two 8×8 transposes and ~2.8% for the back-substitution arithmetic. Removing the largest addressing population — recomputed <code>(jc + c)·ldb</code> products — was worth 3.0% at n=128, bit-identically. Zen5&#39;s <code>trsm</code> column still reads 0.91 because that box has not been re-swept since the tiny-<code>n</code> fixes landed.</p><p><code>syrk</code>/<code>syr2k</code> at n=4096 is the cross-µarch invariant above. By contrast <code>symm</code> ≥ 256 and <code>trsmR</code> ≥ 2048 miss on Zen4 and Zen3 but gate on Zen5, so those are µarch-specific.</p><p><code>trmm</code>&#39;s crossover between recursion-over-<code>gemm!</code> and the packed routine is derived as <code>5·_GEMM_UNPACK_MAX ÷ 2</code> rather than borrowed from gemm&#39;s own unpacked/blocked cut. In a controlled same-process A/B the two sizes whose route changes gained 4.0% and 5.1% (n=512 and n=1024), which moved the worst cell to n=2048 — whose miss lies in the packed path itself and is a separate defect.</p><h2 id="Real-LAPACK" tabindex="-1">Real LAPACK <a class="header-anchor" href="#Real-LAPACK" aria-label="Permalink to &quot;Real LAPACK {#Real-LAPACK}&quot;">​</a></h2><p><code>potrf</code>/<code>geqrf</code>/<code>getrf</code>/<code>gesvd</code> gate on all three boxes, with geomeans of 1.25–1.52. The small-n <code>potrf</code> work — block-small Cholesky plus a fused 12-accumulator <code>trsm</code>-R that does the downdate and triangular solve in one register pass, in both the small-n and NB=128 panel drivers — brings AVX2 <code>potrf</code> to BLASFEO parity, the MKL proxy: 0.91–1.04× its column-major <code>dpotrf</code> at n≤224, 0.87–0.91× at n≥256, and 1.5–2.2× against OpenBLAS fleet-wide.</p><h3 id="Cholesky,-upper-triangle" tabindex="-1">Cholesky, upper triangle <a class="header-anchor" href="#Cholesky,-upper-triangle" aria-label="Permalink to &quot;Cholesky, upper triangle {#Cholesky,-upper-triangle}&quot;">​</a></h3><p><code>potrf</code> gates for both triangles. The upper path is a different route rather than a mirror image — transpose into a padded scratch, factor with the gating lower kernels, transpose back — and its large-n miss came from the same power-of-two-<code>lda</code> cache-set aliasing the factorizations already defend against, this time inside the copy. The transpose read <code>A</code> strided across a row at its raw <code>lda</code>, and at <code>lda·sizeof(T) % 4096 == 0</code>, precisely the benchmarked sizes, every column maps into one L1 set group. Reordering the loops so the contiguous side is the read and the strided side is the non-power-of-two scratch is bit-identical and worth 2.1–3.8× on the copy, taking real <code>potrfU</code> from 0.96 to 1.03 vs AOCL on Zen4 and 0.98 to 1.04 on Zen3.</p><p>The upper path now clears both references at every size on Zen4 (worst 1.11 vs OpenBLAS, 1.03 vs AOCL) and on Zen3 (worst 1.06). The single residual is Zen5 n=2048 at 0.970, a 3.0% gap against a within-run spread of 0.002 — small, but well clear of its noise, so it is a real cell rather than a rounding artifact.</p><p>Complex <code>zpotrfU</code> still sits at 0.95 for <code>n ≥ 1024</code> on Zen4, where it gates on Zen3. The two transposes are the entire remaining gap — remove them and PureBLAS would be 3.4% ahead of AOCL — but they cannot simply be deleted, because factoring the upper triangle natively measures slower than the transpose route at every size and type.</p><p>At <code>n = 32</code> the same route was losing for a second, independent aliasing reason, and it is worth keeping because the predicate involved is easy to write again. The base kernel stages its block through a contiguous scratch when <code>A isa SubArray &amp;&amp; stride(A,2) &gt; n</code>, which tests for the <em>existence</em> of a stride where the staging is an <em>aliasing</em> remedy — worth 2× at <code>lda</code> = 512 or 1024, and a 1.6× cost at <code>lda</code> = 520. The upper route hands it a view of a padded scratch whose leading dimension is deliberately kept off the way-stride, so the copy round-trip could never pay there. Replacing the predicate with the byte-scaled way-stride test the padding helper already derives, adding no new tuning constant, took <code>zpotrfU@32</code> to 0.993 on Zen4 and 1.145 on Zen3, with n=128 and n=256 picking up 7.5% and 5.0% from the same change.</p><h3 id="Triangular-solves-and-the-narrow-B-trsm" tabindex="-1">Triangular solves and the narrow-B <code>trsm</code> <a class="header-anchor" href="#Triangular-solves-and-the-narrow-B-trsm" aria-label="Permalink to &quot;Triangular solves and the narrow-B `trsm` {#Triangular-solves-and-the-narrow-B-trsm}&quot;">​</a></h3><p>The gating band on the <code>getrs</code>/<code>potrs</code>/<code>trtrs</code> rows is for the factorizations. Their solves are not yet gated: they pass OpenBLAS but miss AOCL — <code>getrs</code> 0.93 geomean / 0.85 worst, <code>potrsL</code> 0.94 / 0.78, <code>potrsU</code> 1.13 / 0.91, <code>trtrs</code> 1.07 / 0.88. <code>trsm</code> itself passes OpenBLAS (worst 1.18) while still missing AOCL (worst 0.90).</p><p>Getting there meant fixing three defects in <code>trsm</code>, all of which showed up only once <code>A \\ b</code> with a single right-hand side — the most common solve there is — was measured at all. Each is worth knowing because the shapes recur: 2. The blocked setup is never repaid for narrow B. It costs <code>O(k·nb²)</code> for a triangular inverse of the diagonal blocks, amortised over B&#39;s columns; at k=1024 that was 1123 µs at <code>nrhs=1</code> against 1197 µs at <code>nrhs=8</code>, eight solves for the price of one. Narrow B now sweeps <code>trsv</code> per column, which beat OpenBLAS&#39;s own <code>trsm</code> by 2.9× on that shape.</p><ol start="3"><li><p>A po2-<code>lda</code> A-pad that never paid. When <code>stride(A,2)</code> was a power of two, all of A was copied into an odd-<code>ld</code> scratch to dodge cache-set aliasing — <code>O(k²)</code> work, charged even to a single-column B. Measured across all sixteen combinations of side × uplo × transA × B-width it cost between 3% and 357% and won nowhere. Deleted.</p></li><li><p>A ragged SIMD lane costs a full lane. The kernel processes B&#39;s columns in <code>W</code>-wide lanes, so a partial lane is charged as a whole one: at k=512, <code>nrhs</code> = 1…7 cost 96/180/278/381/464/543/675 µs, about 95 µs per column, while <code>nrhs=8</code> cost 94 µs in total. Widening a ragged B into scratch before the solve collapses that, 2.9–6.8× at k=512.</p></li></ol><p>A residual remains at <code>nrhs = 8</code>, which falls between the two mechanisms — too wide for the per-column sweep, and on Zen3 (<code>W=4</code>) already two full lanes, so the ragged-tail fix does not reach it either.</p><p>The threshold guarding the sweep is worth recording as a tuning lesson. It was first written as <code>nrhs ≤ k/(4·_TRSM_DBASE)</code>, reasoning that a larger <code>k</code> amortises the setup over more columns. That was the wrong shape: blocked costs <code>setup + nrhs·b</code>, the sweep costs <code>nrhs·v</code>, and all three terms scale as <code>k²</code>, so the crossover is <code>k</code>-invariant. The growing rule selected the sweep exactly where it loses, and was itself the cause of the <code>nrhs=8</code> misses it was meant to fix.</p><h3 id="Tridiagonal" tabindex="-1">Tridiagonal <a class="header-anchor" href="#Tridiagonal" aria-label="Permalink to &quot;Tridiagonal {#Tridiagonal}&quot;">​</a></h3><p><code>gtsv</code>, <code>gttrf</code>, <code>gttrs</code>, <code>pttrf</code>, <code>pttrs</code> and <code>ptsv</code> gate against <code>max(OpenBLAS, AOCL)</code> on all three µarchs, every result bit-identical to the reference. Worst case vs AOCL: <code>gtsv</code> 1.20–1.22, <code>gttrf</code> 1.51–1.54, <code>gttrs</code> 1.03–1.06, <code>pttrf</code> 1.11–1.12, <code>ptsv</code> 1.04–1.05. Against OpenBLAS the same ops run 1.31–1.57, and <code>pttrs</code> 1.67–1.73.</p><p>These are <code>O(n)</code> serial three-term recurrences, so the limit is a divide→multiply→subtract latency chain (about 19.5 cyc/elem on Zen4) rather than flops, and every lever was about how data moves around that chain rather than about the arithmetic. Three cost roughly 10 cyc/elem each: a live <code>fadd x, 0.0</code> left on the real path of a complex-shaped expression (LLVM may only fold it under <code>nsz</code>, since <code>x + 0.0</code> is not an identity for <code>x = -0.0</code>); re-reading a recurrence variable the previous iteration had just stored; and a per-element inner loop with a runtime trip count. A fourth, an extra store stream, cost 10.1 and was hoisted into a bulk pass priced at 0.3. Unusually for this project these fixes transfer across µarchs — they are properties of the generated code, not cache- or ISA-dependent tuning.</p><p><code>pttrs</code> is the one cell at parity rather than above it, 0.99 vs AOCL. Both libraries run at about 12.2 cyc/elem, which is exactly the analytic bound: the forward sweep&#39;s recurrence is multiply+subtract at roughly 6 cyc, and the backward sweep&#39;s divide sits off the chain, since <code>B[i]/D[i]</code> does not depend on <code>B[i+1]</code>. That is a shared dependency-chain bound rather than a gap, with 0.0% run-to-run drift. Register-carrying the recurrence and hoisting the <code>uplo</code> test out of both loops were each measured and are neutral, because LLVM already does both.</p><h3 id="Pivoted-QR-geqp3" tabindex="-1">Pivoted QR (<code>geqp3</code>) <a class="header-anchor" href="#Pivoted-QR-geqp3" aria-label="Permalink to &quot;Pivoted QR (`geqp3`) {#Pivoted-QR-geqp3}&quot;">​</a></h3><p>Blocked, and gating on all three boxes (1.11 Zen3 / 1.02 Zen4 / 1.03 Zen5). The first blocked <code>dlaqps</code> port was verified correct but not faster, and larger panels were worse — which was the diagnostic that the cost is the panel&#39;s own per-column BLAS-2 calls rather than the trailing gemm. The shipped version (<code>src/lapack/laqps.jl</code>, wired from <code>geqp3.jl</code>) reaches the kernels pointer-direct, adds a SIMD downdate in the unblocked remainder, and routes the <code>laqps</code> gemv-T through the blocked kernel. See §5 of <a href="./tuning">Tuning</a>.</p><h3 id="Bunch–Kaufman-sytrf/hetrf" tabindex="-1">Bunch–Kaufman (<code>sytrf</code>/<code>hetrf</code>) <a class="header-anchor" href="#Bunch–Kaufman-sytrf/hetrf" aria-label="Permalink to &quot;Bunch–Kaufman (`sytrf`/`hetrf`) {#Bunch–Kaufman-sytrf/hetrf}&quot;">​</a></h3><p>All four variants are blocked: real-symmetric and complex-symmetric through a <code>dlasyf</code> port, Hermitian through a separate <code>zlahef</code> port, both <code>uplo</code>. <code>zlahef</code> is deliberately not <code>dlasyf</code> plus a few conjugations — it realifies the diagonal at points that have no symmetric counterpart, and its 2×2 inverse is asymmetric in the conjugates, with the asymmetry flipping between triangles.</p><p>The row misses one cell: against AOCL on Zen4 the gate sweep reads 1.40 geomean / 0.99 worst, from n=2048 measuring 0.98–1.02 across rounds, which is parity within noise rather than a structural gap. Every other size clears, and on Zen3 both references clear outright (1.34 / 1.04 vs OpenBLAS, 1.49 / 1.17 vs AOCL).</p><p>The panel width is <code>8·⌈√n/8⌉</code> clamped to [16,96] for real — a formula over the problem size, validated on two microarchitectures — times a measured multiplier for complex, because the complex optimum is microarchitecture-dependent (Zen4 selects 1, Zen3 selects 3) where the real one is not. That split was made by measurement: the real formula gates on both boxes, but applied to complex on Zen3 it produced three gate misses at 0.95–0.97.</p><h3 id="Banded-LU-gbtrf" tabindex="-1">Banded LU (<code>gbtrf</code>) <a class="header-anchor" href="#Banded-LU-gbtrf" aria-label="Permalink to &quot;Banded LU (`gbtrf`) {#Banded-LU-gbtrf}&quot;">​</a></h3><p>Blocked, as a port of <code>dgbtrf</code>, with the two corner blocks (A13/A31, whose parts fall outside the stored <code>ldab</code> window) staged dense the way the reference does. Zen4 vs OpenBLAS at <code>kl = ku = n/8</code> reads 1.49 geomean / 0.93 worst, and 1.19 / 0.72 vs AOCL; Zen3 clears OpenBLAS outright at 1.41 / 1.16.</p><p>The single miss is n=128, i.e. <code>kl</code>=16, and it is not a blocking question. OpenBLAS is also unblocked at that width — its ILAENV <code>nb</code>=64 exceeds <code>kl</code>, so <code>dgbtrf</code> takes the <code>dgbtf2</code> branch — so we are losing to its <em>unblocked</em> band downdate. The remaining lever is the quality of <code>_gbtf2!</code>&#39;s rank-1 band update against <code>dger</code>, not the panel width; no <code>nb</code> rescues that cell past about 0.93, measured across <code>nb</code> = 2…16.</p><p>There are two knobs here, and the second exists because of a mistake worth recording. The panel width is a formula over <code>kl</code> (<code>8·(1 + kl÷128)</code>), fitted to the measured optimum at fourteen band widths on Zen4. Shipping only that regressed Zen3 from pass to fail (1.43 / 1.18 → 1.32 / 0.91): at <code>kl</code>=16 blocking wins on Zen4 (0.93 against 0.796 unblocked) but loses on Zen3 (0.91 against ~1.18), because at that width OpenBLAS is unblocked too, so the cell is a scalar-downdate race rather than a blocking race. The blocked-vs-unblocked floor is therefore a measured knob — Zen4 selects 16, Zen3 selects 24 — which restored Zen3 with Zen4 unchanged. That is req#8(b)&#39;s &quot;derive → validate on the fleet → ship&quot; catching a one-box derivation.</p><h3 id="Pivoted-Cholesky-pstrf" tabindex="-1">Pivoted Cholesky (<code>pstrf</code>) <a class="header-anchor" href="#Pivoted-Cholesky-pstrf" aria-label="Permalink to &quot;Pivoted Cholesky (`pstrf`) {#Pivoted-Cholesky-pstrf}&quot;">​</a></h3><p><code>pstrf</code> clears AOCL completely on both Zen4 and Zen5, both triangles, every size (Zen5 worst 1.20 for <code>uplo=&#39;L&#39;</code>, 1.19 for <code>uplo=&#39;U&#39;</code>). Against OpenBLAS it clears everywhere except a narrow window that is consistent across µarchs and mild: Zen4 n=48 at 0.943 and n=64 at 0.921. On Zen5 the window sits at small n instead — <code>pstrfU</code> n=32 at 0.905 is the one cell clear of its own noise (spread 0.022), while <code>pstrf</code> n=8 at 0.876 and <code>pstrfU</code> n=8 at 0.930 both fall within their round-to-round spread (0.289 and 0.142), too noisy to adjudicate and not counted as misses.</p><p>The large-n wins come from batching the pivot row swaps per panel. They are stride-<code>lda</code> and were about 47% of the runtime, so the swap rather than the BLAS-3 update was the bottleneck. ⚠ Float64 only; Zen3 not yet measured for this routine.</p><p>One caveat on reading the published table: the Zen4 gate sweep reports <code>pstrf</code> worst = 0.85 vs OpenBLAS, which is not one of the cells above. It is n=8, where the per-round ratios come out bimodal (1.03 / 1.00 / 0.79 / 1.01 / 1.01 / 0.79 / 0.80 / 1.03). Rounds alternating like that at sub-microsecond scale is the ABBA ordering effect, not a stable regression, and the sweep&#39;s size grid (8, 32, 128, …) skips 48 and 64 entirely, so it never samples the real residual.</p><h3 id="Packed-Cholesky-pptrf" tabindex="-1">Packed Cholesky (<code>pptrf</code>) <a class="header-anchor" href="#Packed-Cholesky-pptrf" aria-label="Permalink to &quot;Packed Cholesky (`pptrf`) {#Packed-Cholesky-pptrf}&quot;">​</a></h3><p>Neither triangle clears both references, and they fail against different ones. <code>uplo=&#39;L&#39;</code> clears OpenBLAS on the Zen4 gate sweep (geomean 1.11, worst 1.04) but misses AOCL (1.17 / 0.93). <code>uplo=&#39;U&#39;</code> is the mirror image: 3.30 / 1.43 vs AOCL, but 1.07 / 0.92 vs OpenBLAS.</p><p>The wide margin against AOCL on <code>uplo=&#39;U&#39;</code> is not evidence of quality. Timing the same reference routine under each library, AOCL&#39;s <code>dpptrf</code> <code>uplo=&#39;U&#39;</code> is 6.0× slower than OpenBLAS&#39;s at n=1024 (175 281 µs against 29 297 µs) — near-stock netlib — while on <code>dpotrf</code> the two sit within 20% of each other. AOCL optimizes dense Cholesky and does not optimize the packed variant. This does not move the gate, which is <code>max(OpenBLAS, AOCL)</code>: a slow AOCL never lowers the bar, it just means <code>max()</code> here equals OpenBLAS.</p><p>The lower path improved once its gap was decomposed. The <code>spr!</code> kernel already ties or beats AOCL at every order, and the whole deficit was per-call overhead — the public entry plus a <code>SubArray</code> built per column — compounded by the lower path reusing the upper path&#39;s cutoff constant, which meant <code>spr!</code> was never called at all at n=32 (worst cell 0.738 → 0.949). See §5.3 of <a href="./tuning">Tuning</a>. The remaining cells are gaps, not ceilings.</p><h3 id="Banded-Cholesky-pbtrf" tabindex="-1">Banded Cholesky (<code>pbtrf</code>) <a class="header-anchor" href="#Banded-Cholesky-pbtrf" aria-label="Permalink to &quot;Banded Cholesky (`pbtrf`) {#Banded-Cholesky-pbtrf}&quot;">​</a></h3><p><code>pbtrf</code> gates fully on Zen4 (Float64, <code>kd</code> = 32…384, n ∈ {1024, 4096}, both triangles: 1.02–3.28× AOCL, 1.49–2.00× OpenBLAS) and beats OpenBLAS on all three µarchs, but does not yet clear AOCL fleet-wide. The residuals are all <code>uplo=&#39;U&#39;</code> at mid bandwidth: Zen5 <code>kd</code>=128 and 256 at 0.989/0.969, and Zen3 <code>kd</code>=128/160/192 at 0.881–0.984, plus <code>uplo=&#39;L&#39;</code> <code>kd</code>=160 and 192 at 0.959–0.995. Zen3 is the worst box and also the one whose <code>_pbtrf_ucross</code> bracket is derived from a narrower <code>_vwidth</code> (W=4, so the switch lands on 192, itself a 0.90 cell), so the leading hypothesis is a candidate bracket putting the optimum at an edge — the same failure this project&#39;s history records for the wide-band panel bracket. Not yet confirmed by measurement. Correctness is clean on all three boxes (11368/11369, identical).</p><p><code>uplo=&#39;U&#39;</code> dispatches between two kernels at a measured bandwidth crossover: a conj-transpose re-pack onto the much faster lower kernel for narrow bands, and a native upper-storage port once the re-pack&#39;s diagonal walk starts to dominate. The panel width likewise has two measured regimes — clamping the wide-band width to <code>kd</code> collapses the in-band panel, and was the routine&#39;s only Zen4 gate miss. See §5.2 and §5.3 of <a href="./tuning">Tuning</a>.</p><h3 id="Symmetric-/-Hermitian-eigensolver" tabindex="-1">Symmetric / Hermitian eigensolver <a class="header-anchor" href="#Symmetric-/-Hermitian-eigensolver" aria-label="Permalink to &quot;Symmetric / Hermitian eigensolver {#Symmetric-/-Hermitian-eigensolver}&quot;">​</a></h3><p>With eigenvectors (<code>jobz=&#39;V&#39;</code>) it clears both references on the Zen4 sweep: 1.32 / 1.25 vs OpenBLAS, 1.36 / 1.19 vs AOCL. Values-only (<code>jobz=&#39;N&#39;</code>) does not — 1.13 / 0.98 vs OpenBLAS, though 1.31 / 1.14 vs AOCL — so the coverage row is below 1.0 on the strict worst-case rule, driven by a single size.</p><p>Three Level-3 pieces get the vectors path there: the two-sided reduction (<code>sytrd</code>/<code>hetrd</code>) is blocked <code>dlatrd</code> + <code>syr2k</code>/<code>her2k</code>; the eigenvector back-transform (<code>ormtr</code>/<code>unmtr</code>) uses compact-WY block reflectors; and the divide-and-conquer solver (<code>stedc</code>) assembles eigenvectors once, with no per-merge column copies, and combines survivors with the <code>dlaed3</code> COLTYP two half-height gemms — exploiting the block-diagonal zero structure for roughly half the combine flops, matching libFLAME.</p><h2 id="Complex" tabindex="-1">Complex <a class="header-anchor" href="#Complex" aria-label="Permalink to &quot;Complex {#Complex}&quot;">​</a></h2><p>The complex surface is SIMD across all levels, in portable SIMD.jl kernels with no x86 intrinsics; the generic scalar path remains for AD.</p><p><strong>CL1</strong> is at parity or better fleet-wide. <code>dznrm2</code> is 7–9× (the same scaled-accumulation story as real <code>nrm2</code>) and <code>izamax</code> 1.2–1.8×. Residuals: <code>zaxpy</code> at the L3 edge (0.94–0.96) and <code>zdotc</code>/<code>zdotu</code> on Zen5 at DRAM sizes (0.89–0.94, bandwidth).</p><p><strong>CL2</strong> gates broadly on all three boxes. Remaining residuals cluster at n=1024 on the AVX-512 boxes: <code>ztrsv</code> (0.948 Zen4, 0.936 Zen5) and <code>ztrmv</code> (0.948 Zen4), a coupled upper/lower-triangular tradeoff, alongside the complex gemv trio <code>zgemvN</code>/<code>zgemvT</code>/<code>zgemvC</code> (0.848–0.877 Zen4, 0.935–0.951 Zen5). <code>zgeru</code> dips at mid/large n (0.817 Zen4 at n=1024, 0.857 Zen5 at n=4096), and <code>zhemv</code> misses only on Zen3 at large n (0.919 at n=2048, near the DRAM roofline) while gating 1.9–2.5× on both AVX-512 boxes.</p><p><strong>CL3.</strong> <code>zgemm</code> beats OpenBLAS fleet-wide (geomean 1.26–1.40, Karatsuba 3M at mid/large n), and the rank-k ops gate within a few percent.</p><h3 id="ztrsmR-—-codegen,-not-blocking" tabindex="-1"><code>ztrsmR</code> — codegen, not blocking <a class="header-anchor" href="#ztrsmR-—-codegen,-not-blocking" aria-label="Permalink to &quot;`ztrsmR` — codegen, not blocking {#ztrsmR-—-codegen,-not-blocking}&quot;">​</a></h3><p>Worth keeping because the mechanism is invisible at the source level and easy to reintroduce. The register-tile leaf wrote its update as <code>muladd(V2(-cr), xv, …)</code>, putting the negation on the <em>scalar</em> coefficient between its load and its splat. That blocks both the <code>vfnmadd</code> opcode and AVX-512&#39;s embedded broadcast <code>(mem){1to8}</code>, so LLVM emitted <code>vmovsd</code> + <code>vxorpd</code> + <code>vbroadcastsd</code> per coefficient — 96 instructions of coefficient preparation against 64 useful FMAs. Negating the B vector once per step instead is exact, since <code>nswap(-v) == -nswap(v)</code> means the swapped operand needs no separate handling, and it took the loop from 2.95 to 1.96 instructions per useful FMA with residuals identical to the last digit.</p><p>The cell was attributed before it was patched: it fits <code>1/GF = α + β/n</code> with an 83.5 GF asymptote, predicting n=192 to 0.2%, so it was a 14% per-call fixed cost rather than a size-specific conflict. The same rewrite applied to the complex side-L slab compiled byte-identical, because LLVM had already chosen <code>vfnmadd</code> there on its own. Remaining: Zen3 n=128 at 0.926 and Zen4 n≥512 at about 0.99.</p><h3 id="Complex-trmm-small-n" tabindex="-1">Complex <code>trmm</code> small-n <a class="header-anchor" href="#Complex-trmm-small-n" aria-label="Permalink to &quot;Complex `trmm` small-n {#Complex-trmm-small-n}&quot;">​</a></h3><p>Open cells here are smaller than the raw ratios suggest, and <code>bench/gate_gaps.jl</code> reports each one&#39;s shortfall against its own round-to-round spread:</p><ul><li><p><code>ztrmmR</code> n=32 on Zen3 — 0.839, a 16.3% gap against a 0.047 spread. Real.</p></li><li><p><code>ztrmm</code> n=128 on Zen3 vs AOCL — 0.917 at a spread of 0.003, the most tightly measured miss in the whole table. Real, and on the packed path.</p></li><li><p><code>ztrmm</code> n=32 on Zen3 — a 9.5% shortfall against a 0.115 spread. That cell is within spread: it needs more rounds, not engineering, and optimising against it would be optimising against measurement error.</p></li></ul><p>Also open, and outside their spreads: <code>ztrsm</code> 0.89 at n=128, and a <code>zsymm</code>/<code>zhemm</code>/rank-2k small-n dip from materialize-plus-microkernel overhead on the small-n complex-L3 path.</p><p>That residual is decomposed rather than guessed at. Holding <code>k</code> at 32 and varying only the number of columns of <code>B</code> separates per-call from per-column cost through the public entry point, by fitting <code>1/GF = a·(1/c) + b</code>. At the gate shape <code>ztrmm</code> carries 15.9% fixed per-call cost, while <code>zgemm</code> at the same <code>k</code> — same kernel family, no triangle — carries 0.7% and holds its rate flat to within 0.8% across its whole sweep. Shared BLAS-3 entry cost is therefore negligible, and essentially all of <code>trmm</code>&#39;s fixed cost is the O(k²) triangle materialize. A direct-read fused-triangle base closed these dips in development but its runtime <code>Val</code> args broke trim-safety and it was reverted; the trim-safe refix, using compile-time <code>Val</code> dispatch, is the follow-up, and this puts its value at about 15% against the 2.9–14.8% the individual cells need.</p><p>A second, unrelated inefficiency shows in the same fit: with the triangle amortized to nothing, <code>trmm</code> still costs 0.1535 µs per column against <code>gemm</code>&#39;s 0.2037 for half the multiply-accumulates, so 66% efficiency. That is the granularity of the K-TRIM tiling in steady state, not a fixed cost, and the triangle base would not address it.</p><h3 id="The-3M-routing-constant" tabindex="-1">The 3M routing constant <a class="header-anchor" href="#The-3M-routing-constant" aria-label="Permalink to &quot;The 3M routing constant {#The-3M-routing-constant}&quot;">​</a></h3><p>Complex BLAS-3 lost to AOCL almost across the board until one routing constant was removed, and it is worth recording because the mistake is a general one.</p><p><code>zsyrk</code>/<code>zherk</code>/<code>zsyr2k</code>/<code>zher2k</code> read 0.94–0.99 against AOCL on Zen4 while the same source read 1.19–1.31 on Zen3. That inversion was not a microarchitecture property: the source routed a different <em>algorithm</em> per SIMD width. The Karatsuba-3M route — three real products on the split real/imaginary parts, 25% fewer flops than the direct four-FMA complex kernel — was enabled only for <code>_vwidth == 4</code>, so Zen3 got the flop cut and Zen4 did not. The default&#39;s own comment called AVX-512 &quot;untested&quot; rather than falsified.</p><p>Nothing physical made it width-dependent. The 25% reduction is algebraic, and 3M&#39;s split/combine overhead is O(n²) against O(n²·k) of product, which the existing size window already bounds, so the test was an artefact of only ever having measured AVX2. Deleting it took <code>zgemm</code> from 0.952 to 1.213 at n=128 and 1.000 to 1.255 at n=2048, and converted every complex rank-k cell at n ≥ 256. The secondary effect was larger than the primary one: everything layered on complex gemm moved without being touched — <code>zhemm</code> 1.004 → 1.136, <code>zsymm</code> 0.994 → 1.146, <code>zherk</code> 0.982 → 1.053, <code>ztrsmR</code> 0.987 → 1.084. Zen3, which already ran 3M, was flat across the same sweep, which is the control saying the change did one thing only.</p><p>The rank-k window&#39;s lower edge turned into a lesson about knobs rather than kernels. The crossover carries a ratio of <em>our own two microkernels&#39;</em> rates — the complex kernel is latency-bound on AVX2 and throughput-bound on AVX-512 — so no cache or ISA constant predicts it, every throughput model gives the wrong sign, and the measured crossovers were far apart (about 80 on Zen4, about 160 on Zen3), so no single value existed. Rather than locate a threshold that could not exist, the overhead was removed: the third Karatsuba panel is now derived from the packed panels, since the complex pack already deinterleaves into two real panels and the third is one add in a loop that exists, and the combine happens per micro-tile into a small scratch instead of via n×n buffers.</p><p>That did not eliminate the threshold, and the reason matters. The pack itself is O(n·k), and 3M packs three panels where the direct kernel packs two; three real products structurally require three real panels. What fusion bought was moving the crossover (about 160 → 112 on Zen3, below 64 on Zen4) until one value finally served both machines. At n=128 fused-vs-packed measured 0.886/0.887 on Zen4 and 0.956/0.963 on Zen3 — both win — so 128 ships, labelled in the source as the empirical bound it is rather than dressed up as a derivation.</p><p>What remains is n = 32 and below, outside the window on every machine: <code>zsyr2k</code> 0.910 and <code>zher2k</code> 0.906 on Zen4, <code>zgemm</code> 0.902, and <code>ztrmm</code> 0.972. Complex rank-2k also has a second, independent fence on AVX2, which is why Zen3&#39;s <code>zsyr2k@128</code> did not move; that path needs the rank-2k reformulation — one product plus a symmetrized add — rather than a window change.</p><h3 id="Complex-symm/hemm" tabindex="-1">Complex <code>symm</code>/<code>hemm</code> <a class="header-anchor" href="#Complex-symm/hemm" aria-label="Permalink to &quot;Complex `symm`/`hemm` {#Complex-symm/hemm}&quot;">​</a></h3><p>On Zen4 <code>zhemm</code> gates at every size from 32 up (1.004–1.012) and <code>zsymm</code>&#39;s three remaining shortfalls are each smaller than that cell&#39;s own round-to-round spread; on Zen3 both gate across 128–2048 at 1.18–1.28. Complex symm used to materialize a dense n×n copy of the symmetric operand and call gemm, while complex hemm avoided the copy but ran through a fork of the complex gemm driver that had drifted, hardwiring off the two specializations — overwrite-C when β=0, and the α=1 store — that the driver itself dispatches, which is precisely the case the benchmark measures. Wiring those back in and routing complex symm through the same packing path removed the copy entirely, taking wrapper cost against a bare gemm of the same shape from 1.065/1.069/1.053/1.026 at n=128/256/512/1024 to 1.026/1.017/1.004/1.002.</p><p>What is left is tiny-n — n=8 and n=32, below the packing cut, still on the copy path — and real <code>symm</code> at n≥256, whose packed path has not yet had the same β=0 fold applied.</p><h3 id="Complex-LAPACK" tabindex="-1">Complex LAPACK <a class="header-anchor" href="#Complex-LAPACK" aria-label="Permalink to &quot;Complex LAPACK {#Complex-LAPACK}&quot;">​</a></h3><p><code>zpotrf</code> and <code>zgetrf</code> gate fleet-wide. <code>zgetrf</code>&#39;s only residual is Zen3 n=256 at 0.94, localized to PB&#39;s complex gemm at the trailing 208×208×48 shape — a gemm-kernel gap rather than a getrf-structure one.</p><p><code>zgeqrf</code> gates at every size fleet-wide (1.07–1.83). The complex trailing update mirrors the real path (<code>herk</code> for VᴴV at half the flops, plus <code>trmm</code> for TᴴW), the reflector norm moved from per-element <code>hypot</code> to SIMD <code>_nrm2</code>, the unblocked rank-2 panel runs while the matrix is L3-resident, and the AVX2 trailing update is chunked to bound the Karatsuba-3M scratch below ½L3. It was validated on square inputs; tall/skinny QR is a separate benchmark shape.</p><p><code>zgesvd</code> — singular values only, against <code>zgesdd(&#39;N&#39;)</code> — gates at every size for real and ComplexF64. The large-n collapse was fixed by a blocked complex bidiagonalization (<code>zlabrd</code>/<code>zgebrd</code> port), and the small-n floor by switching the values path to dqds (<code>dlasq1–6</code>): OpenBLAS&#39;s <code>gesdd(&#39;N&#39;)</code> uses the sqrt-free dqds recurrence rather than Golub–Kahan QR, and matching it lifted n=32 from about 0.86 to about 1.07 for real and complex alike, since the bidiagonal is real. ComplexF32 small-n (n=32/48 ≈ 0.9) is the remaining sub-gate, a <code>cgebrd</code> limit.</p><h2 id="Per-microarchitecture" tabindex="-1">Per microarchitecture <a class="header-anchor" href="#Per-microarchitecture" aria-label="Permalink to &quot;Per microarchitecture {#Per-microarchitecture}&quot;">​</a></h2><p>Which routines carry residuals differs per box — that is why the gate is per-machine rather than pooled. Current ratios are in the generated tables; what follows is which families to look at.</p><p><strong>Zen4 (AVX-512, double-pumped).</strong> The tuning target. Real BLAS-1/2 gate essentially everywhere, and the residuals are the BLAS-3 set above: <code>trsm</code> at mid-n, <code>syrk</code> at n=4096, <code>trmm</code> and <code>symm</code>. The complex residuals are the shared LAPACK gaps plus a handful of BLAS-2 cells — <code>zgeru</code>, <code>zgemvN</code> and <code>ztrsv</code>, all against AOCL rather than OpenBLAS.</p><p><strong>Zen5 (AVX-512, native 512-bit).</strong> Clears every AVX2 ceiling but shows a disjoint residual profile. Open: <code>gemvN</code>, which is the deepest BLAS-2 miss anywhere on the fleet, <code>ger</code>, and the complex pair <code>zgemvN</code>/<code>zgeru</code>, all at large n.</p><p><strong>Zen3 (AVX2).</strong> The hardest target: 16 ymm registers against AVX-512&#39;s 32 zmm. The real surface gates except the <code>trmm</code>/<code>trsm</code> worst sizes; complex carries the widest residual set (<code>zdot</code>, <code>ztrmm</code> both sides, <code>ztrsm</code>, <code>zgetrf</code>). The <code>potrf</code> small-n work reaches BLASFEO column-major parity at n≤224 (0.91–1.04×), and 1.5–2.2× vs OpenBLAS.</p><h3 id="Zen5&#39;s-partial-complex-L3-transfer" tabindex="-1">Zen5&#39;s partial complex-L3 transfer <a class="header-anchor" href="#Zen5&#39;s-partial-complex-L3-transfer" aria-label="Permalink to &quot;Zen5&#39;s partial complex-L3 transfer {#Zen5&#39;s-partial-complex-L3-transfer}&quot;">​</a></h3><p>When the 3M change landed, all eleven of Zen5&#39;s complex-L3 cells rose and five crossed the gate, but they split cleanly in two. The <code>symm</code>/<code>hemm</code>/<code>trsmR</code> family transferred in full (<code>zsymm</code> 0.954 → 1.170 against Zen4&#39;s 0.994 → 1.146; <code>zhemm</code> 0.979 → 1.153; <code>ztrsmR</code> 0.957 → 1.047), while <code>zgemm</code> and <code>zsyrk</code> transferred only partially — 0.947 → 0.996 and 0.949 → 0.969, against Zen4&#39;s 0.952 → 1.213 and 0.958 → 1.133.</p><p>That split is a genuine microarchitectural signal, and its cause is currently unknown. It is <em>not</em> an FMA-throughput difference, which was the obvious guess and has been measured and ruled out. Taking each box&#39;s classical-gemm OpenBLAS arm as an honest throughput probe — the PureBLAS arm cannot serve here, since Strassen–Winograd makes <code>2n³/t</code> overstate the hardware FMA rate — all three boxes sustain the same per-cycle width: 6.84, 7.48 and 6.89 F64 FMA lanes per cycle on Zen3, Zen4 and Zen5, an 8-lane ceiling everywhere. Zen5 is therefore not less FMA-bound than Zen4; it has the same lanes per cycle at a lower clock, giving it the fleet&#39;s lowest F64 peak (31.7 GF against Zen4&#39;s 44.9 and Zen3&#39;s 58.9 — the narrowest-ISA box has the highest peak because it clocks highest). Untested candidates for the real cause: Zen5&#39;s 48 KiB L1d, unique in the fleet, and the lower clock shifting the memory-versus-compute balance.</p><h2 id="vs-AOCL-—-the-residuals" tabindex="-1">vs AOCL — the residuals <a class="header-anchor" href="#vs-AOCL-—-the-residuals" aria-label="Permalink to &quot;vs AOCL — the residuals {#vs-AOCL-—-the-residuals}&quot;">​</a></h2><p>PureBLAS matches or beats AMD&#39;s own library on gemm and every LAPACK factorization, real and complex, by 5–80%. Verified PB/AOCL at mid/large n (&gt;1 means PureBLAS is faster; Zen4, single-thread), with PB/OpenBLAS alongside for context:</p><table tabindex="0"><thead><tr><th style="text-align:right;">op</th><th style="text-align:right;">PB / OpenBLAS</th><th style="text-align:right;">PB / AOCL</th></tr></thead><tbody><tr><td style="text-align:right;">gemm</td><td style="text-align:right;">1.05–1.11</td><td style="text-align:right;">1.08–1.24</td></tr><tr><td style="text-align:right;">potrf</td><td style="text-align:right;">1.16–1.28</td><td style="text-align:right;">1.12–1.20</td></tr><tr><td style="text-align:right;">geqrf</td><td style="text-align:right;">1.63–1.80</td><td style="text-align:right;">1.53–1.70</td></tr><tr><td style="text-align:right;">getrf</td><td style="text-align:right;">1.14–2.24</td><td style="text-align:right;">1.59–1.78</td></tr><tr><td style="text-align:right;">gtsv</td><td style="text-align:right;">1.41–1.43</td><td style="text-align:right;">1.20–1.22</td></tr><tr><td style="text-align:right;">gttrf</td><td style="text-align:right;">1.53–1.57</td><td style="text-align:right;">1.51–1.54</td></tr><tr><td style="text-align:right;">pttrf</td><td style="text-align:right;">1.11–1.13</td><td style="text-align:right;">1.11–1.12</td></tr><tr><td style="text-align:right;">ptsv</td><td style="text-align:right;">1.31–1.33</td><td style="text-align:right;">1.04–1.05</td></tr><tr><td style="text-align:right;"><strong>pttrs</strong></td><td style="text-align:right;">1.67–1.73</td><td style="text-align:right;"><strong>0.99</strong> (shared chain bound)</td></tr><tr><td style="text-align:right;"><strong>trsm</strong> (side-L)</td><td style="text-align:right;">1.13–2.73</td><td style="text-align:right;"><strong>0.93–1.07</strong></td></tr><tr><td style="text-align:right;"><strong>trsmR</strong> (side-R)</td><td style="text-align:right;">1.03–2.29</td><td style="text-align:right;">0.97–1.73</td></tr></tbody></table><p>The one residual against AOCL is side-L <code>trsm</code> at mid-n, worst size 0.92–0.97. It is a codegen-scheduling gap on the fused-leaf kernel — the fused back-substitution&#39;s latency chain runs at about 2 IPC where BLIS&#39;s hand-scheduled assembly hides it — rather than a structural lever: building AOCL&#39;s own packed-triangle structure in portable SIMD.jl lands at the same ~0.95.</p><p>Side-R <code>trsmR</code>, the potrf/getrf panel shape, routes both <code>transA</code> through the fused leaf via the reflection identity Ã = J·Aᵀ·J. Its AVX2 worst cell is now 0.917, and the mechanism is worth keeping: at <code>transA=&#39;T&#39;</code> the reflection branch does not fire, so A reaches the leaf verbatim at the caller&#39;s <code>lda</code>; for a square operand that is a power of two, and at k=128 the 1024-byte column stride is a quarter L1 way period. Padding A into an odd-<code>ld</code> scratch recovers 11.5–15.1% at the leaf. The pad is gated on whether the leaf actually re-reads A — that is, whether it spills, true on AVX2&#39;s 16 ymm and false on AVX-512&#39;s 32 zmm — because on a non-spilling box the same pad measured null at n≥512 and 4.9% slower at n=128. Zen4 therefore never pads; its worst size remains about 0.95 at n=2048.</p><h2 id="Where-the-misses-are" tabindex="-1">Where the misses are <a class="header-anchor" href="#Where-the-misses-are" aria-label="Permalink to &quot;Where the misses are {#Where-the-misses-are}&quot;">​</a></h2><p>36 of the 83 measured rows clear <code>≥ max(OpenBLAS, AOCL)</code> at every size. The rest miss somewhere, usually narrowly — 0.9–0.99 at one or two sizes, often the smallest, where per-call overhead dominates and the measurement is least stable.</p><p>Which cells those are is not listed here on purpose. The per-routine worst cells live in <a href="https://github.com/el-oso/PureBLAS.jl/blob/master/bench/gen_table.md" target="_blank" rel="noreferrer"><code>bench/gen_table.md</code></a> and <a href="https://github.com/el-oso/PureBLAS.jl/blob/master/bench/gen_table_aocl.md" target="_blank" rel="noreferrer"><code>bench/gen_table_aocl.md</code></a>, both generated from the caches, and <code>bench/gate_gaps.jl</code> prints each shortfall against that cell&#39;s own round-to-round spread — which is the number that says whether a cell is worth engineering or just needs more rounds. A hand-copied list here would go stale against those tables, and has.</p><h2 id="Known-open-items" tabindex="-1">Known open items <a class="header-anchor" href="#Known-open-items" aria-label="Permalink to &quot;Known open items {#Known-open-items}&quot;">​</a></h2><p>Tracked in <a href="https://github.com/el-oso/PureBLAS.jl/blob/master/ROADMAP.md" target="_blank" rel="noreferrer"><code>ROADMAP.md</code></a>:</p><ul><li><p><strong>BLAS-3 conversion gap</strong> (above): <code>gemm</code> beats AOCL by 25–31% at n=4096 while <code>syrk</code>/<code>syr2k</code> on the same engine sit at 0.94–0.97. Largest shared lever, and it reproduces on all three µarchs.</p></li><li><p><code>trsm</code> mid-<code>n</code> (Zen4 0.941 at n=128). The whole deficit is an 18.6% per-slab fixed cost, of which an opcode histogram attributes about 22% to scalar addressing and 24% to vector moves, against ~3.2% of total leaf time for the transposes and ~2.8% for the back-substitution. The Zen5 figure predates the tiny-<code>n</code> fixes, since that box has not been re-swept.</p></li><li><p><code>gemvT</code> misses against AOCL on all three µarchs at its worst size, which makes it a size-specific behaviour rather than three per-box stories, and the better-posed problem for it.</p></li><li><p><code>gemvN</code> on Zen5 is the deepest BLAS-2 miss on the fleet, against both references, with <code>ger</code> on the same box behind it.</p></li><li><p>The <code>trmv</code> residual on Zen5 has moved to n=256; the DRAM-regime cells now gate after the fuse-factor routing landed.</p></li><li><p><code>hpmv</code> is still per-column — port the spmv AP-residency panel to complex.</p></li><li><p><code>trsm</code>/<code>ztrsm</code> side-L remain the flagship AOCL gaps (4K power-of-two aliasing in the column-lane back-substitution). Side-R mostly clears, leaving <code>ztrsmR</code> at Zen3 n=128 (0.926) and Zen4 n≥512 (about 0.99).</p></li><li><p>Complex LAPACK: <code>zgeqrf</code> worst size is still just under gate on Zen5, and the <code>zgesvd</code> blocked-bidiagonalization port is pending for values-only, capped at n=1024.</p></li><li><p>Tuning-constant debt: several block-size literals remain to be re-derived as formulas over detected cache and register parameters.</p></li></ul>',105)])])}const g=t(s,[["render",n]]);export{m as __pageData,g as default};
