import{_ as i,o as a,c as t,ao as s}from"./chunks/framework.CJX_rJCp.js";const k=JSON.parse('{"title":"Design","description":"","frontmatter":{},"headers":[],"relativePath":"design.md","filePath":"design.md","lastUpdated":null}'),n={name:"design.md"};function o(r,e,d,l,c,h){return a(),t("div",null,[...e[0]||(e[0]=[s('<h1 id="Design" tabindex="-1">Design <a class="header-anchor" href="#Design" aria-label="Permalink to &quot;Design {#Design}&quot;">​</a></h1><h2 id="One-kernel-set,-two-ways-in" tabindex="-1">One kernel set, two ways in <a class="header-anchor" href="#One-kernel-set,-two-ways-in" aria-label="Permalink to &quot;One kernel set, two ways in {#One-kernel-set,-two-ways-in}&quot;">​</a></h2><p>The kernels are written once, in BLAS-native <code>(n, …, inc)</code> form, over a small accessor interface — <code>_ld</code> and <code>_st!</code> in <code>core.jl</code> — that reads and writes uniformly through either a <code>Ptr{T}</code> or an <code>AbstractVector{T}</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@inline</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _ld</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Ptr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unsafe_load</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(p, i)</span></span>\n<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@inline</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> _ld</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Integer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @inbounds</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a[i]</span></span></code></pre></div><p>That is the whole trick behind having two front ends without two implementations. Two layers sit on top of the same kernels:</p><p>The <strong>native API</strong> (<code>backend.jl</code>, <code>native.jl</code>) exposes ergonomic <code>AbstractArray</code> methods on a <code>SIMDBackend</code>, which satisfies a TypeContracts interface. There is no <code>ccall</code> boundary anywhere in the call tree, so it is all plain Julia the compiler can see into — which is what lets <code>ForwardDiff.Dual</code> flow through.</p><p>The <strong>C ABI</strong> (<code>cabi/</code>) exposes 167 <code>@ccallable</code> ILP64 reference-BLAS and LAPACK symbols — <code>daxpy_64_</code> and the rest, arguments by reference, column-major, <code>Int64</code>. These serve two consumers. <code>juliac --trim</code> compiles them into <code>libpureblas.so</code> for C, C++ and Rust hosts, and <code>activate()</code> registers in-process <code>@cfunction</code> pointers to the same entry points so LinearAlgebra dispatches to them without any shared library involved.</p><p>The implementation spans BLAS levels 1 through 3 — dense, packed and banded — and about forty LAPACK files covering the factorizations, solves, least squares, SVD and the eigensolvers.</p><h2 id="Generic-over-T:Number" tabindex="-1">Generic over <code>T&lt;:Number</code> <a class="header-anchor" href="#Generic-over-T:Number" aria-label="Permalink to &quot;Generic over `T&lt;:Number` {#Generic-over-T:Number}&quot;">​</a></h2><p>One implementation covers <code>s</code>, <code>d</code>, <code>c</code> and <code>z</code>, and anything else numeric. Unit-stride dense inputs dispatch to a SIMD.jl path using <code>Vec{N,T}</code> at the detected register width; everything else — strided data, unusual element types — runs the generic scalar loop. Keeping that scalar path around, rather than specialising it away, is what makes the library differentiable.</p><p>Width detection covers AVX-512 and AVX2, and falls back to 16 bytes where it cannot identify the host, which is the right answer for SSE2 and NEON. The benchmark fleet is x86-64 only, so treat ARM as untested rather than supported.</p><h2 id="Why-the-shared-library-cannot-be-forwarded-into-live-Julia" tabindex="-1">Why the shared library cannot be forwarded into live Julia <a class="header-anchor" href="#Why-the-shared-library-cannot-be-forwarded-into-live-Julia" aria-label="Permalink to &quot;Why the shared library cannot be forwarded into live Julia {#Why-the-shared-library-cannot-be-forwarded-into-live-Julia}&quot;">​</a></h2><p>A <code>juliac --trim</code> library embeds the Julia runtime. Its <code>@ccallable</code> entry points are wrapped in <code>ijl_autoinit_and_adopt_thread</code>, which initializes that runtime lazily on the first call — exactly what a C host needs. But calling <code>BLAS.lbt_forward</code> on it from inside a running Julia process makes LBT call a probe symbol (<code>isamax_64_</code>) while autodetecting the interface, and that second initialization of the shared <code>libjulia</code> aborts the process with signal 6.</p><p>So the division is:</p><ul><li><p>Inside Julia, use <code>activate()</code>. It forwards <code>@cfunction</code> pointers to the in-process kernels and needs no <code>.so</code> at all.</p></li><li><p>The <code>.so</code> is for non-Julia hosts, and keeping it building doubles as a standing check that the kernels stay trim-compatible. <code>juliac/ctest.c</code> exercises it from C on every push.</p></li><li><p>Getting <code>lbt_forward</code> to work would need upstream juliac support for initializing against the host runtime, or a codegen path with no embedded runtime. Tracked in <code>ROADMAP.md</code>.</p></li></ul>',15)])])}const u=i(n,[["render",o]]);export{k as __pageData,u as default};
